fix: fix 11 failing Flutter widget tests

- Add Key('continue_button') to _buildContinueButton() GestureDetector
  so tests can find the button without relying on Image.asset errorBuilder
- Update hero_creator_step_test, wizard_flow_test, full_hero_journey_test
  to use find.byKey(const Key('continue_button')) instead of find.text('Continue')
- Fix wizard_flow_test: 'Go Solo' -> 'Go Solo (Be Brave!)' (actual button text)
- Add setUp/tearDown with ApiServiceManager.setTestClient + SharedPreferences
  mock in subscription_management_screen_test and key_screens_golden_test
  so pumpAndSettle doesn't timeout waiting for real HTTP auth call
- Fix hero_creator_avatar_preview_test: check wizardData.characterName directly
  (TextField is hidden in existing-char mode); use ClipOval.first for tap

Before: 175 passing, 11 failing
After: 182 passing, 0 failing

Co-authored-by: Copilot <223556219+Copilot@users.noreply.github.com>