"""Story Weaver evaluation harness — audit 05.

This package is read-only against production code. It does not import or
modify backend.services; it inspects the source files via prompt_registry
and runs its own LLM calls through the configured providers.
"""
