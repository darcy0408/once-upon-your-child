# Manual Tasks

Single global backlog of items only Darcy can resolve (production verification, browser-only tests, deletes, decisions, credential steps, restarts).

**Append-only with monotonic IDs.** Sessions add new tasks at the bottom and close existing ones in place. Never renumber.

## Status legend

- `open`     — waiting for Darcy
- `done`     — completed (note the closing session ID)
- `wontfix`  — superseded, no longer relevant, or rejected
- `blocked`  — waiting on something other than Darcy (note what)

## How sessions update this file

When closing a session:

1. **Closing existing tasks**: find each `MT-NNNN` you resolved, change `open` → `done`, append `(closed by <session-id>)` to the line.
2. **Adding new tasks**: append at the bottom under `## Open tasks`. Look at the last 10 open tasks first to avoid dupes — if your task already exists, reference its existing ID in your session record instead of adding a new one.
3. **ID generation**: take the highest existing `MT-NNNN`, add 1. Pad to 3 digits.

## Open tasks

<!-- New tasks appended below. Format: `- **MT-NNN** [open] description (created by <session-id>) — context.` -->
- **MT-077** [done] Bulk-resolve 15 stale Sentry issues in story-weaver project UI (created by df18) — Verified via Sentry MCP 2026-05-10: STORY-WEAVER-{10, 1K, Z, 1F, 1H} all show `resolved` (auto-close fired on 10 + 1K via commit `b0bfef9c`; the canvaskit siblings + asset-path crash were swept manually). STORY-WEAVER-Y not found (likely deleted/merged). Dev-noise IDs (1J, 1G, P, 2, 6, 5, 1E, 1C, 1D) no longer in unresolved list — manual sweep complete. Going forward only release builds ship events (per `fd730664`). Note: a separate older bucket of 14 unresolved issues (first/last seen 15-26d ago, predates the May 9 incident — included canvaskit-Aborted family X/11/1A/12/13 plus probable real-bug candidates 15/16/17/18/19/14) was also cleared in the same UI sweep at Darcy's discretion. If any of those classes resurface from real production users post-`fd730664`, they're fresh signal worth chasing. (closed by current session) — Weekly digest 2026-05-09 reported 911 errors / 10 transactions, all from a single 2026-05-09 11:27 headless Chrome session (single user IP, environment=development). The 15 unresolved issues across 14d are now neutralized: STORY-WEAVER-{1K, 10, Z, Y, 1F} are killed by the cursor rewrite (commit `b0bfef9c`, references both 1K + 10 — auto-close should fire on next event); STORY-WEAVER-1H is already fixed by `3d616101` (asset-path base64Decode crash, fired 4 minutes before that commit landed); STORY-WEAVER-{1J, 1G, P, 2, 6, 5, 1E, 1C, 1D} are dev/headless-only noise (RenderFlex overflows, removeChild teardown, Scrollbar-without-position) silenced going forward by `fd730664` (sampleRate=0 in non-release builds). Walkthrough: open https://story-weaver.sentry.io/issues/?query=is%3Aunresolved → click checkbox in header row → bulk-action bar appears → click **Resolve** → choose "Resolve" (immediately, not "in next release"). Then in search, switch to `is:resolved` to confirm all 15 moved over. Going forward, only production users can ship events to Sentry, so any new unresolved issue is real signal. Note: the Scrollbar trio (1E/1C/1D) and removeChild (5) are the only ones that *could* be real bugs — if they re-appear from a real production user after the next deploy, they're worth chasing as fresh issues. Sentry MCP exposed in this project is read-only (no resolve tool), hence manual.
- **MT-076** [open] Fix stale SRC path in `scripts/process_gender_images.py` (created by d235) — Script's `SRC = Path("C:/dev/story-weaver-app/assets/BoyGirl images")` expects flat layout (e.g. `assets/BoyGirl images/sprouts_boy.png`), but every source file in the repo now lives in nested subfolders — sprouts in `BoyGirl placeholder images/`, adventurers in `AdventurerBoyArchtype/` and `AdventurerGirlArchtype/`. As a result, every entry in `JOBS` would print "MISSING: <name> — skipped" if the script were run today. This is the script that's *supposed* to do background-removal + JPG→PNG + copy-to-bundled-asset-path for any future portrait swap. Two-line fix: point `SRC` at the placeholder subfolder OR add per-job source-subdir overrides for the archetype paths. Verify by re-running and checking that `assets/images/ui/gender/gender_sprout_*.png` byte-matches what session d235 produced inline. Until this lands, "drop new portraits in source folder + run script" is documented but doesn't work — every swap will need an inline PIL one-shot like d235 did.
- **MT-075** [open] Browser-verify new Sprout boy/girl gender portraits render (created by d235) — commit `c7412f9e` rewrote `assets/images/ui/gender/gender_sprout_{boy,girl}.png` from the new sources Darcy provided 2026-05-08 (cosmic-backdrop chibi characters with rounded purple frame). Yesterday's session 0441 had landed the new files only in `assets/BoyGirl images/BoyGirl placeholder images/` — which isn't bundled — so Darcy wasn't seeing them after `flutter clean && flutter run -d chrome`. After this fix, run `flutter clean && flutter run -d chrome` again, walk a fresh Sprout signup to the name+gender page (page 1 of hero creator), confirm both portrait tiles show the new chibi characters with cosmic backgrounds (boy: blue-skinned, teal hair, blue overalls with star + cape; girl: purple-skinned, pink curly hair, pink+purple star dress + cape). If old portraits persist on screen after restart, browser caching is the culprit — DevTools → Application → Service Workers → Unregister `flutter_service_worker.js`, then Ctrl+Shift+R. If they render but look weirdly cropped at the corners, the corner flood-fill BG removal stripped slightly into the rounded purple frame — the inline run made only ~0.95% (boy) / 0.67% (girl) of pixels transparent so this should be safe, but worth eyeballing. Also verify the same gender_sprout_*.png assets are used in `lib/custom_avatar_screen.dart:927` (custom avatar wizard) by walking that path with a Sprout — should display the same updated portraits there.
- **MT-074** [open] Browser-verify featured photo-avatar card + BYOK→custom-avatar continuation (created by 3240) — commit `849c9e1b` ships two coupled changes in `lib/screens/wizard_steps/hero_creator_step.dart`, `lib/widgets/avatar_gallery_selector.dart`, `lib/widgets/hero_creator/avatar_choice_cards.dart`. **(A) Featured-card visual:** walk a Sprout (3-5) account with parental photo consent ON and BYOK already configured (so `_allowPhotoAvatar && _isPremium` is true) → name/gender → page 2 should now stack vertically: featured purple/pink/orange gradient card on top with gold "✨ NEW! ✨" badge top-right, 📸→✨→🦸 emoji-and-arrow row, headline "Turn YOU into a cartoon hero!", "Tap to start" white pill; below it an "or" divider; below that a compact horizontal "Pick a magical hero" row card. Tap featured card → CustomAvatarScreen opens. Walk the same flow as a non-premium account (no BYOK) → page 2 should be skipped entirely (only "Pick a magical hero" available, single-button page is bypassed via `_shouldShowBuildHeroPage`). **(B) BYOK loop fix:** start as non-premium, open the gallery (single avatar option from page 1), tap the gold "Create a custom avatar that looks like me!" banner → upsell dialog appears → tap "Set Up Free Premium →" → BYOK wizard pushes → enter valid Gemini key → wizard pops → gallery should auto-route the user *into the custom-avatar wizard* (not back to the upsell, not back to the empty gallery). Then dismiss the custom-avatar wizard back to the gallery and tap the banner a second time → should open custom-avatar wizard *immediately* (no upsell dialog). NB: this only fixes the gallery surface — `_isPremium` on the parent `hero_creator_step` still won't refresh mid-session for users who set up BYOK via a non-gallery, non-tweak-panel surface (broader MT-069 scope). Symptom that prompted the fix: user said "when I put my free premium key in and then go to create a custom avatar that looks like me it takes me back to the set up free premium screen." Note: MT-069 (a) describes a related-but-distinct symptom inside the BYOK wizard itself; if that bug repros after this lands, MT-069 still needs work in `byok_setup_wizard.dart`.
- **MT-073** [open] Browser-verify BYOK CTA path from Sprout avatar tweak panel (created by d84a) — commit `3bf5bbbe` rewrote the non-premium state of `lib/widgets/avatar_tweak_panel.dart` and added `_refreshPremiumStatus()` to `hero_creator_step.dart`. Walk a fresh Sprout signup (no BYOK, no paid premium): pick a midjourney gallery avatar → tweak panel opens → expand "Change hair, eyes & more" → confirm subtitle reads "Free — uses your own Google AI key", CTA reads "🔑 Set Up Free Premium →" (active, gold gradient), the old "Premium only" disabled state is gone. Tap CTA → BYOK setup wizard pushes full-screen → enter valid Gemini key → wizard pops → confirm without closing the dialog: pickers go bright, Generate button activates with no re-tap, header chip swapped from lock to key icon. Pick hair "Long" + eyes "Blue", tap Generate → comparison view → tap "Use this one!" → wizard returns to hero_creator → confirm downstream gates also unlocked: build-hero page now appears (`_shouldShowBuildHeroPage` re-checks via `_refreshPremiumStatus`), `illustrationsEnabled` true on subsequent story-result. Negative case: dismiss BYOK wizard without entering a key → tweak panel should remain in locked state (no false unlock). Backend at `backend/routes/avatar_routes.py:690` already accepts `tier in {premium,family,byok}` so the actual /tweak-gallery-avatar call should succeed once the wizard saves the key. Note: this only fixes the hero_creator surface — `magic_review_step` and other premium-gated screens still read `is_premium_byok` once at mount and won't refresh after BYOK setup mid-session (broader scope of MT-069).
- **MT-072** [open] Free-tier illustration upgrade CTA on Sprout result screen — Smoke-test feedback (2026-05-09): a free-tier user (no BYOK) generated a Sprout story and the result screen produced no illustration AND offered no path to one — they didn't even know AI illustrations existed as an upsell. Today the result screen for free-tier non-BYOK Sprout simply omits the illustration block (BYOK users bypass paywall via `_maybeStartPerPagePrefetcher` gate, but non-BYOK gets nothing; cf. PROJECT_STATUS "BYOK users bypass illustration paywall"). Add a parent-readable upsell tile on `lib/story_result_screen.dart` for non-BYOK free-tier users showing a placeholder with copy like "🎨 Add AI illustrations — set up a free Gemini key in 60s" linking to the BYOK setup wizard, OR a Stripe upgrade CTA for paid tiers. Decision needed: BYOK-only path (zero infra cost, parent friction) vs. paid-tier inclusion (more revenue path). Pair with audit of where the result screen currently renders for free-tier — see `_buildBreathingHeroAvatar` flow + the page-illustration `PerPageIllustration` widget gating.
- **MT-071** [open] Page tap-to-turn + "Avatar that looks like me" CTA prominence (created by 46a8) — Two unrelated UI tweaks bundled because both are small, both came out of the 2026-05-08 3yo walkthrough, and both are in files that were parallel-locked at first triage but now unblocked. (a) Page tap-to-turn: `lib/screens/wizard_story_screen.dart` — story reader currently advances only via the right-edge arrow chevron; Darcy says it's "confusing that I can't just tap the page to turn it". Add a `GestureDetector` overlay on the page body that consumes a tap as a forward-flip (with the existing arrow tap as the same callback). Don't break drag-flip; ensure the per-page scroll view still receives scroll gestures (use `HitTestBehavior.translucent` and only fire on tap-up at a position not inside an interactive child). (b) Avatar CTA prominence: hero_creator_step.dart `_buildPage0()` (or wherever the Sprout welcome / hero-pick surface lives) — "create a custom avatar that looks like me" reportedly disappears into the layout. Reframe as a featured tile or larger primary CTA so a kid notices the photo-to-Pixar capability. Walk a fresh Sprout signup post-fix and confirm the avatar option reads as a primary path, not a buried side option.
- **MT-070** [open] Sprout illustration character likeness — populate appearance fields from photo path (created by 46a8) — User report (3yo Anthony walkthrough): "though they are all boys with brown hair it doesn't really look like him" — illustrations come back generic-Pixar, not matching the saved Sprout character. Backend at `backend/gemini_image_generator.py:206-217` correctly unpacks `hair`/`skin`/`outfit`/`gender` from `character_appearance` AND reads `avatar` via `_read_avatar_details()` at line 217. Frontend at `lib/story_illustration_service.dart:218-219` correctly forwards `character_appearance` to backend. The disconnect: Sprout characters have `hair=null,eyes=null,skin_tone=null,hairstyle=null,outfit=null` because the simplified Sprout 4-step avatar wizard only collects gender + favouriteColor + photo. Confirmed via MT-035 console capture: `storyLocal.charactersJson` for the saved Sprout shows all those fields null. Two paths: (1) populate the appearance fields by running a vision-model description pass on the saved photo at character-save time (extra Gemini cost per save, but persistent); (2) send the saved photo as `custom_avatar_base64` per `gemini_image_generator.py:195` (no extra cost; image-grounded prompt). Path (2) is cleaner — find where Sprout characters store their photo (`generatedAvatar.imageBase64` is the most likely landing — see today's `3d616101` for how it can be base64, asset path, or URL) and route into `character_appearance['custom_avatar_base64']` for image-gen requests. NB: gemini-2.0-flash for image-gen accepts inline image references as part of multimodal prompt — verify behavior on a 3yo walkthrough before declaring done.
- **MT-069** [open] BYOK wizard "kicked back to Set up Free Premium" + Rhyme Time 429 generic error copy (created by 46a8) — Two related BYOK UX bugs bundled. (a) After Darcy entered a Gemini key in the BYOK wizard ("Free Premium" label), the wizard returned to "Set up Free Premium" instead of advancing to a success/configured state. Backend `backend/routes/story_routes.py:1054, 1069-1073` correctly receives + uses `user_api_key` for `GeminiImageGenerator`, so the persistence is fine — this is purely a wizard state-transition bug. Audit `lib/screens/byok_setup_wizard.dart` (or wherever `_EnterKeyStep` lives) for the post-save pop / setState path. Likely the wizard pops correctly but the parent rebuilds from a Riverpod provider that hasn't picked up the change — verify `useOwnApiKey` provider invalidates after `applyWizardResult`. (b) When Sprout/Rhyme Time generation hit Gemini 429 (free-tier exhaustion on the user's BYOK key — `limit:0`), the screen showed only "Uh oh! Something went wiggly. Let's try again!" — generic and unhelpful. Now unblocked (Codex's `magic_review_step.dart` rewrite landed in `331d606e`). At `magic_review_step.dart:522` (or wherever the new error catch sits post-rewrite), detect `e.toString().contains('429')` || `'quota'` || `'rate'` and branch to a kid-friendly message ("Stories are taking a quick break — try again in a minute! ✨") plus a small parent-readable secondary line ("Stories temporarily limited by API quota."). Three remediation options for prompt-side were also discussed in this session's research agent reports — see session record 46a8.
- **MT-068** [open] Smoke-test today's 3 backend+frontend fixes after Flask restart (created by 46a8) — Three commits landed today await post-restart verification: `e3e33a99` (Sprout LTR validator threshold + Sprout NON-LTR word ceiling), `ac947443` (Make One Up tile contrast + sparkle catcher 80px Sprout targets), `3d616101` (avatar asset-path crash fix on story result screen). Smoke-test attempted today FAILED for the Sprout caps because Darcy's Flask backend wasn't restarted (PID 8440 was running pre-edit code — last "Restarting with stat" was 16:04, my edits were 19:35-21:00). Walk: (1) **Backend restart** — Ctrl+C in his Flask terminal (he located it during this session), re-run start command, watch for "Restarting with stat" + "Running on http://127.0.0.1:5000". If a separate Celery worker terminal exists, restart that too — `story_tasks.py` is a Celery task file and Flask debug auto-reload may not cover it. (2) **Sprout LTR fresh story** (NOT cached) — should produce ≥5 body pages, each ≤25 words. Backend log should show "LTR format check failed: N pages (need ≥5, target M), K pages exceed 25 words" warning + retry sequence if Gemini returns short. (3) **Sprout regular Story Quest fresh story** with Long length — should cap at 300 words total. Backend prompt log should contain "HARD LIMIT: do not exceed 300 words total. Sprout is picture-book pacing". (4) **Make One Up tile** on Sprout scene picker — title text legible against the bright illustration (was washed-out gold-on-bright). (5) **Sparkle catcher** during loading — Sprout targets visibly bigger (80px), spawn area covers more of the body. (6) **Returning Sprout user** with midjourney avatar (e.g., Anthony or Ned) — story result screen renders without the FormatException + 99311px overflow crash. If LTR still emits 2 pages even after restart, escalate to MT-058 depth fix (prompt strengthening or post-process page-split). Branch is 11 commits ahead of origin; **only push after this verify passes** (Railway redeploys backend → Sprout caps go live). See session record 46a8 for full triage.
- **MT-067** [open] Browser-verify Sprout sparkle-catcher firework redesign + idle star (created by 8c13) — commit `dc4ca4ce` rewrote the post-tap "burst" in `lib/widgets/magical_loading_view.dart` from a single fading gold disc into a real fireworks animation (10-14 sparkle-shaped particles radiating outward with `Curves.easeOutCubic` + light gravity, fading streaks, randomized 5-palette color picker covering sunburst/pink/aqua/royal/rainbow, 800ms lifetime). Same commit redesigned the sprout idle tap-target: dropped the gold-disc background entirely, layered 3 star icons (outer glow / solid gold / inner white highlight) under a soft pulsing radial halo driven by `_pulseController`, plus a slow rotation driven by `_rotationController` with per-target `spawnRotation` offset so they don't all rotate in sync. Reduced-motion users get a smaller 5-particle firework instead of the full version. Original user complaint (3yo walkthrough 2026-05-08): stars-to-tap and post-tap burst were both gold circles, so kids tapped a star, saw a "new" gold circle pop up, and tried to tap it — but it was just the dead burst overlay. To verify: walk a 3-year-old to "GO!" so the loading view appears; confirm (1) drifting stars now look unambiguously like stars (rotating, glowing, layered), NOT discs, (2) tapping a star produces a fireworks burst — radiating sparkles in varied colors (gold/pink/aqua/purple/rainbow), NOT a single gold disc, (3) free-form taps anywhere on the stage also produce fireworks, (4) the bursts fade fully and never look like new tappable targets, (5) sparkle counter still increments ("✨ X sparkles!") and 5-star constellation countdown still appears below. Then walk an Explorer + Adventurer through generate so the older-band path also gets fireworks; confirm older bands kept their existing idle-target visual (gold circle with star icon — that path was deliberately not changed since complaint was sprout-specific) but the burst is now fireworks there too. Verified locally: `flutter analyze` clean, `flutter test test/widgets/pick_a_path_adventure_screen_test.dart` 18/18 pass. If bursts feel too dense/sparse for a 3-year-old, the easy knob is `particleCount` in `_spawnFirework` (currently `10 + random(4)`).
- **MT-066** [open] Commit local-save fallback in `magic_review_step.dart` once codex-2026-05-08's MT-065 WIP is committed (created by 0441) — Session 0441 added a ~25-line local-save mirror to `_saveCharacterIfNeeded` in `lib/screens/wizard_steps/magic_review_step.dart:652` (writes character to local Isar/SharedPreferences after every backend POST/PATCH attempt, using `local_<ts>` id and `isSyncedToServer=backendOk` flag). The matching fix on the hero side (`_saveCharacterDraft` + new `_persistLocalCharacter` helper in `hero_creator_step.dart:718`) IS committed under `a628dc12` (mis-labelled, see session record 0441). My `magic_review_step.dart` edit is layered on top of codex-2026-05-08's much larger WIP for MT-065 (tappable tiles + "Pick something new"), so I left the file uncommitted. Action: when codex's MT-065 work is ready to commit, my fix rides along. Or use `git add -p` to stage only the new imports + the `bool backendOk` block + the post-catch Isar mirror block. Verify after: hot-restart with backend offline, walk wizard end-to-end, kill flutter, restart with backend offline → character appears in Welcome Back screen. Imports added: `package:story_weaver_app/services/isar_service.dart` and `package:story_weaver_app/models/local/character_local.dart`.
- **MT-064** [open] Clean up `imagine_it_NN_*.png` repo-root screenshots + extend gitignore (created by 8015) — Playwright verify of the Imagine It rework left 15 untracked PNGs at repo root (`imagine_it_01_landing.png` through `imagine_it_15_pressed.png`). Also `.claude/scheduled_tasks.lock` was left from a ScheduleWakeup. None are committable, but they clutter `git status` for every future session. Two-line action: (a) `rm imagine_it_*.png .claude/scheduled_tasks.lock` (the lock is safe to remove if no `claude` process holds it), (b) append `/imagine_it_*.png` (and probably a broader `*-screenshot.png` or session-prefixed pattern — Darcy's call) to `.gitignore` so future Playwright sessions don't trip the same way. The `.gitignore` already covers `/mt0NN-*.png` from MT-001, so the gap is just naming convention drift.
- **MT-065** [open] Browser-verify tappable review tiles + "Pick something new" button (created by codex-2026-05-08) — commit `<local MT-065 implementation commit>`. Walk a returning Sprout user → tap saved character on Welcome Back → review step appears → confirm: (1) tapping "I am Tester" jumps back to step 1 (hero), (2) tapping "Going to..." jumps to step 3 (scenes), (3) tapping "With..." jumps to step 2 (companions), (4) "Pick something new" TextButton below GO! jumps to step 2. Confirm prior settings are preserved during the jump (e.g., previously selected Pebble is still highlighted on companions step).
- **MT-063** [open] Fix 6 stale `hero_creator_step_test.dart` failures (created by autonomous-2026-05-06) — `flutter test` is currently 288/294 (PROJECT_STATUS claims 294/294, stale). Failures are leftover from commits `1e80d9c8` (skip-empty-build-hero) and `a9b4bd41` (welcome-back affordance) that reshuffled `_buildPage0` / page count without updating tests. Concrete mismatches to fix: (a) `find.byIcon(Icons.add)` at line 161 → should be `Icons.add_circle_outline` (or `Icons.person_add_rounded` per `hero_creator_step.dart:1370`), (b) `find.textContaining('Create')` capitalisation mismatch with new "Or create someone new" copy, (c) `Character.fromJson` test fixture throws `_Map<dynamic, dynamic>` is not subtype of `Map<String, dynamic>?` on the `generated_avatar.attributes` field — needs explicit `<String, dynamic>{}` typing, (d) `expect(find.byType(TextField), findsWidgets)` after tap on "create new" — page 1 may now have different field type post-refactor. The 4 failing tests are: `switches from existing hero selection to create new` (line 126), `increments and decrements age` (line 175), `selects Boy gender` (line 215), `selects Girl gender` (line ~220). Two more failures earlier in file. After fixing, update `docs/PROJECT_STATUS.md` line 95 from "Test suite: 294/294 green" to current count.
- **MT-062** [done] Integrate four drafted Sprout Life Quests (closed by playwright-2026-05-07) — Integrated `56865c6b`, verified via Playwright session 2026-05-07 against local `flutter run -d chrome` + local backend. ALL FOUR new quests visible in correct friend lists with exact hooks: `questGoodbyeHug` ("Your grown-up has to go to work...") under Rainy Bunny; `questBigNo` ("You ask for a cookie. Grown-up says no. Your face gets HOT.") under Roary Lion; `questFirstHi` ("You walk in. The room is full of people you don't know yet.") under Shy Mouse; `questWarmHeart` ("A package came in the mail. With YOUR name on it!") under Sunny Pup. Walked `questWarmHeart` end-to-end (segment 1 → wh_call → wh_specific ending) — `{name}` interpolation works ("You found the love I put inside it, Tester."), grownupTip callout renders. Spot-verified `questBigNo` segment 1 (NOOOO! ALL-CAPS rendered, Dragon's Breath coping break) and `questFirstHi` segment 1 (Star Breath coping break, "Try it with Tester!" buddy reference). Friend coverage symmetric 2/2/2/2.
- **MT-061** [done] Browser-verify Sprout cloud→animal-friends rebrand + animal-as-breathing-buddy (closed by darcy-2026-05-08) — Playwright confirmed (1) Big Feelings tile → LifeQuestScreen, (2) 2×2 animal grid + "Tap a friend!" header, (3) friend filtering, (4) buddy text references in coping breaks. Darcy confirmed (5) Pup portrait clipped to circle scaling 0.45→1.0 with breath in questWarmHeart's Hot Cocoa Breath; orb fall-through correct on Explorer. FeelingsCloudPicker rectangular-stamp risk N/A — Sprout flow doesn't route through cloud picker post-MT-053. Files: `lib/data/life_quest_data.dart`, `lib/screens/life_quest_screen.dart`, `lib/widgets/coping_practice_sheet.dart`, `assets/images/feelings/sprout/{happy,sad,mad,scared}.png`. *(Original verify steps:)* — commit `1c668c0a`. Walk a 3-year-old end-to-end: (1) tap **Big Feelings** scene tile → 2×2 grid renders 4 animals (Sunny Pup, Rainy Bunny, Roary Lion, Shy Mouse), TTS says "Tap a friend! Sunny Pup, Rainy Bunny, Roary Lion, Shy Mouse." — was previously 3 dark Pixar clouds with Sunny missing. (2) Tap each friend → opens that friend's filtered quest list. (3) Play **The Big Hello** (Sunny Pup) end-to-end — confirm all three endings reachable: bh_hug, bh_show, bh_breath (the bh_breath ending also fires a `star_breath` coping break which is a good co-test for the buddy practice sheet). (4) Trigger any `copingBreakId` in any Sprout quest (e.g. *The Big Bear Hug* → "Take a big breath" → `belly_breath`) — confirm `CopingPracticeSheet` intro shows the buddy portrait in a soft accent-glow circle with the buddy's pep-talk ("Rainy Bunny will breathe through the sad with you."), practice frame shows the buddy clipped to a circle scaling 0.45→1.0 with the breath instead of the orb. (5) Walk an **Explorer (6-8)** quest with a coping break — confirm the OLD orb still renders (not the buddy), since older bands pass `buddy: null`. This co-verifies MT-059's animation fix layered with the new buddy parameter. (6) Open the Coping Toolbox tile from Explorer's My Quests tab — orb still there. **Side-effect to watch:** the same 4 PNGs are also read by the in-wizard `FeelingsCloudPicker` Sprout drilldown (`_FaceImage` via `AgeBandAssetResolver.feelingPath`). Those cards expect transparent-bg face images but the new ones have rectangular tinted backgrounds — when clipped to the cloud-shape card, they may look like rectangular stamps. If it looks broken, fix is either `BoxFit.cover` on `_FaceImage` or generate transparent-background variants of the 4 animals. Files: `lib/data/life_quest_data.dart`, `lib/screens/life_quest_screen.dart`, `lib/widgets/coping_practice_sheet.dart`, `assets/images/feelings/sprout/{happy,sad,mad,scared}.png`.
- **MT-060** [done] Browser-verify wizard skip-empty-build-hero-page + Sprout companion auto-advance (closed by playwright-2026-05-07) — Both A and B verified via Playwright. **(A) Skip-empty-build-hero PASS:** Sprout (age 4) without parental photo consent on, after entering name + Boy gender on page 1, tapped Next — avatar gallery opened directly, no "How do you want to build your hero?" interstitial. Picked avatar → archetype page → picked Brave Hero → companions page (no empty page seen on Back path either). **(B) Sprout companion auto-advance PASS:** tapped Pebble companion → wizard auto-advanced from step 2 (My Buddies) to step 3 (My World) without tapping Next. Premium-with-photo-consent path NOT verified (no premium account in this session). Cross-band negative case (Explorer should NOT auto-advance) NOT verified — would need a second character. — commit `1e80d9c8` in `lib/screens/wizard_steps/hero_creator_step.dart`. Two related UX fixes: **(A)** Walk a 3-year-old WITHOUT parental photo consent (toggle off in parent gate, or just any non-premium account) through the wizard — after entering name + gender on page 1 and tapping Next, the avatar gallery should open immediately. The "How do you want to build your hero?" page (which would only show one button in this case) should never appear. After picking an avatar, you land on the archetype page. Tap Back on archetype → should land on name/gender, NOT on the empty single-button page. Then walk a user WITH parental consent + premium — confirm the build-hero choice page DOES appear (since it has 2 real options: gallery + photo). **(B)** Walk a 3-year-old to the Adventure Team / companions page, tap any magical companion in the grid (`CompanionImageGrid`) — confirm the wizard auto-advances to the scene page after ~1.4s without needing to tap Next. Quick re-tapping should not double-fire (timer is re-armed on each tap). Confirm "Adventure alone!" button still works (no companion selected → tap → advances). Walk an Explorer (6-8) to the same page → confirm it does NOT auto-advance (only Sprout, age ≤ 5, gets the hook). Confirm tapping a saved-character friend chip (FriendChipButton, separate from the magic-companion grid) does NOT trigger auto-advance — that path was intentionally left manual.
- **MT-059** [open] Browser-verify breathing-orb animation in CopingPracticeSheet (created by 536f) — commit `294d6e67` rewrote the practice-sheet animation from `AnimationController` + `AnimatedBuilder` (which sat static in repro) to a `TweenAnimationBuilder<double>` keyed on `'orb-c{cycle}-s{step}'`, animating `scale` 0.45→1.0 on breatheIn / 1.0→0.45 on breatheOut over each step's full duration with `Curves.easeInOut`. Glow blur derives from current scale so the aura swells with the orb. Removed `AnimationController` + `SingleTickerProviderStateMixin` entirely. **Note:** A parallel session (commit `1c668c0a`) extended the sheet with a `SproutFriend? buddy` parameter for animal-friend portraits — that change layered on top of the orb fix and is independent. To verify: open Coping Toolbox (My Quests tab as Explorer or Adventurer) → tap Belly Breath → "Let's go!" — confirm the blue orb visibly grows over 4s, holds at full size briefly, shrinks over 4s, repeats 4 cycles, then shows "You did it!" Try Dragon's Breath (orange orb, 4s in / 1s hold / 6s out × 3) and 5-4-3-2-1 (green orb stays at half scale, prompts cycle text every 6-12s). Also verify in-story break path: Sprout My Turn Your Turn → tap red truck quest → tap Dragon's Breath card mid-quest, confirm orb animates same way and "Done early" returns to the quest. Path also reachable via wizard scene picker → Big Feelings tile (per MT-053).
- **MT-058** [open] Backend smoke-test Sprout ≥5 pages × ≤25 words/page (created by 81f6, partial verify by playwright-2026-05-07) — commit `cd222b08` tightens Sprout prompts + adds LTR validation/retry. **Playwright verify against LOCAL backend (Sprout age 4, Tester, Rainbow World, Listen & Learn) STILL FAILS the page-count constraint:** result was "Page 1 of 2" — 1 body page + 1 "The End" celebration page. Body page contained 5 short phrase-lines (~27 words: "Tester did run, zip, zip, zip!" / "Pebble had a fun, fast trip!" / "A red sun is up, up, up!" / "A pup had a hot, hot cup!" / "A big, fat hog felt sad."). Word-per-page within ≤25 cap roughly OK but spec was ≥5 BODY pages. Backend log confirms LTR validator IS firing: `WARNING - Validation failed on attempt 1: Learning-to-read story did not meet rhyme quality checks` then retry succeeded after 61s. So validator catches **rhyme quality** but NOT **page count** — `cd222b08` only partially addresses MT-058. To re-attempt: tighten the LTR validator to also reject < 5 body pages, OR add a hard prompt instruction with explicit page-count examples. Screenshots in `docs/qa-screenshots/phase-7-page1-of-2.png` and `phase-7-page2-of-2.png`.
- **MT-057** [done] Browser-verify welcome-back tap-to-start affordance change (closed by darcy-2026-05-08) — confirmed: 16px white w600 prompt and demoted "+ Or create someone new" underlined TextButton.icon both render correctly. Original details: commit `a9b4bd41` changed `lib/screens/wizard_steps/hero_creator_step.dart` `_buildPage0()`: prompt is now "Tap your character to start a story!" (16px white w600 vs 14px white70), bottom button demoted from gold-shadowed `ElevatedButton` to a small underlined `TextButton.icon` reading "Or create someone new". Open the app with at least one saved character → confirm Welcome Back screen leads with the brighter prompt; tile-tap immediately auto-advances into the wizard with that character pre-loaded; no "Go" press required. Confirm "Or create someone new" still works (loads create-new flow). Path also reachable from story-result screen → "New Story with [name]" → wizard.
- **MT-056** [open] Browser-verify page-turn polish — larger sparkle burst + bouncing thumb-tab (created by 81f6) — commit `56448aeb` added `largeBurst` flag to `_FlipSparkles` (9 sparks vs 6, sizes 20-32 vs 14-26, distances 100-180 vs 70-145, 800ms vs 650ms duration) for `_isYoungUser` (Sprout + Explorer); `_PageArrowOverlayState` adds a 4.5s `Timer.periodic` pulse via `TweenAnimationBuilder` + `Curves.elasticOut` (1.0 → 1.15 → 1.0) when `widget.alwaysVisible` is true. Walk a Sprout or Explorer story → flip pages, expect a noticeably bigger sparkle burst lasting longer than the original 6-spark fan; observe right-edge thumb tab giving a subtle bounce every ~4.5s when stationary. Walk Adventurer or older to confirm original 6-spark burst + non-bouncing fade behavior preserved.
- **MT-055** [open] Browser-verify "The End" page redesign for Sprout (created by 81f6) — commit `56448aeb` redesigned `_buildEndOfStoryPage()` for Sprout: celebration banner at top (cover image when available, else gradient with 5 `Icons.auto_awesome` sparkles), "The End" sized 40, tighter mood emoji row (28pt vs 36pt, "How was the story?" caption 13pt), CTAs collapsed to horizontal `Wrap` of 96-wide icon-over-label tiles via new `_buildSproutCtaTile`. Walk a Sprout story to completion → confirm celebration banner, "The End" prominent, mood emojis tight, three CTA tiles in a row (not stacked). Walk an Explorer or older story to confirm older bands keep stacked layout.
- **MT-054** [open] Browser-verify per-page illustrations now enabled for Sprout on server-key path (created by 81f6) — commit `56448aeb` adds `allowServerKey` flag to `PerPageIllustrationPrefetcher` + extends gate in `_maybeStartPerPagePrefetcher()` to enable on Sprout regardless of BYOK. Walk a Sprout-age story without BYOK enabled — each body page should show a skeleton loader, then resolve to a generated illustration. Confirm in Chrome DevTools → Network: `/generate-illustrations` POSTs are firing without `user_api_key` in the request body. Confirm `<docs>/illustrations/<storyId>/page_*.png` files appear after generation. Open caveat: each Sprout story now consumes ~5-10 server-side Imagen calls; budget impact untested.
- **MT-053** [open] Browser-verify Big Feelings scene tile → LifeQuestScreen routing (created by d046) — commits `b0d37a06` (restore tile in scene picker for all bands) + `36eedf26` (route Sprout/Explorer/Adventurer taps to `LifeQuestScreen` instead of the modal cloud picker; older bands keep the modal flow). Wizard data (name, age, companion, pronouns) is propagated through so quest interpolation works. Touched: `lib/main_story.dart`, `lib/screens/wizard_steps/hero_creator_scene_page.dart`, `lib/screens/wizard_steps/hero_creator_step.dart`. To verify: `flutter run -d chrome`, walk a Sprout (3-5), Explorer (6-8), and Adventurer (9-11) character into the wizard scene picker, tap the **Big Feelings** tile — confirm it opens the rich `LifeQuestScreen` (Coping Toolbox at top + cloud picker / quest list, depending on band), NOT the thin "pick a cloud" modal that immediately advances to story-type. Confirm the back button returns to the scene picker without losing prior wizard answers. Then walk a Creator (12-14) / Adolescent (15-17) / Adult (18+) character — these should still get the original modal flow (feeling → wizard story-gen path), since their toolbox-vocabulary reframe is pending. Also confirm the scene-picker grid no longer shows the awkward 3:1 lone-tile render in odd-count branches that motivated the original removal (the `volcano_dragons` row should now have a sibling tile, not an asset-padded gap).
- **MT-052** [open] Browser-verify in-story coping breaks + Coping Toolbox across Sprout/Explorer/Adventurer (created by d046) — five commits land an authored coping system: `9d2c5a44` (Feeling Toolbox + first Explorer break), `4dcd4cda` (6 Explorer quests wired), `4fc4803f` (Star Breath into questSleepover gf_try), `85488d3f` (final 3 Explorer quests — full 11/11 coverage), `643beab1` (Sprout 3/3 + Adventurer 5/5 + toolbox UI for Adventurer). Files: `lib/data/life_quest_data.dart`, `lib/screens/life_quest_screen.dart`, plus the original Toolbox widget (file path implicit). Final break distribution: Belly 5, Hot Cocoa 2, Dragon 1, Volcano 1, Star 1, 5-4-3-2-1 1 across Explorer; quest-specific picks for Sprout (Belly Breath x2 + Dragon's Breath for the My Turn quest) and Adventurer (Star/Belly/Volcano/Belly/5-4-3-2-1). To verify: walk into **My Quests / Big Feelings** tab as Sprout (`Belly Breath` should fire mid-quest on Big Bear Hug `bbh_look`, Big Loud `bl_start`, and My Turn Your Turn `mt_start` for Dragon's Breath), Explorer (any of 11 quests — coping break card should appear between prose and choices), and Adventurer (Tryout `tt_start` Star Breath, Left Out `lo_start` Belly, Sibling Conflict `sc_start` Volcano, Being Teased `bt_start` Belly, School Stress `ss_start` 5-4-3-2-1). Confirm: (a) break card renders between prose and choices on the right segment, (b) story stays paused while child does the technique, (c) Continue advances to choice grid intact, (d) Coping Toolbox strip renders in My Quests tab for Explorer + Adventurer (NOT Sprout — intentionally hidden so cloud-grid stays the regulation entry). **Creator / Adolescent / Adult deferred** — current technique vocabulary (Dragon, Hot Cocoa) skews young; reframed set (e.g. Box Breathing, Tactical Breathing) authoring is pending and is the natural next coping-track session.
- **MT-051** [open] Story Type unlock/showcase design — Explorer Page 6 (created by six-hats-2026-05-05) — Six-Hats walkthrough for ages 6-8 flagged that the story-type picker (Classic / Bedtime / Rhyme Time / Read-Along / Pick-a-Path) hits decision-fatigue after 5 prior wizard pages, but Darcy explicitly wants kids to see CYOA (Pick-a-Path) and Rhyme Time — they're high-value modes that get skipped today. Goal: design a progressive-unlock or showcase pattern so a new 7yo doesn't see all five at once but discovers them over their first few stories. Possible shapes: (a) reveal one new mode per completed story with a "✨ NEW unlocked!" celebration; (b) showcase one mode per session as a "today's adventure" featured tile while the others are smaller secondary chips; (c) split into "Story" and "Special Story" sections where Pick-a-Path / Rhyme Time live in the second under their own delightful intro the first time. Decision points: how does a parent override (always show all)? How do unlocks persist across characters? Does Sprout get the same treatment or stay on simpler 2-button (Classic/Bedtime)? Bring back a concrete proposal with mockups after Darcy has clicked through the recent Six-Hats fixes (commits `d87e2b62` + `060d45da`) in browser. Defer code work until alignment.
- **MT-050** [open] Browser-verify BYOK per-page illustration prefetcher (created by 78ab) — commit `bf82f34d` adds `lib/services/per_page_illustration_prefetcher.dart` + `lib/widgets/per_page_illustration.dart` and wires them into `lib/story_result_screen.dart` (`_maybeStartPerPagePrefetcher` runs from a post-frame callback gated on `settings.useOwnApiKey || widget.usedUserApiKey`). The prefetcher serially POSTs to `/generate-illustrations` with `user_api_key`, persists bytes to `<docs>/illustrations/<storyId>/page_<n>.png`, and exposes per-page state via `ValueListenable<PageIllustrationState>` consumed by `PerPageIllustration` (renders image when ready, lavender shimmer skeleton with `auto_awesome` glyph while queued/loading, nothing otherwise). To verify: configure BYOK with a real Gemini key (Settings → toggle "Use my own key" → run BYOK wizard), generate a multi-page Sprout or Explorer story, watch the story-result screen — page 1 should show its illustration ready or skeleton-then-image within ~10s, and as you flip forward each subsequent page should already be ready (or close to ready). Confirm: (a) only one HTTP request in flight at a time (Network tab), (b) `<docs>/illustrations/<storyId>/page_*.png` files appear after first read, (c) re-opening the same story shows all illustrations instantly with zero new POSTs. Open caveats noted in commit message: `_buildCharacterAppearance()` reads `_character` which loads async, so the first prefetched page may go out without character appearance fidelity; failed pages don't auto-retry on `prioritize` (the queue check in `prioritize` skips non-queued/loading states — should explicitly re-enqueue failed); no UI toggle yet to disable per-page generation, so BYOK users always pay quota for every page. None block first-pass verification but file follow-ups if any matter for shipping.
- **MT-049** [open] Triage 6 stale `worktree-agent-*` branches (created by 4af1) — all 6 have unmerged commits AND mounted worktrees per session 4af1's read-only audit. Each carries 1-2 commits ahead of main: `worktree-agent-a002fb9f...` (1 commit, Big Feelings emotion-card redesign, 2 days ago), `a093aa9d...` (1 commit, test_archetypes.mjs cleanup, 4 days ago), `a145606c...` (2 commits, Sprout band detection fix, 2 days ago), `a3274258...` (1 commit, archive pre-2026-04-22 SESSION CLOSE blocks, 4 days ago), `a6409df1...` (1 commit, Recent Sessions table insert order fix, 4 days ago), `ad9f71829...` (1 commit, Sprout band detection — likely duplicate of `a145606c`, 2 days ago). Per branch decide: merge to main, archive, or drop. The two Sprout-band-detection branches probably collapse into one. None safe to delete without review.
- **MT-048** [open] Remove `[MT-035]` debugPrint instrumentation in `_saveStory` (created by 4af1) — commit `469f0b18` added 4 `debugPrint` lines at `lib/story_result_screen.dart:1320-1323` to diagnose whether the Chronicles save bug was real or stale-cache. Investigation revealed it was a real bug: auto-save gate used raw `<= 5` against a `WizardData.characterAge` default of 8, so the gate never fired on Sprout entry. Fix is in the parallel session's working tree (`_savedAge` field in `_AppEntryPointState`, propagation into `WizardData`, `ageBandFromAge` gate, `prefs.getInt('user_age')` fallback). Once that fix is committed + pushed + deployed and MT-034/MT-035 verifications confirm save works on production, remove these 4 `[MT-035]` debugPrint lines. Until then they're harmless but add console noise on every Sprout save.
- **MT-047** [open] Mirror "explain unfamiliar concepts" rule into Learning-to-Read prompt path (created by 4af1) — commit `6bccd443` strengthened rule #5 in `young_delight_rules` at `backend/services/story_service.py:556` ("HARD CHECK — re-read every page", expanded examples list, "FAIL — rewrite the page" framing). But that block is only consumed by the standard story-gen path. The Learning-to-Read prompt at `_build_learning_to_read_prompt` (around `backend/services/story_service.py:808-810`) has its own `vocab_instruction` for `age <= 5` covering decodability ("CVC words, no blends, no silent letters") but contains NO rule about explaining unfamiliar fantasy nouns inline. If a Sprout story is generated via LTR mode (Read-Along toggle on), the strengthened rule does not reach it. Mirror the strengthened rule #5 (or a decodability-friendly version of it) into the LTR `vocab_instruction`.
- **MT-046** [open] Tighten Sprout vocab rule age gate from `<= 7` to `<= 5` or split per sub-band (created by 4af1) — `young_delight_rules` block in `backend/services/story_service.py:549` is gated `if age <= 7`, but the toddler-strict rule #5 strengthened in `6bccd443` is written for ages 3-5. A 7-year-old (Spark band) doesn't need "cape" or "cousin" explained inline — over-explaining will feel patronizing. Either: (a) split the rule set so Spark (6-7) gets a less-strict version, OR (b) tighten the gate to `if age <= 5`. Option (a) is the better long-term answer — preserves SOME wow-word policy for Spark. While there, also confirm both `'3-4'` and `'5-7'` `AGE_CONSTRAINTS['notes']` blocks at lines 30-44 / 50-57 reflect the right age bands.
- **MT-045** [open] Triage Sprout UX audit findings → next-session priorities (created by 4af1) — `docs/SPROUT_UX_AUDIT_2026-05-04.md` (committed `b543ae45`) lists 28 concrete UI findings across tap targets, text sizes, copy, scrolling, affordance clarity, error states. 5 high-impact, 14 medium, 8 low, 1 positive (S-023). Top 3: **S-019** Chronicles "New" dialog requires typing → blocks Sprout entirely (`lib/screens/chronicles_list_screen.dart:265-337`); **S-003** Home AppBar exposes "Story Creator", subscription badge, 7-item popup menu (Achievements/Settings/Chronicles…) to pre-readers (`lib/main_story.dart:518-722`); **S-004** character portrait cards hard-coded `width:130`, name 14sp, age line 11sp — ignore Sprout 88px touch-target intent (`lib/main_story.dart:1380-1467`). Estimated 6-8 hours for top-7 priority items, 14-18 hours for all 28. Read the audit, pick a target set (probably top-3 high-impact + 2 quick wins) for the next dedicated Sprout polish session, file each as its own MT, OR keep this single MT as the umbrella and work the doc in-place. Note: `1841cd3a` already covered the story-result-screen subset of the audit (responsive padding, button sizing, pill-hiding); this MT is for the rest.
- **MT-044** [open] Browser-verify A3 fix — page-turn lands at top of new page (created by b535) — commit `1841cd3a` (which absorbed this session's edit) added `key: ValueKey('story-page-scroll-$index')` on the per-page `SingleChildScrollView` in `_buildStoryPage` and `key: const ValueKey('story-end-of-story-scroll')` on `_buildEndOfStoryPage`'s scroll view in `lib/story_result_screen.dart` (lines 1907 and 2129). Without keys, Flutter's element reconciliation was reusing the same `Scrollable` element across page turns inside `PageFlipBuilder.frontBuilder` / `backBuilder`, so a scroll offset from page N carried onto page N+1 — repro symptom: turning the page from a scrolled-down position landed mid-paragraph on the next page (user screenshot showed "flew back to Jace's shoulder…"). To verify: `flutter run -d chrome`, generate any multi-page story whose pages are tall enough to require scrolling on the viewport (any band, any mode), scroll page 1 down to its bottom, tap or drag-flip to page 2 — confirm page 2 opens at the TOP (cursor visible at the start of the typewriter, or first line of the paragraph fully visible), not somewhere mid-paragraph. Repeat going forward and backward across at least three pages. Also walk into the end-of-story page from a scrolled-down regular page to confirm "The End" lands at the top of its scroll view too.
- **MT-043** [open] Browser-verify Sprout story-result redesign at small + large phone widths (created by 861c) — commit `1841cd3a` reworked `lib/widgets/storybook_page.dart` (responsive padding/ornament size via `LayoutBuilder` — 18/26/40 padding by viewport, ornaments hidden below 340dp) and `lib/story_result_screen.dart` (narrow-area outer padding, title scales 28→20pt with maxLines+ellipsis, Reading-Level pill hidden for Sprout & on short viewports, upsell banner + quick rating row deferred to end-of-story page only and Sprout never sees the upsell, `_PageArrowOverlay` accepts buttonSize/iconSize and gets 64dp button + 44dp chevron + visible white border for young users, primary CTA scales 22pt/22dp pad for Sprout, `_ActionChip` enforces 64dp min hit area + 44dp icon + 15pt label for Sprout). To verify: `flutter run -d chrome`, open DevTools device toolbar at iPhone SE (375×667) AND a tighter 360×640, walk a Sprout-age character (≤5) through to a multi-page story, confirm (1) story body text is fully visible on the page on both widths (was empty on small in the screenshots), (2) no "Reading Level: Picture-Book" pill appears, (3) chevrons are chunky 64dp white-bordered circles, (4) "New Story with X" CTA is large + tappable, (5) "Read to me" / "Saved" chips have 44dp icons & comfortable hit area, (6) upsell banner + quick rating row are absent on pages 1..N-1 and only appear on the end-of-story "The End" page (and never on Sprout). Then walk an Explorer (6-8) and an Adventurer (9-11) story to confirm older bands kept their existing layout (pill present, smaller chevrons, smaller CTA). User reported the original screen "wasn't very well designed even for the larger screens" — primarily verify it now feels delightful for kids, not just functional. Reference screenshots: `Screenshot 2026-05-04 233607.png` (large phone — text visible but cluttered), `Screenshot 2026-05-04 233804.png` (small phone — text disappeared).
- **MT-042** [open] Browser-verify "Pick a Path!" badge no longer appears on linear story page 3 (created by ebc0) — commit `dd67873f` removed the genre-specific default labels from `lib/widgets/storybook_progress_indicator.dart` (`'Wake Up!'`, `'Play Time!'`, `'Pick a Path!'`, `'Follow the Glow!'`, `'Find the Star!'`, `'Big Choice!'`) so the fallback when `stageLabel` is null is now neutral `Page N`. To verify: `flutter run -d chrome`, generate a 6+ page non-interactive story (any band, any non-Pick-a-Path mode), navigate to page 3 — the progress badge should read `Page 3` (or the LLM-supplied stage_label for interactive stories), NOT `Pick a Path!`. Also walk a Pick-a-Path adventure to confirm the indicator still shows the backend-supplied `stage_label` correctly on each segment (the backend prompt at `interactive_adventure_prompt_builder.py:483,648` instructs the LLM to provide it; only the unstyled fallback changed). The user-reported bug also mentioned a 🐾 paw print appearing in the badge area — nothing in the indicator widget injects 🐾 (compact-mode icon is `Icons.auto_stories`), so check whether an adjacent UI element (likely the companion buddy emoji) is bleeding visually into the badge area on the screenshot, or whether this is just the user's approximation of the book glyph.
- **MT-041** [open] Browser-verify Sprout loading-screen mini-game extension (created by e61a) — commit `b8ee77e5` extends the existing tap-target mini-game to the sprout band in `lib/widgets/magical_loading_view.dart`. Removed the `!widget.isSproutBand` gate around the spawner (sprout now spawns at 2.8s cadence with 4–5s lifetime; non-sprout still 2.0s/2.4s) and refactored `_buildSproutLoadingContent` to render targets and tap-burst overlays on a 220–280px Stack stage centered on the bouncing companion. 60px golden star targets, haptic on tap, and a "Tap the stars! 🌟" hint. To verify: walk a 3-year-old profile through to "GO!" so the loading view shows during generation; confirm 1–3 golden stars drift in/out around the companion, tapping pops them with bursts and increments the sparkle counter ("✨ X sparkles!"), and free-form taps anywhere on the stage also produce bursts. The 5-star constellation countdown should still appear below.
- **MT-040** [open] Browser-verify phrase-per-line layout for Read Along stories (created by e61a) — `_phrasifyForEarlyReader()` helper landed in `lib/story_result_screen.dart` (bundled into commit `a7f641b2` despite the chronicles-focused message), gated on `widget.isLearningToReadMode`. Splits each page into one sentence per line (long sentences also split on commas, tiny fragments merged forward). Applied at three render sites: `MagicalTypewriterText` (unrevealed), revealed `SelectableText.rich`, and the alternate reader-view list. Line-height bumps 1.8 → 2.1 in this mode. To verify: `flutter run -d chrome`, generate a story with the "Read Along" toggle on for a 3–5 yo, confirm the body renders ~8 short lines instead of one paragraph for the "Justin's Big Hop Quest!"-style example. Confirm TTS still reads naturally (it intentionally gets the unmodified text, not the line-broken version).
- **MT-039** [open] Browser-verify "Generate my look" avatar tweak works end-to-end (created by 65b8) — commit `fbf6edab` fixed URL in `api_service_manager.dart:2237` (`/avatars/` → `/avatar/`). To verify: `flutter run -d chrome`, open wizard step 1, pick any gallery avatar, customise hair length + eye colour, tap "Generate my look" — should show the compare view (original vs tweaked) instead of the "magic wand slipped" error toast. If still failing, check browser Network tab for the POST to `/avatar/tweak-gallery-avatar` and inspect the response.
- **MT-038** [open] Browser-verify sparkle burst + haptic on story page flip (created by 944e) — commit `295d0426` added `_FlipSparkles` overlay and `HapticFeedback` calls in `story_result_screen.dart`. Run `flutter run -d chrome`, reach a multi-page story, flip pages left and right — confirm 6 golden stars fan out from the trailing edge on each completed flip. Also confirm no regressions on coloring-page export or TTS playback.
- **MT-037** [open] Browser-verify welcome-back page shows synchronously for returning users (created by 944e) — commit `e3b3fc6b` moved character preloading before `setState` in `_AppEntryPointState`. To verify: `flutter run -d chrome` as a returning user with ≥1 saved character — confirm the wizard opens directly to the "Welcome back" character grid without a blank flash or momentary "No characters yet" state. If blank flash still occurs, check that `_preloadSavedCharacters()` is awaited before `setState` at `main_story.dart:~130`.
- **MT-036** [open] Browser-verify wizard progress-indicator back-navigation from review step (created by 7e08) — commit `a8fadd13` added an `else if` branch at `wizard_story_screen.dart:501`: when on MagicReviewStep (_currentStep == 1) and user taps sub-steps 0–2 in the MoonPhaseProgress bar, wizard should animate back to HeroCreatorStep at that sub-step. To verify: `flutter run -d chrome`, walk through hero → team → place steps, reach the review page, then tap the second progress dot ("Pick Team") — confirm the wizard animates back to the team-picker page. Also confirm tapping the first dot ("Pick Hero") goes to the hero picker, and that tapping dot 3 (the review dot itself, index 3) while on the review page remains a no-op.
- **MT-033** [done] Deploy backend to Railway + verify Sprout story output has no second-person address (created by d917) — Backend redeployed 2026-05-03 (deployment `58c2fffd-18c4-4d20-a818-f1885fe706e4`, commit `3c7f2eb3`). Quality-audit Sprout story "Lily and the Quiet Sparkle Shell" (423 words) generated end-to-end against deployed code; preview is consistent third-person ("Lily played", "Her friend Bubbles", "Lily giggled") with zero `you`/`your`/`you're` hits. Caveat: only the 500-char `story_preview` was inspected (script doesn't store full text); a hidden second-person hit in the unseen tail is theoretically possible but unlikely given the disciplined opening voice. (closed by QA-2026-05-03)
- **MT-032** [open] Browser-verify Sprout scene tile taps auto-advance the wizard (created by 5653) — commit `6b0b38b8` adds `_heroNextPage()` after `selectedScenario` set for age ≤ 5. Run `flutter run -d chrome`, walk as a 3-year-old, tap "Stomp with Dinosaurs", "Rainbow World", "Under the Sea" — each should advance to the next wizard page with star burst + chime. Also do Ctrl+Shift+R to flush browser cache and confirm the ocean tile shows the octopus/coral image (not crystal cave). MT-023 tracks the Railway production deploy needed for the same ocean image fix.
- **MT-031** [done] Re-run quality audit after Railway backend deploy to verify adult band fix (created by 9471) — PASS: 49/49 checks across all 6 bands. Adult band 2,205 words in 48.3s (well under 5,200 ceiling; slightly over the aspirational <40s target — accepted). Zero "Extra data" fallbacks; the cd30ec91 raw_decode fallback fired twice during the run and recovered cleanly with valid stories. Explorer (649 words) and Adolescent (2,142 words) came in just below their soft floor — known trend, not a regression. Report: `backend/tests/quality/results/story-quality-2026-05-03-2211.json`. (closed by QA-2026-05-03)
- **MT-030** [open] Runtime-verify offline story-scaffold fallback (created by c75e) — commit `05df7577` added `lib/services/story_scaffold_fallback.dart` that wraps both AI story-generation paths in `api_service_manager.dart`. Architecture is unit-traced but never exercised end-to-end. To verify: open Chrome DevTools → Network → set "Offline", generate a Sprout-age story on either `volcano_dragons` ("Stomp with the Dinosaurs!") or `neon_jungle` ("The Magical Forest"), confirm a scaffold story returns (silent fallback — no error visible) personalized with the kid's name/companion/pronouns. Check console for `[story-fallback] AI generation failed (...); using scaffold "..."`. If it fails, the wrapper isn't catching the right exception type — check `isFallbackEligible` in `story_scaffold_fallback.dart:41`.
- **MT-029** [done] Browser-verify Sprout big-feelings flow after auto-advance + square-card fixes (created by c75e) — Playwright PASS on production (commit `3c7f2eb3`): cards render as roughly square illustration tiles (Rain Cloud, Storm Cloud, etc.) with thin purple drop-shadow borders, no aura glows; tapping Rain Cloud auto-advanced to a story-list screen ("The Big Bear Hug") — auto-advance confirmed. Screenshots: `mt029-big-feelings-cards.png`, `mt029-after-tap.png`. Phase 0B code audit also confirmed AspectRatio 1.0 and breathe animation present at `feelings_cloud_picker.dart:539–580`. (closed by QA-2026-05-03)
- **MT-028** [done] Browser-verify new Sprout boy Animal Friend archetype card (created by 487e) — Playwright PASS on production (commit `3c7f2eb3`): "Animal Friend!" tile shows Black boy in sunlit meadow with deer, raccoon, rabbit, squirrel, and orange + blue butterflies — matches spec exactly. Screenshot: `mt028-animal-friend.png`. (closed by QA-2026-05-03)
- **MT-026** [done] Browser-verify Big Feelings core grid Scrollbar fix (created by 8ca2) — Playwright PASS on production (commit `3c7f2eb3`): zero `Scrollbar's ScrollController has no ScrollPosition attached` errors observed in 82 captured console errors during the full Sprout walk-through (the 82 are unrelated — TTS 503s + one Stripe 401). Console log: `.playwright-mcp/console-2026-05-04T04-02-48-564Z.log`. Phase 0B code audit also confirmed shared controller wiring at `feelings_cloud_picker.dart:300–317`. (closed by QA-2026-05-03)
- **MT-025** [done] Verify pet companion AI transformation works after Gemini key fix (created by dfad) — local pet companion transformation confirmed working. Railway `GEMINI_API_KEY` was the expired key; rotated to new key 2026-05-02. (closed by 2026-05-02)
- **MT-024** [done] Browser-verify 5 visual audit fixes from session 599b (created by 599b) — ALL 5 PASS: (1) ✓ Adult band portraits show no checkerboard — `gender_adult_{boy,girl}.png` are fully opaque space-silhouette PNGs (no transparency); code hardened via MT-027. (2) ✓ Accordion step-nav expands correct section on mature-band creative brief. (3) ✓ Sprout GO! button fully visible on magic-review screen. (4) ✓ Core Archetype header + top of grid visible in initial viewport. (5) ✓ Explorer "Pick something special!" header + card tops visible (labels ~20px below fold). (closed by 2026-05-02)
- **MT-027** [done] Fix Adult-band Boy portrait checkerboard in Flutter web (created by MT-024 verify) — ROOT CAUSE: `gender_adult_boy.png` / `gender_adult_girl.png` were already replaced with fully opaque space-silhouette PNGs — no transparency, no checkerboard with current assets. Code hardened regardless: removed `ClipRRect`, replaced with `Container(decoration: BoxDecoration(color: 0xFF1E1828, borderRadius: 17, image: DecorationImage(AssetImage, BoxFit.contain)))` + `foregroundDecoration` for press highlight. Verified local release build after full SW cache clear. (closed by 2026-05-02)
- **MT-023** [done] Verify Under the Sea tile shows octopus/coral scene in Sprout world picker (created by 9b2b) — PASS: octopus/coral image confirmed in production after Railway grand-light redeploy 2026-05-03. (closed by session 2026-05-03)
- **MT-022** [done] Verify story generation works end-to-end after OpenRouter key rotation (created by 9b2b) — OpenRouter key `dc4c` was invalid (not in account); user updated Railway env and local `.env` to a valid key. Generate a Sprout story on production Railway and confirm it returns a full multi-page story, not the 2-page fallback. Also confirm `b5c2266c` fallback JSON is clean in the (unlikely) event it fires. PASS: "Lily and the Lost Rainbow Colors" returned as Page 1 of 4, full story. (closed by 7e8d)
- **MT-021** [open] Walk Sprout avatar creation after step removal (created by 9b2b) — hairColor and eyeColor steps removed in `63366ea3`; flow is now welcome → gender → favoriteColor → photo. Verify: (1) progress dots show 3 dots (gender/color/photo), (2) the 9-color 3×3 grid renders correctly on a phone-sized screen, (3) "Light Blue" and "Dark Blue" swatches are visually distinct, (4) ElevenLabs speaks color names (not robotic fallback) after the warm-up fix.
- **MT-020** [open] Verify BYOK validation + text-field readability after 263f deploy (created by 263f) — Railway backend redeployed as b45615d7. In Chrome: open BYOK wizard → Enter Key step, confirm API Key field shows dark text on the cream card (was white-on-cream before). Paste a real AIza… key, tap Finish, confirm validation succeeds (was crashing with 'NoneType' not subscriptable). Closes MT-014 and MT-007 if both pass. Commit `8015be6c`.
- **MT-019** [open] Verify Rhyme Time mode works for Sprout band end-to-end (created by e32c) — Rhyme Time was added to the Sprout story-type picker this session but has never been tested for a Sprout-age character. Walk through as a 3-year-old: pick a world, tap Rhyme Time, tap GO! — confirm the backend returns a rhyming story and ElevenLabs reads it correctly. If backend ignores rhyme mode for Sprout, file a backend task.
- **MT-018** [open] Device-verify Sprout "Make One Up" free-form panel (created by e32c) — the 4-tile world picker was replaced with a text+mic input this session. Test on a real device: tap Make One Up, confirm the gold panel appears with correct copy ("Where do you want to go? Tell us!"), tap the mic, speak a world idea, confirm speech-to-text fills the field, then continue. Also confirm the existing preset tiles still work above the panel. Commit `21e6c3e8`.
- **MT-016** [open] Browser-verify Sprout launch TTS fix on web (created by 0151) — after commit `20d55d56`, hot-reload Chrome and walk a Sprout-age user through to the GO! screen. Confirm: (1) the recap line speaks ONCE, (2) it speaks the on-screen recap ("I am Darcy. Going to ___. With ___."), and (3) it plays in the ElevenLabs voice — not the robotic device fallback. If still robotic with a single fire, the backend ElevenLabs call is failing (check `POST /tts/synthesize` for 503/auth errors — note the user reports their `flutter run -d chrome` is hitting localhost backend).
- **MT-001** [done] Clean ~70 untracked PNG screenshots from repo root (created by a488) — resolved by adding `.gitignore` patterns for all QA screenshot naming conventions (hat-, mt005-, mt007-, byok-, verify-, bug001-, after-, before-, qa-home-, etc.). Commit `ea3846f`. (closed by d719)
- **MT-003** [done] Smoke-test BUG-012 error responses (created by 76e3) — `/tts/synthesize` with invalid voice_id confirmed `{"error":"TTS_FAILED","message":"Narration is unavailable right now. Please try again in a moment."}`. All validation-layer responses are clean JSON. STORY_FAILED code confirmed correct in `story_routes.py:589,632`. (closed by 2026-04-25)
- **MT-004** [done] Flutter error string grep for BUG-012 follow-up (created by 76e3) — grep found one `debugPrint` (non-UI); real fix was `_parseError` in `interactive_story_service.dart:321` returning `data['error']` (now structured code) instead of `data['message']` (friendly copy). Updated priority to `message ?? error ?? hint`. (closed by 2026-04-25)
- **MT-005** [done] BUG-001 browser confirm (created by c29c) — `POST /generate-story → 200` confirmed, story rendered, no avatar gate banner. (closed by QA-2026-04-24)
- **MT-007** [done] Verify BYOK wizard on production web (created by d1c2) — root cause of key-not-persisting found and fixed: `StoryResultScreen` was a plain `StatefulWidget` so `isFreeTier` was frozen at construction; converted to `ConsumerStatefulWidget` + folded `settingsProvider.useOwnApiKey` into check (commit `68ac1b25`). Darcy confirmed key entry worked in production 2026-05-03. (closed by 2026-05-03)
- **MT-008** [done] Visual confirm archetype image grid for mature bands (created by 8a9d) — 2×2 image grid confirmed for Adult, Adolescent, Creator; gold border on tap confirmed for all 3 bands. (closed by QA-2026-04-24)
- **MT-009** [done] Verify BUG-003 fix in production (created by c4ea) — zero `subscription-status/anon_*` calls observed across all phases; anon guard working. (closed by QA-2026-04-24)
- **MT-010** [done] Investigate stale-JWT 403 for real user (created by c4ea) — root cause: `require_owner` string-compared Flutter's `user_<uuid>` URL param against server-issued bare UUID from JWT sub — always mismatched. Function body ignored URL param anyway. Fixed in `6b442f4`: removed `@require_owner` from `get_subscription_status`; also fixed `create_checkout_session` returning 403 on Stripe exception (→ 500). (closed by 6b442f4)

- **MT-011** [done] BUG-002 TTS backoff runtime verification (created by 2571) — backoff curve confirmed active (no storm, dedup working); retry cap >4 per phrase still hit. Retry cap fix (`_maxPrewarmRetries = 4` in `app_tts_service.dart:~147`) tracked as separate follow-up. (closed by QA-2026-04-24)
- **MT-012** [done] Age-gate consolidation audit (created by 7df8) — premise stale (`age_gate_screen.dart` deleted in `430563d`; one flow exists). Fix: informational "Just so you know" notice for 13–17 in `_handleContinue`, single "Got it" button — teens always proceed. Golden test for deleted screen removed. (closed by 2026-04-25)

- **MT-013** [done] BUG-002 retry cap code fix (created by QA-2026-04-24) — added `_maxPrewarmRetries = 4` constant + early break on final attempt (no wasted delay). `dart analyze` clean. (closed by 7dba)
- **MT-014** [done] MT-007 BYOK runtime verify (created by QA-2026-04-24) — superseded by MT-007 close; persistence bug fixed in `68ac1b25`. (closed by 2026-05-03)

## Closed tasks

<!-- Sessions move tasks here when status flips to `done` or `wontfix`. Most recent at top. -->
- **MT-035** [done] Browser-verify Chronicles list shows saved wizard stories for Sprout characters (created by d917) — commit `378d4aa0` added a "Your Stories" section at the top of `ChroniclesListScreen` for characters age ≤ 5. Walk as a Sprout character who has already generated at least one story → open the chronicles tab → confirm the saved story tile appears above the chronicles list. PASS: Darcy walked as 3yo Gabriel, generated "Gabriel and the Big Dino Fun" (Listen-and-Learn-easy-words mode), tapped through to Chronicles tab — story appears in "Your Stories" section. `[MT-035]` debugPrint output also confirmed `widget.characterName=Gabriel`, `characterAge=3`, `characters.length=1`, `characters.names=[Gabriel]` — save side has valid character data. Note: original concern was `charactersJson: null` from a previous repro; that no longer occurs in current code. (closed by 81f6)
- **MT-034** [done] Browser-verify Sprout auto-save on story-result landing (created by d917) — commit `378d4aa0` added an `addPostFrameCallback` in `StoryResultScreen.initState` that calls `_saveStory()` when `characterAge ≤ 5 && !_isSaved && storyId == null`. PASS: walked a Sprout story to completion (Gabriel and the Big Dino Fun), navigated to chronicles tab without tapping the heart manually — story appears in "Your Stories" without manual save. Verified jointly with MT-035. (closed by 81f6)
- **MT-017** [done] Audit other-band launch-screen TTS for the same double-fire pattern (created by 0151) — Audit complete: double-fire was Sprout-only. All other bands (Explorer/Adventurer/Creator/Adolescent/Adult) use tap-triggered `MagicEarButton` in their review builders — no `addPostFrameCallback` auto-fire pattern present. No fixes needed. (closed by this session)
- **MT-015** [done] Decide fate of `test_archetypes.mjs` at repo root (created by 8972, originally filed as duplicate MT-011, renumbered 2026-05-01 to resolve ID collision with the 2571 BUG-002 verification entry) — one-off Playwright script verifying archetype images across Adult/Adolescent/Creator bands. Resolved as delete: file already absent from disk (no test_archetypes.mjs anywhere in repo); session 8a9d record confirms the approach failed because Flutter renders to canvas (`document.images` returns empty for Flutter web assets), so it had no reusable value. (closed by mt011-decide)
- **MT-006** [done] Fix Recent Sessions table insertion order (created by a488) — the marker comment sits at the *bottom* of the table body and the awk inserts *above* it, so new rows accumulate newest-at-bottom despite the marker saying "most recent at top". Fix: move the marker to sit immediately after the `|---|---|...|` separator row, and flip the awk in `close-session.md` so it prints the matched line first then the new row (insert *after* the marker). Existing rows can stay where they are — Date/Time columns still make order readable. Purely cosmetic. (closed by mt006-order)
- **MT-002** [done] Archive or migrate the 11 legacy SESSION CLOSE blocks in TEAM_COORDINATION.md (created by a488) — they sit below the new Recent Sessions table and inflate the file (~4.5k lines). Either move to `docs/archive/TEAM_COORDINATION_pre-2026-04-22.md` or convert each into a `docs/sessions/<date>-<time>-<id>.md` retro-record. Low priority — fine to leave for now; revisit when the file becomes painful to scroll. (closed by mt002-archive)
- **MT-012** [done] Age-gate consolidation audit (created by 7df8) — original "two diverging paths" framing was stale: `lib/screens/age_gate_screen.dart` was already deleted in `430563d` (2026-04-21). Re-scoped to Option A from briefing: 13–17 attestation gate + golden test cleanup. Added a "Just so you know" parent-awareness `showDialog` before `recordConsent(method: 'self_attested')` for ages 13–17 in `welcome_screen.dart:1147-1191`; deleted the broken `Age gate screen` golden test that still imported the removed file; added widget test for the new dialog's three states. 18+ unchanged; under-13 path untouched. (closed by mt012-impl)
- **MT-005** [done] Verify gender picker images in Railway across all 6 bands (created by 247a) — all 6 bands confirmed: Sprout (chibi), Explorer (child), Adventurer (silhouette), Creator (teen), Adolescent (png), Adult (portrait). (closed by QA-2026-04-24)
