# SESSION CLOSE — 2026-05-10 09:39 [c8f0] — Branch: main — MT-077 Sentry sweep verified + closed

## Accomplished
- Verified MT-077 (Sentry bulk-resolve) via Sentry MCP. Confirmed `STORY-WEAVER-10` and `STORY-WEAVER-1K` are now `resolved` — auto-close fired on cursor commit `b0bfef9c`. Sibling issues from the digest also resolved: `1F`, `Z`, `1H`. `STORY-WEAVER-Y` not found in Sentry (likely deleted/merged). Dev-noise IDs from MT-077 (`1J`, `1G`, `P`, `2`, `6`, `5`, `1E`, `1C`, `1D`) no longer appear in unresolved list.
- Identified separate older bucket of 14 stale unresolved issues (first/last seen 15–26 days ago, predates the 2026-05-09 headless-Chrome incident that drove MT-077). Bucketed for handoff: canvaskit-Aborted family (`X` 2491 events, `11`, `1A`, `12` 125 events, `13`) — same root cause as cursor fix; probable real-bug candidates (`15`/`16` paired `toString` crashes in `bQ7.yJ(main.dart)`, `17` RenderErrorBox/RenderSemantics type error, `18` setState during build, `19` Duplicate GlobalKey, `14` RangeError 0..26:29); residual noise (`E` 2px overflow, `F` `<unknown>`, `1B` `getAttribute`). Darcy bulk-resolved all 14 in the same Sentry UI pass at his discretion. Sentry MCP still returned them in `is:unresolved` immediately after — caching lag, will catch up.
- Closed MT-077 in `docs/MANUAL_TASKS.md` with full verification trail (auto-close evidence + note that the older 14-issue bucket was cleared in the same sweep).
- No code commits this session. Only `docs/MANUAL_TASKS.md` touched.

## Still Pending / Deferred
- Working tree on close still has 11 modified backend + lib files plus 1 untracked file (`backend/services/cost_tracker.py`) from a parallel session working on the cap-then-BYOK / cost-validation track (cf. recent `docs(premium):` commits `bc9229a5`..`befe9cbd`). Not mine — left alone per parallel-session protocol.
- df18's next-actionable handoff still applies: **MT-074** (browser-verify featured photo-avatar card + BYOK loop fix from session 3240) is the most actionable open thread. Sentry side will stay quiet for a week or so now that dev events are silenced via `fd730664`.
- If any of the older bucket's real-bug candidates (`STORY-WEAVER-15/16/17/18/19/14`) resurface from real production users post-`fd730664`, treat as fresh signal — those classes (`toString` undefined, `RenderErrorBox` cast, `setState` during build, `Duplicate GlobalKey`, `RangeError`) all looked like genuine Flutter web bugs rather than dev artifacts.

## Blockers
- None.

## Files touched
- docs/MANUAL_TASKS.md

## Manual tasks
- Created: none
- Closed:  MT-077

## Next session: start here
> MT-074 (browser-verify the featured photo-avatar card + BYOK loop fix in `lib/screens/wizard_steps/hero_creator_step.dart` / `avatar_gallery_selector.dart` / `avatar_choice_cards.dart`, commit `849c9e1b`). Walk a Sprout w/ photo-consent + BYOK to confirm the gradient featured card; then non-premium → gallery → upsell → BYOK wizard → confirm auto-route into custom-avatar wizard. Sentry side remains quiet for ~a week.
