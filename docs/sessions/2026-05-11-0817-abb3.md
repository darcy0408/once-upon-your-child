# SESSION CLOSE — 2026-05-11 08:17 [abb3] — Branch: main — Image-gen A/B: production routing audit + verified OpenRouter $0.0375/img cost

## Accomplished
- `d1e36f8c docs(image-gen): routing audit + verified OpenRouter $0.0375/img cost` — augmented `docs/IMAGE_GEN_AB_TEST_RESULTS.md` with the production-routing audit that the parallel session (which built the harness in `0e48f6d4` and the visual scoring in `dc7ecf47`) missed:
  - Production primary is `OpenRouterImageGenerator` → Gemini, NOT direct Gemini ($0.039/img). Audit at `backend/app.py:466-491` + `backend/openrouter_image_generator.py:177`.
  - `ReplicateImageGenerator` is NOT wired as a per-page-illustration fallback (despite the brief's claim) — only used by `avatar_generation_service.py` and `interactive_adventure_service.py`.
  - `max_tokens: 1000` cap at `backend/openrouter_image_generator.py:177, 285, 366, 443` is below image-output token count (~1290) but production works — OpenRouter appears to ignore `max_tokens` for image modality. Worth removing or raising.
- **Verified OpenRouter actual per-image cost from <https://openrouter.ai/activity>: 4 Nano Banana requests = $0.15 = ~$0.0375/img.** OpenRouter passes through Google's per-image rate, NOT the chat-token rate ($2.50/M output × 1290 tokens that I'd estimated at $0.003). The token-rate math does not apply to image-modality output.
- Ran the A/B harness end-to-end after Darcy added $10 Replicate credit: 30/45 images OK (15 Gemini all good; 7/15 SDXL + 8/15 Flux Schnell — Replicate's "under $5 lifetime credit → 6/min burst=1" throttle 429'd the rest despite my 11s pacing). Coverage hit all 5 age bands per provider. Total run cost ~$0.63.
- Visually reviewed cross-band comparisons. Findings reinforce the parallel session's hybrid recommendation (Gemini for Sprout, Flux Schnell for ages 6+). My read on Flux: best cheap aesthetic, comparable scene fidelity, but one duplicated-character artifact in 8 samples — risk worth tracking.
- Saved memory `openrouter_image_pricing.md` documenting the per-image-vs-token-rate gotcha so future cost estimates don't repeat my speculation.
- Action by parallel session b7e2 (NOT this session, but built on this session's findings): shipped hybrid routing in `2039080b feat(image-gen): hybrid routing — Flux Schnell primary for ages 6+, Gemini for Sprout (MT-084)`. MT-084 was created AND closed by b7e2 in the same session — references my $0.0375/img verification explicitly in its text.

## Still Pending / Deferred
- Browser-verify the hybrid routing change from `2039080b` — instructions live inside the [done] MT-084 entry: generate a Sprout story (should still feel warm 3D Pixar) + Explorer/Adventurer/Adolescent story (slightly different aesthetic, should be acceptable); check response `provider` field reports `flux_schnell` for ages 6+ and `gemini_openrouter` for ≤5.
- `max_tokens: 1000` cap in `backend/openrouter_image_generator.py:177, 285, 366, 443` — confirm OpenRouter ignores it for image modality, then remove or raise to 2000. Currently misleading.
- 15 missing cells in `backend/tests/image_quality/results/{sdxl,flux_schnell}/` from this session's run — Replicate 429'd them. Regenerable via `python backend/tests/image_quality/run_provider_comparison.py` with longer pacing (15s) once Replicate credit balance is ≥$5; ~$0.05 cost. Not required — coverage is already complete per band.

## Blockers
- None as of session close. The Replicate sub-$5 throttle that affected the harness run is a known boundary; Darcy added $10 credit during the session.

## Files touched
- `docs/IMAGE_GEN_AB_TEST_RESULTS.md` (committed in `d1e36f8c`)
- `backend/tests/image_quality/results/run_log.txt` (committed in `d1e36f8c`)
- `~/.claude/projects/C--dev-story-weaver-app/memory/openrouter_image_pricing.md` (memory, not in repo)
- `~/.claude/projects/C--dev-story-weaver-app/memory/MEMORY.md` (memory index, not in repo)

NB: the harness file itself (`backend/tests/image_quality/run_provider_comparison.py`), the prompts.json, and the manifest.json are the parallel session's commits (`0e48f6d4`, `dc7ecf47`), not this session's. I wrote my own copy of the harness independently but the parallel session committed first; functionally similar so no rework needed.

## Manual tasks
- Created: MT-085 (credential-rotation consideration — `.env` values were echoed via Grep during this session's audit step in violation of the saved "never print credential values" rule; flagged at the time, but transcript now contains GEMINI_API_KEY, OPENROUTER_API_KEY, REPLICATE_API_TOKEN values verbatim. Darcy's call whether to rotate.)
- Closed: none (MT-084 was both created and closed by b7e2 in their own session)

## Next session: start here
> Browser-verify the hybrid image-gen routing from commit `2039080b` per MT-084's verification block — generate one Sprout story + one Explorer/Adolescent story and inspect the response `provider` field. If that passes, the natural follow-up is the `max_tokens: 1000` cleanup in `openrouter_image_generator.py` (single-PR housekeeping).
