# SESSION CLOSE — 2026-05-02 15:57 [5653] — Branch: main — TTS quota 503 + Sprout scene tap auto-advance

## Accomplished
- **TTS quota error handling** (commit `6b0b38b8`): ElevenLabs `quota_exceeded` (HTTP 401) now returns 503 instead of 500, so Flutter falls back silently to on-device TTS. Also `generate_speech_with_timestamps` re-raises quota errors immediately instead of falling through to `generate_speech` (which would also fail, doubling the round-trip).
- **Sprout scene tap auto-advance** (commit `6b0b38b8`): `_onSceneTap` in `hero_creator_step.dart` now calls `_heroNextPage()` for age ≤ 5 after selecting a scene tile. Previously tapping "Stomp with Dinosaurs", "Rainbow World", or "Under the Sea" as a 3-year-old only set internal state — no visible forward motion. Pattern already existed for `big_feelings_quest`; now consistently applied to all three story-world tiles.
- **Orphaned session-9471 code committed** (commit `e563b3f8`): `companion_widgets.dart` selection bounce animation + float phase fix, `hero_input_widgets.dart` border radius refactor, two test timer fixes — were sitting uncommitted since session 9471 closed.
- **Diagnosed ElevenLabs quota exhaustion**: Darcy is on the Impact Program Creator plan (100k credits/month); credits reset ~May 5. Until then on-device TTS is the fallback. No API key change needed — the key and account are correct.
- **Confirmed ocean tile image is correct on disk**: `assets/images/ui/sprout/tiles/ocean.png` is the octopus/coral scene. The wrong image in the browser is browser cache, not a code or asset bug.

## Still Pending / Deferred
- MT-023: Railway grand-light frontend still needs a manual redeploy to show the ocean tile fix in production.
- MT-032: Browser-verify the scene tap auto-advance fix locally (Ctrl+Shift+R + walk the Sprout wizard).

## Blockers
- ElevenLabs credits exhausted until ~2026-05-05 reset — on-device TTS is the fallback in the meantime.

## Files touched
- `backend/elevenlabs_tts_service.py`
- `backend/routes/tts_routes.py`
- `lib/screens/wizard_steps/hero_creator_step.dart`
- `lib/widgets/hero_creator/companion_widgets.dart` (orphaned from session 9471)
- `lib/widgets/hero_creator/hero_input_widgets.dart` (orphaned from session 9471)
- `test/widgets/big_feelings_flow_screen_test.dart` (orphaned from session 9471)
- `test/widgets/wizard_steps/hero_creator_step_test.dart` (orphaned from session 9471)

## Manual tasks
- Created: MT-032
- Closed:  none

## Next session: start here
> Do MT-032 first: hard-reload Chrome (Ctrl+Shift+R) and walk the Sprout wizard as a 3-year-old to confirm scene tiles advance the page. Then trigger Railway grand-light redeploy for MT-023 (ocean image in production).
