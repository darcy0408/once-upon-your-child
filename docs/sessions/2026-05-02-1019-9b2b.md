# SESSION CLOSE — 2026-05-02 10:19 [9b2b] — Branch: main — Sprout avatar simplification, ocean tile fix, story fallback cleanup + Gemini key rotation

## Accomplished
- **`63366ea3`** — Sprout avatar wizard stripped from 6 steps to 4 (welcome → gender → favoriteColor → photo). Removed `hairColor` and `eyeColor` steps (AI infers both from photo). Expanded `_sproutFavoriteColors` to 9 entries (3×3 grid of 88 px circles): Red, Orange, Yellow, Green, Blue, Light Blue, Dark Blue, Purple, Pink. Added `Light Blue`/`Dark Blue` swatches. Pre-warmed all 9 color names in `kWarmUpPhrases` to fix the dual-voice bug (color-tap echo was falling to robotic flutter_tts fallback because the names weren't in the ElevenLabs cache).
- **`0ba55f14`** — Replaced `assets/images/ui/sprout/tiles/ocean.png` with the correct underwater scene (purple octopus, clownfish, seahorse, coral). Was showing the crystal-cave image. Source: `StoryWeaverImagesToShare/5cf081bf-d729-4fc6-8cbd-8266979d70ef.jpg`.
- **`b5c2266c`** — Fixed `_fallback_story()` in `backend/tasks/story_tasks.py`: was returning bracket-tagged plain text (`[TITLE: ...]`, `[WISDOM GEM: ...]`) that bypassed JSON parsing and rendered raw in the UI as a 1-sentence 2-page story. Now returns valid JSON (4 clean pages). Added Gemini key rotation to `StoryGenerationService`: reads `GOOGLE_API_KEY_2/3/4` from env and rotates on `ResourceExhausted` before giving up.
- **Diagnosed OpenRouter key failure** — the `OPENROUTER_API_KEY` in `.env` and Railway was `dc4c...`, which doesn't appear in the account's key list (deleted or from a different account). This caused every OpenRouter fallback attempt to 401, silently dropping to the static fallback. User updated the key on Railway and in local `.env`.

## Still Pending / Deferred
- Hero-creator wizard changes are in-flight (tracked but uncommitted): `lib/screens/wizard_steps/hero_creator_creative_brief.dart`, `hero_creator_scene_page.dart`, `hero_creator_story_type_page.dart`, `magic_review_step.dart`, `lib/widgets/hero_creator/hero_input_widgets.dart` — 406 insertions, parallel work, left untouched.
- `pubspec.lock` and generated plugin registrant files also modified (parallel work, not staged).
- Untracked: `assets/images/archetypes/sprout/animal_whisperer.jpg`, `assets/images/archetypes/sprout/brave_hero.jpg` — new sprout archetype images, not yet committed. Whoever owns those should stage them when ready.

## Blockers
- None for completed work. Story generation (Gemini key rotation + OpenRouter fix) needs production smoke-test (MT-022).

## Files touched
- `lib/custom_avatar_screen.dart`
- `lib/services/app_tts_service.dart`
- `assets/images/ui/sprout/tiles/ocean.png`
- `backend/tasks/story_tasks.py`
- `backend/services/story_generation_service.py`

## Manual tasks
- Created: MT-021, MT-022, MT-023
- Closed: none

## Next session: start here
> Smoke-test story generation on production (MT-022) — walk a Sprout user all the way through to a rendered story and confirm it's a real multi-page story, not the fallback. Then verify the avatar wizard 4-step flow (MT-021) and the ocean tile (MT-023) while you're there.
