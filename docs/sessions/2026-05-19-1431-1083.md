# SESSION CLOSE — 2026-05-19 14:31 [1083] — Branch: main — Reword hero-creator greeting + diagnose robotic TTS voice

## Accomplished
- Reworded the hero-creator page-1 voice greeting in `lib/screens/wizard_steps/hero_creator_step.dart` (`_speakPagePrompt`). Replaced the Sprout-only *"Will [name] be your hero? Tap a picture!"* with a warm *"Hi [name]! Let's go on a magical adventure together…"*, and ADDED a band-tailored page-1 greeting for Explorer (6-8) and Adventurer (9-12), which previously had no page-1 voice prompt at all. Wording steps up with age. — landed in HEAD via commit `68df77de` (a parallel session's `git add` swept the file in via the known index race; the change is intact and correct in HEAD, and already pushed).
- Diagnosed the "robotic voice" report — **not a bug**. Verified end-to-end: the production backend `/tts/synthesize` returns `provider: edge` (natural Microsoft Edge neural voice — ElevenLabs is off, Edge fills in as designed). The production web build (`Dockerfile.frontend`) compiles with `--dart-define=FLAVOR=production`, so real users hit that backend and get the natural voice. The robotic voice only occurs in local dev: `flutter run -d chrome` with no `--dart-define` uses the `development` flavor → backend `http://127.0.0.1:5000`; with no local backend running, TTS calls fail and the app drops to the browser's robotic `SpeechSynthesis` voice. Pointing dev-Chrome at prod does not help — prod returns HTTP 403 for `localhost` origins (localhost CORS is dev-only by design).

## Still Pending / Deferred
- None for this session's scope. The greeting change needs a post-deploy audio check — filed as MT-164.

## Blockers
- None.

## Files touched
- lib/screens/wizard_steps/hero_creator_step.dart  (committed in 68df77de by a parallel session)
- docs/MANUAL_TASKS.md  (added MT-164)

## Manual tasks
- Created: MT-164
- Closed:  none

## Next session: start here
> The greeting change is committed and pushed; once the frontend redeploys, run MT-164 — audio-verify the hero-creator page-1 greeting on all three age bands. No code follow-up needed for the TTS "robotic voice" report; it was confirmed to be a local-dev-only artifact (no local backend on `flutter run -d chrome`).
