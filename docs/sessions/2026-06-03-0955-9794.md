# SESSION CLOSE — 2026-06-03 09:55 [9794] — Branch: session/finops — Cost/FinOps audit + actioned F-01/F-02/F-04/F-05, verified F-03 (PR #205)

## Accomplished
All work is on `session/finops` → **PR #205** (not yet merged to main). Done in an isolated worktree at `C:\dev\sw-finops` per the multi-agent protocol.

- **Cost & FinOps audit** (`f92624da`) — ran the `10-cost-finops.md` audit prompt. Fanned out 4 parallel Explore agents (AI text, image gen, Stripe/tiers, infra/observability), grounded rates against the repo's own `cost_tracker.py`. Wrote `audit-reports/10-finops-20260602.md` + `unit-economics-model.csv`. **Headline: text gen is ~$0.001/story (negligible); TTS dominates.** 7 findings (F-01..F-07).
- **F-01 (Critical) fixed** (`b01627ce`) — ElevenLabs was the *default* paid narration voice ($0.18/1k chars ≈ $1.62/medium story, ~1000× text cost; ~6 narrations/mo erased a premium subscriber's margin). Demoted ElevenLabs to **opt-in** (`premium_voice=true`, or `character_voice_id` for dialogue — ElevenLabs-only). Default paid voice is now **Gemini Flash TTS → Edge → on-device**. Surgical: just gated the existing overflow chain. `backend/routes/tts_routes.py` + tests reworked (13 passed). **No frontend change needed** — client sends `character_voice_id` only for dialogue, so single-voice narration auto-moves to Gemini. User-approved trade-off: regular narration loses ElevenLabs word-timestamps → read-along highlighting falls back to text-only on short stories.
- **F-02 (High) fixed** (`f3ac4f2b`) — paid OpenRouter text *fallback* was Claude Sonnet 4.7 (~50× Gemini text cost). Repointed to **Claude Haiku 4.5** (~3× cheaper) — deliberately stayed on Anthropic to keep cross-provider redundancy vs a Google outage + native JSON + low child-refusal. `backend/services/openrouter_story_generator.py:19`. Env-overridable. 34 tests passed.
- **F-04 (Medium) fixed** (`138dc1fd`) — `backend/cost_tracking.py` `COST_RATES` diverged from the verified `cost_tracker.py`. Reconciled (flash $0.075/$0.30, flash-lite $0.0375/$0.15) **and** added the missing `gemini-2.5-flash` key, which fixed a latent KeyError (DEFAULT_MODEL pointed at an absent key). 8 cost tests pass.
- **F-05 (Medium) fixed** (`138dc1fd`) — `lib/main.dart` Sentry `tracesSampleRate` 0.2 → 0.05 (release). Error `sampleRate` and the COPPA `beforeSend` consent gate untouched. `flutter analyze lib/main.dart` clean.
- **F-03 (High) verified DORMANT** (`86a5bb8e`) — verified Cloudflare Workers AI pricing (web + live dashboard): **Workers Free plan, 0/10k Neurons/day, app pre-launch**. flux-1-schnell @ 4 steps = $0.00042/img; free pool ≈ 260 imgs/day. Cascade (`story_routes.py:1684-1690`) is **already** ordered cheapest-first → no code change. Updated the report's placeholders with verified numbers; filed **MT-220** as the scale trigger.

## Still Pending / Deferred
- **PR #205 awaits review/merge.** Contains the audit + 4 fixes + F-03 verification + MT-220. All green (CI will run flutter analyze gate #204).
- **F-06 (Low)** — TTS prewarm 119 phrases × up to 4 retries/session → make cache-first (a TTS cache like the illustration cache). Report only; deferred (low value pre-launch).
- **F-07 (Low)** — Stripe monthly 5.9% vs annual 3.3% effective → push annual plans in the paywall. Report only; deferred.
- **Re-verify the ElevenLabs plan's monthly character allotment** before acting on exact F-01 dollar figures — the one external number I couldn't confirm; it sets the ceiling on TTS cost exposure.
- **Optional prod check:** 0 Neurons could be confirmed as "Cloudflare is the live image path" via a one-story smoke test once launched (not urgent — explained by pre-launch).

## Blockers
- None. (F-03 "blocked on pricing verification" was resolved this session.)

## Files touched
- audit-reports/10-finops-20260602.md
- audit-reports/unit-economics-model.csv
- backend/routes/tts_routes.py
- backend/tests/unit/test_tts_routes_fallback.py
- backend/services/openrouter_story_generator.py
- backend/cost_tracking.py
- lib/main.dart
- docs/MANUAL_TASKS.md (MT-220 added)
- docs/sessions/2026-06-03-0955-9794.md (this record)
- TEAM_COORDINATION.md (index row)

Untouched (left for their owners): worktree `*/flutter/generated_plugin_registrant.*` (Flutter-generated churn from `flutter analyze`); main-checkout parallel-session work (`backend/services/story_service.py` + `audit-reports/14-story-quality-*` + `backend/tests/quality/adventurer_audit_gen.py`).

## Manual tasks
- Created: MT-220 (add Cloudflare Workers Paid $5/mo when daily Neurons near 10k — scale trigger, dormant pre-launch)
- Closed:  none

## Next session: start here
> Review + merge **PR #205** (cost audit + F-01/F-02/F-04/F-05 fixes; all green). The audit's Critical/High/Medium items are all handled — only Low F-06/F-07 remain, best deferred until post-launch usage data exists. Nearest-launch follow-up: confirm the ElevenLabs plan's monthly character allotment (the one unverified external number bounding TTS cost).
