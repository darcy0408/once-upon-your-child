# SESSION CLOSE — 2026-05-14 22:59 [358e] — Branch: main — Multi-agent Phase 1 follow-through: MT-118 / MT-119 / MT-121 / MT-104 all closed + verified

## Accomplished

- **MT-118** root-caused, fixed, and Playwright-verified end-to-end on production. `wizard_data_mapper.dart` was gating `theme='superhero'` and the hero* fields on `selectedScenario == 'superhero'`, but after the superhero flow set that value, any subsequent scene-tile tap at Pick Place rewrote it to (e.g.) `rainbow_world` — silently demoting the request off the superhero dispatch path. Changed both mapper gates to use the sturdier `heroPower != null && heroPower!.trim().isNotEmpty` (heroPower is set once in `superhero_power_screen.dart:163` and never touched by later steps). Also suppressed `conflictHook` / `sensoryPalette` / `worldBible` inheritance when in superhero mode — the superhero prompt chain owns those, and feeding it a non-superhero world bible was the contamination contributing to MT-121's villain drift. 4 new tests in `test/screens/wizard_data_mapper_test.dart` (4/4 pass). Commit `db0a4ccf` (fix) + `f4226978` (debug rollback + ticket close).
- **MT-119** (COPPA bypass useless in production) closed via option (c) doc fallback. Updated `docs/agent-briefs/PLAYWRIGHT_SMOKE_TEST.md` with a new "COPPA parental consent — Playwright canonical approach (post MT-119)" subsection documenting the scroll-then-click pattern + flagging that `?bypass_consent=1` is local-debug-only because `kDebugMode` tree-shakes the bypass out of release builds. Commit `d4c8f7c2`.
- **MT-121** Explorer Superhero villain roster + costume color regression — delegated to agent (worktree `worktree-agent-aa5b98ccf4c30d285`, commit `cbb00c83`), merged in `c9f14b10`. Agent (1) strengthened `_build_superhero_prompt_explorer` to enumerate the full canonical villain list with "MUST be one of" language plus an anti-abstract-motif clause naming the exact substitutes the model was reaching for (Whispering Mountain, Rainbow Maze, riddle/puzzle substitutes); (2) fixed `lib/screens/wizard_steps/superhero_entry_screen.dart:69` which was hardcoding Sprout plum `0xFF2D1B42` for the loading scaffold regardless of band — now reads `gradientStart` from the active band; (3) brightened the Explorer gradient in `age_band_theme.dart` from near-black-purple to vivid royal-violet/magical-purple/indigo-purple. 17/17 prompt tests pass. **End-to-end verified by the MT-118 verification walk** — the generated Explorer Feeling Sense story used Echo Bandit (canonical roster) as the villain.
- **MT-104** SubscriptionService dedup — delegated to agent (worktree branch `mt-104-subscription-dedup`, 7 commits `4755abe0` → `59b714b7`), merged in `75a57d2a`. Agent executed full 10-step plan from `docs/agent-briefs/BRIEF_subscription_dedup.md`: deleted `lib/subscription_service.dart` (the broken local-only 268-line class), absorbed its useful methods onto canonical `lib/services/subscription_service.dart`, migrated 7 production consumers (character_management_screen_v2, interactive_story_screen, main_story, paywall_dialog, pick_a_path_adventure_screen, quick_story_screen, settings_screen), rewrote `test/integration/paywall_test.dart` with injected fake SubscriptionSyncService (2/2 pass). Step 8 Option B: dev "Force Premium / Reset to Free" buttons now write to the correct SharedPref keys (`is_paid_premium`, `subscription_status`) instead of the wrong `user_subscription` key — closing a silent-broken-for-months bug.
- **Phase 1 housekeeping**: 57 untracked `p1-*.png` / `p3-*.png` Playwright screenshot artifacts from the prior MT-107 verify deleted; `.gitignore` extended with the `/p1-*.png` `/p3-*.png` patterns to prevent re-accumulation. Commit `d4c8f7c2`.
- **MT-125 filed** during MT-104 close — pre-existing `SubscriptionTier.premium.displayName == 'Premium'` vs widget tests asserting "Adventurer" mismatch in `test/widgets/subscription_ui_test.dart` and `test/widgets/subscription_sync_widget_test.dart`. Already present on `main` pre-MT-104; 2-line follow-up.

## Still Pending / Deferred

- **MT-120** verify (per-page visual override on a paid-tier account) — still blocks the full MT-117 close. Now that MT-104 Step 8 fixed the dev "Force Premium" button to write the right SharedPref keys, this verify is actually unblocked. Walk: Explorer Feeling Sense story on a free-tier account with Force Premium toggled, then check that pages 2-5 render the soft empathy halo (Feeling Sense) and translucent wisp silhouette (Soft Step). If neither override appears, troubleshoot the per-page Gemini/Flux dispatch starting at `backend/routes/story_routes.py:1045`. ~30 min.
- **MT-122** (off-Railway pg_dump) and **MT-124** (first DAILY snapshot lands in 24h) — both deferred from prior session; unchanged. MT-124 should be checkable on 2026-05-15 evening or 2026-05-16 morning.
- **MT-125** (filed this session) — 2-line displayName test fix; deferred as a tomorrow item.
- The **MT-118 mystery hour** is worth noting: I deployed the heroPower fix, ran a fresh walk that still showed pre-fix behavior, spent ~25 min adding debug prints and inspecting the bundle headers, then a third walk against a freshly-fetched bundle worked. Almost certainly browser HTTP-cached `main.dart.js` from the pre-fix deploy — the etag was different but the browser was happy to keep serving the old one until cache expiry / hard reload. **Lesson for future Playwright re-verifies after deploy:** call `caches.keys() → delete()` AND unregister service workers AND issue a HEAD on `main.dart.js` to compare Last-Modified against the new deploy time, before declaring "fix didn't work."
- 4 unmerged backbone branches from prior sessions still exist locally: `agent/integration-tests`, `image-gen-ab-test`, and the two merged worktree branches `worktree-agent-aa5b98ccf4c30d285` + `mt-104-subscription-dedup` (worktrees should be cleaned up). Not urgent; `git worktree prune` and `git branch -d` when convenient.

## Blockers

- None during the productive part of the session. The MT-118 verify dragged because of the browser-cache issue described above, but it resolved on the third walk.

## Files touched

This session's commits (`db0a4ccf`, `f3cede33`, `c9f14b10` [merge], `75a57d2a` [merge], `d21ff6c2`, `f4226978`, plus the prep commit `d4c8f7c2` and merge prep commits):

- `backend/services/prompt_service.py` (MT-121 agent)
- `backend/tests/test_superhero_prompt_explorer.py` (new, MT-121 agent)
- `lib/screens/wizard_steps/wizard_data_mapper.dart` (MT-118 fix)
- `lib/screens/wizard_steps/superhero_entry_screen.dart` (MT-121 agent)
- `lib/theme/age_band_theme.dart` (MT-121 agent, Explorer gradient)
- `lib/services/subscription_service.dart` (MT-104 agent, +130 lines absorbing the deleted methods)
- `lib/subscription_service.dart` (MT-104 agent, **DELETED**)
- `lib/character_management_screen_v2.dart` (MT-104 agent, import migrate)
- `lib/interactive_story_screen.dart` (MT-104 agent, import migrate)
- `lib/main_story.dart` (MT-104 agent, import migrate)
- `lib/paywall_dialog.dart` (MT-104 agent, import migrate)
- `lib/pick_a_path_adventure_screen.dart` (MT-104 agent, import migrate)
- `lib/quick_story_screen.dart` (MT-104 agent, import migrate)
- `lib/settings_screen.dart` (MT-104 agent, dev-buttons rewrite + import migrate)
- `test/integration/paywall_test.dart` (MT-104 agent, full rewrite)
- `test/screens/wizard_data_mapper_test.dart` (new, MT-118 fix)
- `backend/tasks/story_tasks.py` (diagnostic log added then rolled back)
- `.gitignore` (Playwright screenshot patterns)
- `docs/agent-briefs/PLAYWRIGHT_SMOKE_TEST.md` (MT-119)
- `docs/MANUAL_TASKS.md` (closes + MT-125 file)

## Manual tasks

- **Created:** MT-125 (Premium/Adventurer displayName test mismatch)
- **Closed:** MT-104, MT-118, MT-119, MT-121

## Next session: start here

> **MT-120 is the cheapest remaining win** — MT-104 Step 8 just fixed the "Force Premium" dev button, so the previously-blocked per-page visual verify is now actually doable. Walk: free-tier signup → settings → Force Premium → Explorer Feeling Sense story → check pages 2-5 for the soft empathy halo. ~30 min including the walk. After that, **MT-125** is a 2-line cleanup that gets the test suite fully green again.
