# SESSION CLOSE — 2026-05-17 21:42 [149a] — Branch: feat/cost-reduction — Free Edge TTS narration fallback shipped; prod deploy crash fixed

## Accomplished
- Shipped a free **Microsoft Edge TTS fallback** for story narration — when ElevenLabs is unconfigured/out of credits, `/tts/synthesize` now returns natural neural MP3 (with per-word timestamps) instead of 503. New `backend/edge_tts_service.py`, wired into `backend/routes/tts_routes.py`; `edge-tts==7.2.8` locked into requirements. Commit `ad1a0a0e` (on `main`, deployed).
- Fixed a **production deploy crash** — the security-hardening non-root container user couldn't create `/app/backend_errors.log` because `COPY --chown` doesn't chown the WORKDIR itself. Added `RUN chown app:app /app` to both Dockerfiles and made the log `FileHandler` best-effort. Commit `aa2ccb4b` (on `main`, redeployed clean).
- Removed dead `backend_last_error.log` debug writes from `story_routes.py`. Commit `4ba94cb8` (on `main`).
- Root-caused the "robotic voice" report: local `backend/.env` had `TTS_DISABLED=true`, which 503s before any provider is tried. Darcy flipped it — local `/tts/synthesize` now returns `provider: edge`. Production never had `TTS_DISABLED` set (verified via Railway agent).
- Confirmed the ` RAILWAY_FRONTEND_URL` Railway env var had a leading space (so `os.environ.get` never matched it); Darcy renamed it — re-verified clean.
- Committed BYOK cover-illustration routing (`ffb3ab20`) and a MANUAL_TASKS docs entry batch (`e9d113ab`) on `feat/cost-reduction`.
- Ran `feat/cost-reduction` readiness checks: backend pytest **893 passed / 0 failed**; `flutter analyze` **0 errors** (2 trivial lints).

## Still Pending / Deferred
- **MT-147** — companion-story generation failure is misdiagnosed. The prior "premium gate" theory is disproven (no backend companion gate exists; `runWithScaffoldFallback` does not swallow 403s). Real cause unreproduced — likely a sync-generation timeout on companion-heavy prompts. Needs reproduction + fix.
- `feat/cost-reduction` → `main` merge: readiness checks are green, but MT-147 should be resolved before merge (free-tier companion stories degrading to a canned 1-page story is a real UX hit). Merge timing is Darcy's product call.
- MANUAL_TASKS collision: commit `e9d113ab` added BYOK/illustration entries MT-140..145, but parallel session `ebfe` reused those numbers for legal-compliance tasks and won the reconcile. The BYOK entries (wizard "go solo" no-op, BYOK illustration fallback gap, story re-entry, redundant wizard arrows, character-creation gating model) are LOST from the file. Only the companion-gating one was salvaged (corrected) into MT-147 — the other ~4 may need re-logging if still relevant.
- `flutter analyze` lints (non-blocking): `use_build_context_synchronously` in `byok_setup_wizard.dart:731`; unused `_averageWordsPerPage` in `story_result_screen.dart:1145`.
- MT-016 (Sprout web TTS browser-verify) was "blocked on MT-091" — MT-091 is now closed, so MT-016's audio half is unblocked and re-verifiable.

## Blockers
- None.

## Files touched
- backend/edge_tts_service.py (new)
- backend/routes/tts_routes.py
- backend/requirements.in
- backend/requirements.txt
- Dockerfile
- Dockerfile.worker
- backend/app.py
- backend/routes/story_routes.py
- lib/story_illustration_service.dart
- docs/MANUAL_TASKS.md

## Manual tasks
- Created: MT-147
- Closed:  MT-091

## Next session: start here
> Reproduce the companion-story generation failure (MT-147) — generate a story with a companion and capture the real exception from `runWithScaffoldFallback`; fix the true cause (likely a sync-generation timeout on companion-heavy prompts). That is the last thing gating a `feat/cost-reduction` → `main` merge.
