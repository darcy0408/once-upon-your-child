# Session 2026-07-16 16:05 (c1c0) — Actions billing root-caused (PR #453) + rubric-transcription prompt fix, all 6 bands (PR #454)

**Branch:** main checkout untouched; work in two worktrees — `C:\dev\sw-ci-cost` on `session/ci-cost` → **PR #453 (OPEN)** and `C:\dev\sw-prompts` on `session/prompts` → **PR #454 (OPEN)**. Both pushed; neither merged (CI still blind — see Blocked).

## Done

**1. GitHub Actions outage (MT-376) root-caused — it is NOT a failed payment.**
- Owner pasted the billing page: no payment due, no payment history, net $0.00 owed. Gross metered usage **$79.87 vs $79.91 included** — the allowance is exhausted to within four cents, and with the Actions spending limit at $0 (correct — keep it), every job account-wide refuses to start. That included free-tier public-repo Linux jobs, because the hold is account-level.
- Culprit: `story-weaver-app` = **$75.39 of the $79.87 (94%)**, and the only metered runner in the repo is `.github/workflows/ios-build.yml` on `macos-latest` (10× billing, meters even on public repos), triggered on `pull_request` touching `lib/**` — i.e. nearly every PR, ~7m40s each. `ios-testflight.yml` is workflow_dispatch-only and innocent.
- **PR #453**: drops `lib/**` from the paths filter; the job still fires on `ios/**`/pubspec changes and by hand. iOS shipping is blocked on owner store setup (MT-350) anyway, so per-PR iOS compiles were guarding an unshippable path. One-file config change; cannot show green CI for the same reason it exists.
- Repo visibility checked: **still PUBLIC** (owner believed it had been made private — it wasn't). Note the MT-366 private-flip cost estimate is understated: it assumed Linux minutes; one macOS job alone burned $75 in two weeks.

**2. Prod backend confirmed healthy end-to-end.** `/health` 200, `/auth/anonymous` issues tokens, `POST /generate-story` returned a complete 10-page story (openai success, no fallback). Caveat: the hand-rolled curl payload's age field didn't map (task kwargs default it), so that story generated as Sprout — real clients send the proper contract.

**3. The big one: every age band was transcribing its writing rubric into child-facing prose (PR #454).**
- Evidence-first: generated one fixed-input story per band (ages 4/7/10/13/16/30) with current prompts via OpenRouter gpt-5-mini (`scripts` in session scratchpad; ~$0.10). Every band except Explorer leaked: "Attempt one: Mia climbed a low drift. It failed." (also observed on live prod), "Jordan felt the first escalation", "The climax turned on Priya's decision", "his flaw—he could be brisk", "The companion arc completed", "The cost was named out loud: scraped hands", Sprout pages ending in bare "CRUNCH...?" (8 of 10 pages).
- Root cause: the band rules in `backend/services/story_service.py` (standard path, `AdvancedStoryEngine.generate_enhanced_prompt`) describe structure in craft vocabulary (attempt/want/flaw/escalation/cost/arc) and dictate copyable wording ("Attempts 1 and 2 fail", "name the cost") — the model reuses the vocabulary it is handed. Round 2 showed even rule HEADINGS get copied ("Escalation came when...").
- Fix, prompt side: global immersion rule now bans the concrete transcription forms with examples; RULE OF THREE / TRY-FAIL / MOUNTING PRESSURE (né ESCALATION) / COMPANION DEPTH (né WANT + FLAW) reworded to avoid the copyable nouns; SILENT SKELETON CHECK final self-pass for the 8-12/13-18/adult blocks. Band extras: Sprout complete-sentence page hooks (kills "ZING...?"), contractions in Sprout dialogue, no prose+caps sound restating (Explorer), name-echo cap in ≤12 POV rule, sensory palette never copied verbatim (a prod story rendered "Cold fresh air on cheeks." straight from the Sprout table).
- Fix, post-processing net: new `_strip_attempt_labels()` (excises "Attempt N:" prefixes keeping content, drops bare "It failed." sentences) wired before `_strip_meta_leakage`; `_META_LEAK_TERMS` extended. Unit tests in `backend/tests/test_strip_attempt_labels.py`.
- Verification: same six stories regenerated per round. Final: 5 bands zero leak patterns; Adolescent 17 → 2 borderline idioms (natural first-person "it didn't work", one "turning point" idiom). Full backend suite **1853 passed / 0 failed** in the worktree; black + flake8 clean; two pinned sensory-palette assertions updated (`test_story_service.py`, `test_cinematic_features.py`).

## Decisions
- Keep the Actions spending limit at $0 (guarantees $0 charges); the open question is timing, not money — see Blocked.
- Rule vocabulary is now treated as part of the attack surface for prompt quality: rules must not name their own devices in copyable nouns. Headings renamed so a leak degrades to idiomatic prose.
- MT-376 updated in place (root cause + remaining decision) instead of filing a new MT.
- Baseline/after story artifacts kept in session scratchpad only (not committed) — the PR body carries the comparison table.

## Next (ordered)
1. **OWNER: billing decision (MT-376)** — wait for the Jul 31 allowance reset (16 days of blind CI) or set a small spending limit (~$5) to unblock now. With #453 merged, realistic extra spend is $0, but note free-tier Linux jobs were ALSO blocked by the account-level hold, so a small limit is the likely-but-not-guaranteed unblock.
2. Merge **PR #453** (config-only, nothing to test) — August must not repeat July's burn either way.
3. Merge **PR #454** (verified locally: 1853 tests, six-band before/after) — every story generated after deploy improves.
4. **PR #450's `test` job failed after a real 7m38s run** (pre-outage) — a genuine failure nobody has read yet; diagnose before merging avatar polish. Also memory says its merge was held on a live COPPA-gate regression — re-check that state.
5. PR #452 (BYOK sunset) — blocked purely by CI blindness; merge decision after billing.
6. Carried from 6573: post-#448-deploy re-demo (audio + adult Medium story), MT-374 owner decision pass, MT-371 Sprout illustration cohesion.

## Blocked on user
- **MT-376 decision** (above) — nothing shows green CI until Jul 31 or a limit bump.
- Four PRs are open (#450, #452, #453, #454); merging while CI is blind is owner's call per PR.

## Risks / unverified
- PR #453 and #454 have no CI runs (billing) — #454 is locally verified (full backend suite + six-band regeneration); #453 is workflow-config-only.
- Prompt changes verified against gpt-5-mini via OpenRouter (prod model, different route). Prod uses direct OpenAI with `reasoning_effort=low` — behavior should match, but the post-merge spot-check in Next #6 is the proof.
- SESSION_HISTORY.md (docs/context/sessions/) is ~3 months stale (top entry 2026-04-20); the real handoffs are SESSION_NOTES.md + docs/sessions/. Worth retiring or backfilling the stale file some session.
- The `_strip_attempt_labels` failure-announcement regex is deliberately narrow (It/That/The plan/The try + failed/didn't work) to avoid eating legitimate prose.
