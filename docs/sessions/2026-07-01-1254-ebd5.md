# SESSION CLOSE — 2026-07-01 12:54 [ebd5] — Branch: session/crux-client — Crux Choice client (MT-258) shipped behind OFF flag; MT-251 dots found stale

## Accomplished
- **MT-258 Crux Choice client, phases 3–5 — PR #359** (`session/crux-client`, off `origin/main` @ 33146242). Entirely behind `FeatureFlags.cruxChoiceEnabled` (default OFF) → live single-shot path byte-for-byte unchanged.
  - **Phase 3** (`635e2fac`): `AntiheroCruxResult`/`CruxChoice` models; `ApiServiceManager.generateAntiheroCrux` + `generateAntiheroResolution` (POST the #273 endpoints, `ApiError`/`HttpException` on non-200, body sends the exact snake_case keys `_build_antihero_task_kwargs` reads); `lib/config/feature_flags.dart`; 7 unit tests.
  - **Phase 4** (`061af395`, delegated to a fresh general-purpose agent, contract locked first): `story_result_screen.dart` — 4 null-default constructor params; end-of-story page becomes `_buildCruxChoicePage` (crux line + two noir `_CruxChoiceCard`s) when a choice is pending; on tap → `generateAntiheroResolution` → splice full 7-beat story into `_storyPages` → `onCruxResolved(superheroMeta)` → jump to Beat 5. Fires from both render paths (page-flip + ListView).
  - **Phase 5** (`061af395`): crux branch in `magic_review_step._doLaunchStoryCreation` (flag + adolescent + superhero + antihero) → `_launchAntiheroCrux`; `_recordAntiheroSaga` DEFERRED into `onCruxResolved` so no saga Issue is recorded for an abandoned crux (mirrors single-shot band-aware `hero_code`: heroLine → heroSecret).
  - Verify: `flutter analyze` on all 5 touched files → **No issues found!**; 7 new crux tests + existing `story_creation_flow_test` (generateStory) all green.
- **MT-251 dots sub-item — found STALE, no code needed.** Both `_ProgressDots` invocations (costume `superhero_costume_screen.dart:391`, Edge `superhero_power_screen.dart:601`) already receive the band-aware `_gold` getter (adolescent→teal, creator→purple) — the later MT-273/274 noir reskin fixed it after MT-251's note was written. Annotated MT-251 in place.

## Still Pending / Deferred
- **MT-258 remaining:** owner e2e device-verify + flip `cruxChoiceEnabled` (tracked as MT-316); roadmap leftovers B2/C1/E1/D2 still open (Claude-doable).
- **PR #359 not yet merged** — CI will run the analyze gate (#204); the app was NOT walked e2e against a prod generation (flag is OFF, so nothing to walk until flipped).
- Design doc `docs/ADOLESCENT_CRUX_CHOICE_DESIGN.md` Status marked phases 1–5 done (committed in `061af395`).

## Blockers
- None. Worktree `C:\dev\sw-crux-client` left in place until #359 merges (then `git worktree remove`).

## Files touched
- lib/models/antihero_crux_result.dart (new)
- lib/config/feature_flags.dart (new)
- lib/services/api_service_manager.dart
- lib/screens/wizard_steps/magic_review_step.dart
- lib/story_result_screen.dart
- test/unit/services/antihero_crux_test.dart (new)
- docs/ADOLESCENT_CRUX_CHOICE_DESIGN.md
- (docs on main: docs/MANUAL_TASKS.md, TEAM_COORDINATION.md, docs/sessions/2026-07-01-1254-ebd5.md)

## Manual tasks
- Created: MT-316
- Closed:  none (MT-258 + MT-251 annotated in place, both stay open — owner e2e/verify remains)

## Next session: start here
> Merge PR #359 (Crux client, OFF-flag) once CI is green, then MT-316 (flip `cruxChoiceEnabled` + device-verify the crux flow e2e). Or pick up the MT-258 roadmap leftovers (B2/C1/E1/D2) — all Claude-doable off a fresh main.
