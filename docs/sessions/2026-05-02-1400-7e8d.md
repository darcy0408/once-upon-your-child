# SESSION CLOSE — 2026-05-02 14:00 [7e8d] — Branch: main — MT-022 verified, MT-027 fixed, MT-023 diagnosed

## Accomplished
- Committed pre-session hero_creator voice-first work: `3c6613b5` — feat(wizard): voice-first Sprout "Make One Up" panel + mic in name/story-type inputs (was uncommitted from a prior parallel session)
- **MT-022 CLOSED**: Verified production Railway story generation returns full multi-page story. Ran full Sprout wizard via Playwright on grand-light production — "Lily and the Lost Rainbow Colors" returned as Page 1 of 4. OpenRouter key is working. Commit `f7749f06` (docs).
- **MT-023 DIAGNOSED**: Confirmed ocean tile is FAILING in production (still shows old crystal-cave image). Root cause: commit `0ba55f14` IS in `origin/main` but Railway grand-light frontend has not auto-redeployed. Needs manual Railway redeploy. Marked blocked.
- **MT-027 CODE FIX + DEPLOYED**: `3d73b6b5` — replaced `ColoredBox + SafeAssetImage` inside `ClipRRect` in `GenderImageButton` with `Container(decoration: BoxDecoration(color: 0xFF1E1828, image: DecorationImage(AssetImage(path))))` + `foregroundDecoration` for press highlight. Background and transparent PNG now composite in one layer, eliminating CanvasKit checkerboard on Adult Boy portrait. Removed now-unused `SafeAssetImage` import from the file. Production build confirmed at 17:34 UTC (Railway auto-deployed on push).
- **TTS warm-up reorder**: `3d73b6b5` — moved Sprout avatar step prompts (gender, favorite color, photo) to top of `kWarmUpPhrases` list so they cache before a fast-tapping child can outrun them and hit the robotic flutter_tts fallback. Old hair/eye color phrases demoted to lower priority (still present for backwards compat).
- **Animal Friend boy archetype image**: `a51ec945` — found uncommitted change to `assets/images/archetypes/sprout/animal_whisperer_boy.png` (468KB → 2MB, new scene: Black boy with deer, raccoon, rabbit, squirrel, butterflies in sunlit meadow). Committed as companion to the dfad-session girl replacement.
- Cleaned up 90+ QA screenshot PNGs accumulated from MT-022/MT-024/MT-027 Playwright runs.

## Still Pending / Deferred
- **MT-027 browser verify**: code fix deployed but visual confirm blocked by Playwright limitation. Navigate to production in real Chrome, start Adult band wizard (age 18+), check Boy portrait has dark navy background not checkerboard. If pass, also close MT-024 fix-1.
- **MT-023**: Railway grand-light manual redeploy needed. After redeploy, verify ocean tile shows octopus/clownfish scene.
- Playwright Flutter canvas navigation limitations: text fields require click at y=540 (not 563) on the welcome name screen; semantics placeholder not appearing after localStorage clear; dialog buttons require native `page.mouse.click()` at exact coordinates. Noted for future Playwright sessions.

## Blockers
- Playwright can't reliably navigate full Adult band wizard flow due to Flutter text input focus issues on the welcome screen — verified down to coordinate level but Flutter TextInputClient wasn't syncing typed text to its controller. MT-027 verify left to Darcy.
- Railway grand-light does NOT appear to auto-deploy on every push (MT-023 ocean tile commit has been in origin/main for days but didn't deploy). Manual trigger needed.

## Files touched
- `lib/screens/wizard_steps/hero_creator_scene_page.dart` (3c6613b5)
- `lib/screens/wizard_steps/hero_creator_step.dart` (3c6613b5)
- `lib/screens/wizard_steps/hero_creator_story_type_page.dart` (3c6613b5)
- `lib/widgets/hero_creator/hero_input_widgets.dart` (3d73b6b5)
- `lib/widgets/imagine_it_input.dart` (3c6613b5)
- `lib/services/app_tts_service.dart` (3d73b6b5)
- `assets/images/archetypes/sprout/animal_whisperer_boy.png` (a51ec945)
- `docs/MANUAL_TASKS.md` (f7749f06, this close)
- `pubspec.lock`, `linux/`, `macos/`, `windows/` generated plugin files (3c6613b5)

## Manual tasks
- Created: none
- Closed:  MT-022

## Next session: start here
> MT-027 and MT-024 are both unblocked — open grand-light production in Chrome, navigate to Adult band wizard (age 18+, enter any name, proceed to gender step), confirm Boy portrait shows dark navy background. If it does, close both. Then trigger Railway grand-light redeploy (or ask Darcy to) and check the ocean tile for MT-023.
