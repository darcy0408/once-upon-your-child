# SESSION CLOSE — 2026-05-27 07:14 [d003] — Branch: reliability-hardening — Audit 05 end-to-end: harness built, run, RCAs, fixes, rubric-mismatch finding

Long multi-day session — covered the entire AI/ML quality audit lifecycle. Pick up next time at MT-183 / MT-184 / MT-187.

## Accomplished

- **Audit 05 harness built and committed.** `backend/eval/` directory: `prompt_registry.py` (12 templates content-hashed), `rubrics.py` (5 rubrics per spec), `test_set.py` (30 fixed-seed inputs across common/edge/adversarial), `harness.py` (resume-safe runner with `--filter`, `--calibration`, `--dry-run`, budget cap), `judge.py` (two-judge dispatch + `--ping`), `providers.py` (GitHubModelsClient + GeminiClient), `snapshot.py` (drift detector), `generation.py` (real prompt assembly + Gemini call), `report.py` (per-cell distributions, Critical detection, agreement). Earlier commit string: `15c4116a` through `293a278e`.
- **Full 1020-cell generation pass on Gemini Flash-Lite — $0.60 total, 0 errors, 6 refusals (0.6%).** Run id `20260522-153126-a76436`. Notable adversarial blocks: A02 lock-picking, A09 banana 500×, A05/A06/A07 trauma cells. Two unexplained blocks: `bedtime adult C04` and `standard 15-18 C06` — worth a closer look someday.
- **Both judges wired and ping-verified.** `gpt-4.1` via GitHub Models (free for Student tier) + `gemini-2.5-flash` via existing GEMINI_API_KEY. Gemini judge required `thinking_budget=0` to stop the JSON output being eaten by reasoning tokens (commit `15c4116a`).
- **Full Gemini judge pass: 1020/1020 cells scored, free.** GitHub Models hit the Student-tier daily limit at 28 cells and partial-completed; the rest is a multi-day drip awaiting resume (MT-186). Inter-judge agreement on the 28 overlapping cells: 77.1% — below the 80% calibration bar but low confidence at N=28.
- **First Audit 05 report shipped: `audit-reports/05-ai-quality-20260522.md` (commit `b7d47abf`).** Six Critical cells identified, all on Sprout (3-4) or the LTR Seussian path: `superhero|3-4` (63.3% fail), `ltr_seussian|13-15/15-18/adult/3-4`, `rhyme_time|3-4`.
- **RCA + fix cycle for `superhero|3-4`: PASS validated.** Identified three concrete bugs in `prompt_service._build_superhero_prompt` (commit `d8801722` RCA writeup): grammatical template-fill in beat 2, parenthetical metadata leak in beat 4, single-page output format. Applied fixes across commits `0fbc4dfd` (Bugs 1+2 + `--filter` flag in harness), `c9a903f3` (Bug 3 — parallel session), `59b65572` (name-density tweak + validation writeup). **Result: age_fail_pct 63.3% → 0.0%, age_band_fit 2.30 → 4.60, narrative 3.03 → 4.33, mode adherence preserved at 100%.** Cell exited Critical; now scores better than its `superhero|5-7` sibling. Cost: $0.02, 5 min wall.
- **Methodology finding shipped:** Bugs 1+2 (grammar, metadata leak) alone did NOT move scores (63.3% → 70.0%). **Bug 3 (single-page format) was load-bearing.** The judge weights structural format much higher than sentence-level grammar for age-fit. Calibration lesson for future RCAs.
- **LTR-13+ branch added (commit `d7a434c7`):** routed ages 13+ away from limericks (correctly identified as age-inappropriate) to a new phonics-friendly prose branch in `story_service._build_learning_to_read_prompt`. Routing now: ≤5 CVC / 6 Dr. Seuss / 7-12 limericks / 13+ prose (no rhyme).
- **Audit-meta finding from the LTR-13+ re-test:** the d7a434c7 prose outputs are objectively better for the use case (Hi-Lo style decodable text for teen/adult learners), but the judge's generic `age_band_fit` rubric rates them lower than the limericks because the rubric asks "pitch-perfect for an N-year-old" — and Hi-Lo content for LTR is intentionally simpler than the reader's age. **The rubric and the mode are misaligned for LTR @ 13+**; needs a per-mode rubric override (option A) or a UI restriction (option C). Tracked as MT-184.

## Still Pending / Deferred

- **MT-187** — F-01 versioning (persist prompt_template_id + revision_hash on every `Story` row). Still the right structural fix. Snapshot drift-detection works as an interim mitigation, but the structural fix is ~2h and lets any future regression be a one-query investigation.
- **MT-186** — Resume the GitHub Models judge when the daily quota window allows. Multi-day drip via `--resume`; goal is N≥200 overlapping samples for a confident inter-judge agreement.
- **MT-185** — RCA + fix for `rhyme_time|3-4` (60% fail). Likely same pattern as the original Superhero Sprout finding: an older-skewed prompt applied to 3-4.
- **MT-184** — Decide LTR-13+ path: per-mode rubric override, refine Hi-Lo prose, or restrict UI. Owner call.
- **MT-183** — RCA + fix for `ltr_seussian|3-4` (30% fail after partial re-test). The CVC branch is over-constrained — CVC vocab + page-pair AABB rhyme + DECODABILITY CHECK + 5-8 word sentences stacked is too much for the model. Worst sample got mode_adherence=0.
- Background generation/judge artifacts under `backend/eval/results/` are gitignored — local-only. Three runs persist there: `20260522-153126-a76436` (full 1020), `20260524-213705-cc568f` (Sprout re-test), `20260524-214607-997196` (LTR re-test). Free to delete if disk pressure.

## Blockers

- None for the audit pipeline itself. GitHub Models rate limit gates further inter-judge data but `--resume` handles it cleanly.

## Files touched (this session's commits)

- `backend/eval/__init__.py`, `README.md`, `prompt_registry.py`, `rubrics.py`, `test_set.py`, `harness.py`, `judge.py`, `providers.py`, `snapshot.py`, `generation.py`, `report.py`, `requirements.txt`, `results/.gitignore`
- `backend/services/prompt_service.py` (Superhero Sprout bugs 1+2 + name-density)
- `backend/services/story_service.py` (LTR 13+ prose branch)
- `audit-reports/05-ai-quality-20260521-interim.md`
- `audit-reports/05-ai-quality-20260522.md`
- `audit-reports/05-ai-quality-20260524-superhero-sprout-rca.md`
- `audit-reports/05-ai-quality-20260524-superhero-sprout-fix-validated.md`
- `docs/sessions/2026-05-21-1039-3de3.md` (previous close in this conversation)
- `TEAM_COORDINATION.md` (one row per close)

## Manual tasks

- Created: MT-183, MT-184, MT-185, MT-186, MT-187
- Closed:  MT-182

## Next session: start here

> Pick the lightest-effort, highest-value next step: either **MT-183** (`ltr_seussian|3-4` RCA — same pattern as the Superhero Sprout cycle, ~30 min, free) or **MT-185** (`rhyme_time|3-4` — similar). For a one-decision item with cross-team impact, **MT-184** (LTR-13+ path) needs owner input but unblocks the rest of the LTR Critical work. MT-187 (F-01 versioning) is the structural fix that makes every future audit cheaper and is independent of the cell work.
