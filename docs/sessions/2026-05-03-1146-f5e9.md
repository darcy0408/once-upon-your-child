# SESSION CLOSE — 2026-05-03 11:46 [f5e9] — Branch: main — TTS_DISABLED toggle + ElevenLabs quota explanation

## Accomplished
- Diagnosed ElevenLabs quota exhaustion from Flask logs (1 credit remaining, monthly limit of 160,351 chars hit; resets in 2 days)
- Added `TTS_DISABLED` env-var toggle to `backend/routes/tts_routes.py` — short-circuits `/tts/synthesize` before any API call, returning same 503 Flutter already handles with on-device fallback (commit `ccc2bf2b`)
- Added `TTS_DISABLED=true` to `backend/.env` — TTS is now disabled locally
- Confirmed `backend/.env` is in `.gitignore` and not tracked by git (keys are safe)

## Still Pending / Deferred
- None from this session

## Blockers
- ElevenLabs quota resets in ~2 days (2026-05-05). Set `TTS_DISABLED=false` (or remove the line) in `backend/.env` and restart Flask to re-enable.

## Files touched
- `backend/routes/tts_routes.py`

## Manual tasks
- Created: none
- Closed:  none

## Next session: start here
> Re-enable TTS by setting `TTS_DISABLED=false` in `backend/.env` once the ElevenLabs quota resets (~2026-05-05). All open MT tasks (MT-016 TTS web verify, MT-021 avatar flow, MT-031 Railway deploy, etc.) are unchanged.
