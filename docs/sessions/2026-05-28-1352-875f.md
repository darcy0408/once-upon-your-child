# SESSION CLOSE — 2026-05-28 13:52 [875f] — Branch: reliability-hardening — MT-187 F-01 prompt-template versioning shipped + prod Postgres migrated

## Accomplished
- **MT-187 closed** — F-01 prompt-template versioning shipped as commit `3ad77e18` (5 files, +329 lines; pushed to origin as part of `reliability-hardening` at `6aeaf4e4`). Two nullable columns on `Story`: `prompt_template_id` (VARCHAR 40) + `prompt_revision_hash` (VARCHAR 16). The hash is the sha256[:16] of the builder function's source computed at module-import time via `inspect.getsource()` — any code change to a prompt builder produces a new hash without anyone remembering `snapshot --refresh`. The static `content_hash` values in `backend/eval/prompt_registry.py` remain the offline audit-harness baseline; the live values are what prod writes.
- New module `backend/services/prompt_versioning.py` exposes `resolve(mode, age) -> (template_id, revision_hash)`. Mode is one of `standard`/`bedtime`/`ltr`/`rhyme_time`/`superhero`; age disambiguates Superhero (Sprout vs Explorer) and LTR (limerick vs everything else). Unknown modes fall back to `T1_STANDARD` so persistence never raises.
- Wired into `backend/tasks/story_tasks.py` right after the prompt-build if/elif chain finalises the prompt; values flow into the `Story()` insert at line ~1620. The safety-classifier fallback path was the subtle bit — it always rebuilds via `engine.generate_enhanced_prompt`, so the wiring re-tags the row as `T1_STANDARD` when the fallback fires (otherwise row metadata would lie about the persisted body).
- Idempotent migration script `backend/migrations/add_prompt_versioning_to_story.py` follows the established `add_content_and_task_id_to_story.py` pattern (standalone SQLAlchemy, `DATABASE_PUBLIC_URL` preferred, columns checked before ALTER). Verified twice on a temp SQLite (adds → noop) before prod.
- **Prod Postgres migrated** 2026-05-27 via `railway run --service Postgres python backend/migrations/add_prompt_versioning_to_story.py` against `radiant-tranquility/production` (`yamanote.proxy.rlwy.net:28615/railway`). Schema inspection confirmed `prompt_template_id VARCHAR(40)` + `prompt_revision_hash VARCHAR(16)` present, story table now 13 cols. Re-run is no-op.
- 19 new tests in `backend/tests/test_prompt_versioning.py` cover all (mode, age) cells, garbage-input safety (None / non-int age, unknown mode), hash stability across calls, and that distinct templates produce distinct hashes. Full backend suite **1054 pass** (suite has grown from the PROJECT_STATUS-recorded 294).
- Survived **two parallel-Claude races** mid-session. (1) HEAD on `reliability-hardening` advanced from `bb339c21` to `b7ba808a` (backup-workflow fix) while I was editing — my unstaged edits to `backend/tasks/story_tasks.py` were silently dropped; re-applied by anchor-search. (2) After Darcy told me to "move to another branch to work uninterrupted", I created `mt-187-prompt-versioning` off `b7ba808a` and committed there; a parallel session then committed a docs(session) close `6aeaf4e4` onto my isolation branch unbidden. Fast-forward merge of `mt-187-prompt-versioning` back into `reliability-hardening` after the parallel session moved on left history clean at `6aeaf4e4` → `3ad77e18` → `b7ba808a`.

## Still Pending / Deferred
- **Backend deploy not yet run.** The /close-session command interrupted the deploy step. Until prod redeploys the `story-weaver-app` Railway service from current HEAD, the new columns sit empty — new Story rows will write NULL for both fields. Safe (columns nullable, no code path errors), but zero attribution data flows until deploy. Run `railway up --service story-weaver-app` or push triggers auto-deploy if the service is git-integrated to `reliability-hardening` or `main`.
- **Stale local branch `mt-187-prompt-versioning`** points at the same `6aeaf4e4` as `reliability-hardening`. Pure noise — safe to `git branch -D mt-187-prompt-versioning` whenever convenient.
- **LTR sub-branch attribution is coarse.** Four sub-branches (CVC ≤5 / Seuss age 6 / limericks 7-12 / prose 13+) collapse onto 2 template_ids (T2 limericks + T3 catch-all non-limerick). Documented in `backend/services/prompt_versioning.py` module docstring. Finer attribution requires splitting `_build_learning_to_read_prompt` into separate functions — defer until the LTR Audit 05 work (MT-183 / MT-184 / MT-185) lands and the function shape settles.
- **Audit 05 backlog unchanged:** MT-183 (`ltr_seussian|3-4` RCA), MT-184 (LTR-13+ owner call), MT-185 (`rhyme_time|3-4` RCA), MT-186 (resume GitHub Models judge) all still open.

## Blockers
- None for MT-187 itself. Backend deploy was queued but the user invoked /close-session before it ran — not a blocker, just a sequencing choice.

## Files touched
- `backend/migrations/add_prompt_versioning_to_story.py` (new)
- `backend/models/story.py`
- `backend/services/prompt_versioning.py` (new)
- `backend/tasks/story_tasks.py`
- `backend/tests/test_prompt_versioning.py` (new)

## Manual tasks
- Created: none
- Closed:  MT-187

## Next session: start here
> First action: deploy the `story-weaver-app` Railway service so MT-187 columns start populating — until then prod is migrated but inert. Then: pick from the Audit 05 Critical backlog — **MT-185** (`rhyme_time|3-4` RCA) is the highest-leverage one since it matches the Superhero Sprout pattern that's already been proven to fix in ~30 min; **MT-184** (LTR-13+) is owner-decision-blocked. If a parallel session has already deployed, run `railway logs --service story-weaver-app | grep prompt_template_id` to confirm new Story rows carry the attribution tags.
