# SESSION CLOSE — 2026-05-02 20:12 [c1ff] — Branch: main — MT-027 root-cause investigation + MT-024/MT-027 closed

## Accomplished
- **Investigated MT-027** (Adult band Boy portrait checkerboard) end-to-end in a local release web build at 1280×720.
- **Identified root cause**: `gender_adult_boy.png` and `gender_adult_girl.png` are **fully opaque** space-silhouette PNGs (no transparency). The checkerboard seen in MT-024 was caused by the browser's service worker serving stale cached face-portrait assets from a previous build. Once SW + caches cleared, no checkerboard.
- **Code hardened** in `GenderImageButton` (`lib/widgets/hero_creator/hero_input_widgets.dart`): removed `ClipRRect`, switched to `Container(decoration: BoxDecoration(color: 0xFF1E1828, borderRadius: 17, image: DecorationImage(AssetImage, BoxFit.contain)))` with `foregroundDecoration` for press highlight. Committed as `3d73b6b5` (earlier in this session's compacted context) and finalized in `e563b3f8`.
- **MT-024 closed**: all 5 visual audit fixes from session 599b confirmed passing in local release build.
- **MT-027 closed**: checkerboard non-issue with current opaque assets; code hardened as defensive measure.

## Still Pending / Deferred
- None from this session.

## Blockers
- Playwright browser persists a service worker profile across navigates — `caches.delete()` + SW unregister via `evaluate()` did not fully flush the old face-portrait assets. Had to use `location.reload(true)` + direct inspection of `build/web/assets/` to confirm the actual built assets are opaque silhouettes. Future verification: always clear SW before testing asset changes.

## Files touched
- `lib/widgets/hero_creator/hero_input_widgets.dart` (committed `3d73b6b5`, later touched by `e563b3f8`)
- `docs/MANUAL_TASKS.md` (MT-024 + MT-027 closed, committed `7d51fe08`)

## Manual tasks
- Created: none
- Closed:  MT-024, MT-027

## Next session: start here
> Open items worth picking up next: MT-026 (Big Feelings Scrollbar console error — quick browser verify), MT-028 (Sprout Animal Friend archetype card image verify), MT-029 (Sprout big-feelings square cards + auto-advance). The uncommitted parallel-session changes (`backend/services/story_service.py`, three test files) may need a commit if not already handled.
