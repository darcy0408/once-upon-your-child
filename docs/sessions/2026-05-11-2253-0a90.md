# SESSION CLOSE — 2026-05-11 22:53 [0a90] — Branch: main — Sprout reader fixes: page cap, JSON salvage, image-text scrub + UX debugging

## Accomplished

- Long debugging arc with Darcy over multiple story-generation attempts as a 3-year-old. Found and fixed three real bugs, all on local backend only.
- **`9628de7d`** `fix(image-gen): strip sound words + forbid readable text in illustrations`. Imagen was rendering ALL-CAPS onomatopoeia ("TINKLE TINKLE", "*ZZUND*") and asterisk-wrapped emphasis from page text as visible *gibberish* text baked into the illustration itself — observed pages 3 and 5 of a June story showing "Willihrs litte leokied vert still" and "June with Playbinbe" as fake writing inside the image. Sanitized `scene_description` before substituting into the prompt (lowercase 3+ char ALL-CAPS, strip `*…*`); added an ABSOLUTE RULE at the top of the prompt forbidding any readable characters/captions/signs/banners in the output.
- **`e753ef40`** `fix(story-service): Sprout page cap 8-12 + regex page-salvage for malformed JSON`. Two distinct fixes that both surfaced on the same Darcy-debugging-as-Sprout reproduce: (a) the regular adventure-story Sprout prompt had `HARD MIN 5` with no max, so models padded to 15+ pages → unreadable for a 3yo's attention span; now `HARD MIN 8 / HARD MAX 12` (traditional picture-book pacing). (b) When the model returned malformed JSON (bad brackets, stray `image_prompt` field), `_safe_extract_title_and_gem` was falling through to "dump the entire raw response as a single page" — Darcy saw a 3-page story where page 2 was literal JSON text. Added a regex salvage layer: pattern-extract `"text": "..."` matches, json-decode each individually for escape/unicode restoration, recover a real page list + title; only falls to raw-text dump when <2 pages can be salvaged.
- Confirmed via Darcy's F12 console that the prefetcher's BYOK debug-print added in 5b5b is working: `PerPagePrefetcher: BYOK key ABSENT; allowServerKey=true; pages=14`. Confirmed Darcy's BYOK wizard never completed successfully because backend was unreachable during validation attempts (validation endpoint returned `ClientException: Failed to fetch`). Once backend was up Darcy could redo the wizard.
- Walked Darcy through: (1) the dual-Flutter-instance / no-backend mixup, (2) the difference between local `localhost:NNNNN` and the production `grand-light-production-68d9.up.railway.app` URL she was accidentally testing on, (3) `debugPrint` on Flutter web going to the browser DevTools console rather than the PowerShell terminal (which is why she initially saw no `PerPagePrefetcher:` line).
- Validated the pagination "trust backend page splits" fix from 5b5b — Darcy's first non-LTR Sprout story came back with 15 pages all properly displayed (no client-side collapse). The frontend pagination logic is correctly preserving backend's page list.
- Confirmed the circuit-breaker from 5b5b is firing as designed: first Darcy test showed only 1 illustration on a 15-page story because 2 consecutive non-quota failures aborted the rest (intended). On a later attempt all 15 pages got illustrations because the prefetcher had time to retry between user page-turns and the counter reset on each success.

## Still Pending / Deferred

- **`backend/tasks/story_tasks.py:625-720`** — Sprout non-LTR validation is the next fix needed; the 8-12 page cap shipped today is *soft prompt text* and models still ignore it ~30% of the time (Darcy's last test came back at 3 pages despite the new directive). MT-098 documents the recipe: mirror the LTR validate-retry-then-postprocess pattern. Effort ~1-2 hours.
- Story reader doesn't "feel like a book" — Darcy raised this twice today (small card on purple background, page-flip animation feels detached from the page). MT-099 backlogged with proposed direction (full-bleed paper page, visible edges, 2-page spread on wide screens). Mock layouts first.
- Production has none of today's fixes. Railway is still serving the 15-page padding, JSON-dump-as-page, and gibberish-in-image behaviors. Push when verified locally — both fixes today are pure backend Python, so the Railway redeploy is straightforward.
- Production `/generate-illustrations` is returning 429 (Too Many Requests) — server-key Imagen quota exhausted on the Railway-hosted backend. Separate from today's fixes; the same warning was logged 2026-05-10 on grand-light. Either bump the rate limit / monthly cap, or push users toward BYOK (which requires the BYOK wizard to be reachable, which it now is locally).
- TTS on local backend returning 503 SERVICE UNAVAILABLE — ElevenLabs likely not configured in `backend/.env`. Not blocking today's work; Darcy is using on-device fallback. Possibly relevant to MT-091 (Google Cloud TTS fallback layer).

## Blockers

- None for [0a90]. All identified bugs are fixed locally. The remaining work is Darcy actions (verify locally, push to deploy, optionally tackle MT-098 next) and design decisions (when to land MT-099).

## Files touched

- `backend/gemini_image_generator.py` (9628de7d)
- `backend/services/story_service.py` (e753ef40)

## Manual tasks

- Created: MT-097, MT-098, MT-099
- Closed:  none

## Next session: start here

> Restart local backend, generate 3 fresh Sprout stories as age 3, walk MT-097 verification. If pages keep coming back outside the 8-12 range, prioritize MT-098 (Sprout non-LTR validation + retry in story_tasks.py — the LTR pattern is ready to copy). Once both verify locally, push to deploy and re-verify on production (which also clears the 429 illustration quota concern when paired with completing the BYOK wizard).
