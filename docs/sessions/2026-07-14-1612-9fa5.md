# SESSION CLOSE — 2026-07-14 16:12 [9fa5] — Branch: main — "Once upon a time" openers shipped (PR #437) + competitive product audit + Family Voice Narration spec (PR #438 open)

## Done

**1. Age-banded story openers — PR #437 MERGED (`10857ab5`).**
- `backend/services/story_service.py`: new `_get_opening_rule(age, seed)` + `_EXPLORER_OPENER_ROTATION`. Sprout (≤5) always opens with the exact phrase "Once upon a time" (ritual overrides the vary-every-time rule at this age); Explorer (6–8) gets a server-picked classic opener with "Once upon a time" holding half the rotation weight (seed semantics mirror `_pick_situation`); 9+ keeps the prior FRESH OPENING anti-sameness rule verbatim.
- Landed on the LIVE standard path (`AdvancedStoryEngine.generate_enhanced_prompt`) — note for future prompt work: `PromptService.build_story_prompt` is superhero-mode-only in prod; an edit there would have been a silent no-op.
- Verification: 1,833 backend tests green in the worktree (full suite), 5 new tests pin band behavior/weighting/seed-determinism, black+flake8 clean, PR CI 8/8 real checks green, and LIVE gpt-5-mini generations via OpenRouter confirmed ages 4 and 7 open with the mandated phrase while ages 10/13/16/21 kept varied openings.

**2. Competitive product audit — `docs/COMPETITIVE_PRODUCT_AUDIT_2026-07-14.md` (commit `f289bbcb`, pushed).**
- Part 1: six live per-band story samples generated (gpt-5-mini via OpenRouter) and graded — all good-to-very-strong; the prompt machinery (emotional spine, real-life echo, named-cost choices) demonstrably shapes output. **Finding A-1 (MEDIUM):** the default sensory palette ("sweet smells") produced lemon/sugar/bakery smells in ALL six samples and drove the entire premise at age 16 — agent-sized fix, rotate per band.
- Part 2: competitor matrix from live listings (Sonnet research agent): 7 verified features nobody in the 9-app set has (feelings input, saga memory, ages past 12, coloring export, LTR/Rhyme, documented moderation); Slumberkins (SEL segment leader) names no credentialed advisor and maps to Washington State standards, not CASEL — a real CASEL map + named clinician leapfrogs the set. Sample stories saved in session scratchpad (ephemeral).

**3. Family Voice Narration spec — PR #438 OPEN (`docs/FEATURE_SPEC_FAMILY_VOICE_NARRATION.md`, branch `session/voicespec`, worktree `C:\dev\sw-voicespec`).**
- Owner-championed feature: stories narrated in a cloned parent/grandparent voice (faraway grandparents, deployed military parents, memorial recordings). Design + compliance only, no code, explicitly off the launch critical path.
- Hard rules fixed in the spec: never clone a child's voice; the voice owner consents personally (recorded statement + own-email verification); BIPA-grade standalone consent as the all-states floor; deletion cascade extends to vendor-side voice models; memorial path parked behind heir attestation + legal review (Amazon Alexa 2022 lesson: the backlash is positioning, not tech).
- Vendor research (dedicated Sonnet agent, sources in PR body): Azure Personal Voice preferred BUT its Limited Access page suggests only Microsoft-account-team customers qualify — **solo-dev eligibility is the #1 open risk, confirm in P0**; Resemble AI is the verified fallback; pricing figures marked UNVERIFIED in the spec. Includes P0.5 "Voice Dedications" no-cloning demand test and the grandparent gift-subscription loop (rides the existing gift entitlement channel, `routes/gift_routes.py`).

**4. Housekeeping.** `session/openers` branch/worktree cleaned up post-merge; `chore/ci-artifact-retention` found already deleted remote-side; yesterday's untracked `docs/COPPA_INTERNAL_REVIEW_2026-07-13.md` committed (`fb7ffc7e`); memory file `family_voice_narration_direction` added to auto-memory.

## Decisions
- Openers are banded, not global — the existing FRESH OPENING rule actively prohibited ritual openers, so it now applies only 9+; ritual predictability is the feature at ≤5 and reads babyish at 9+.
- Standard wizard path only for chunk 1; superhero/bedtime/learning-to-read/rhyme builders are separate follow-up chunks.
- Voice cloning is spec-first: consent/vendor gating makes an immediate build unsafe; owner approved "Spec it" over "start building".
- Docs-only commits went straight to `main` by path (shared-checkout protocol); feature work went through worktree + PR.

## Next (ordered)
1. **A-1 sensory-palette rotation** — agent-sized, mirror the opener/`_pick_situation` rotation pattern in `generate_enhanced_prompt`; batch the A-2 cosmetics (title casing) with it.
2. **Extend openers to the Sprout/Explorer superhero builders + bedtime path** once #437 looks good in prod.
3. **PR #438**: owner review + §9 decisions; P0 = confirm Azure Personal Voice Limited Access eligibility for a solo-dev account.
4. Standing owner items: MT-350 store setup (iOS critical path), MT-338 (Celery solo-pool/Redis SPOF), COPPA memo F-2 (emotional-data wording decision) + F-3 (OpenAI DPA/ZDR execution).

## Blocked on user
- PR #438 merge word + the five §9 owner decisions (voice count, gift pricing, memorial stance, timing, P0.5 yes/no).
- F-2 product decision and F-3 DPA/ZDR (self-serve in the OpenAI console).
- `session/coppa-f4-f7` (worktree `C:\dev\sw-coppa-f4`) still holds 2 commits with **no remote copy** (F-4 cache fix + F-7 policy line) — needs a push + PR or an explicit decision to abandon; until then it's one disk failure from gone.

## Risks / unverified
- Azure Personal Voice pricing and solo-dev access are UNVERIFIED (flagged inline in the spec) — do not scope the build until P0 confirms them.
- Explorer opener weighting is unit-test-pinned but only one live draw was observed; audit grading is one sample per band.
- Netlify deploy-preview checks fail on every PR (standing orphaned-frontend noise) — ignore them, but don't let them camouflage a real frontend failure.
