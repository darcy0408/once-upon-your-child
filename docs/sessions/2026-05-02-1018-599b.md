# SESSION CLOSE — 2026-05-02 10:18 [599b] — Branch: main — Age-band visual audit: fix 5 UI bugs identified in Playwright screenshots

## Accomplished

- **Portrait checkerboard transparency (Creator/Adult)** — Added `ColoredBox(0xFF1E1828)` inside `ClipRRect` in `GenderImageButton` so the dark-navy background paints reliably behind transparent PNG pixels in Flutter web. Previously the clip was eating the parent container's background decoration and the checkerboard showed through. Commit `09d12533`.
- **Accordion not expanding on step navigation** — Added `ExpansibleController` per section to the mature-band creative brief. `_jumpToSubStep` now collapses the three non-target sections and expands the target before scrolling — proper accordion focus rather than a scroll hint that left the tile collapsed. Commit `09d12533`.
- **Sprout GO button clipped at 720p** — Restructured `_buildSproutLaunchScreen` from a single centred `Column` into `Expanded` + `SingleChildScrollView` for content above, with the GO button pinned via `Padding` at the bottom of the outer `Column`. Button always reachable regardless of viewport or recap card height. Commit `09d12533`.
- **Core Archetype below fold on creative brief step 1** — Reduced gender picker spacers from 24→12 px each and portrait card dimensions from 110×140→96×120. Recovered ~44 px so the archetype label and top row of cards land in the initial viewport. Commit `09d12533`.
- **Explorer "Pick something special!" below fold** — Reduced post-orbs `SizedBox` from `band.space(28)` to `band.space(12)`. Brings the special-section header and first card row into view without scrolling at 720p. Commit `09d12533`.

## Still Pending / Deferred

- Issues 2 and 5 are spacing-only fixes; no pixel-perfect confirmation was done (no new screenshots generated). Browser-verify MT-024 before shipping these to production.
- `hero_creator_scene_page.dart` had 303 lines of parallel-session changes in the working tree that were left unstaged — another session owns those.
- `pubspec.lock` + all platform plugin registrant files had pre-existing unstaged changes not touched by this session.

## Blockers

- None. All five fixes applied and analyzed clean.

## Files touched

- `lib/widgets/hero_creator/hero_input_widgets.dart`
- `lib/screens/wizard_steps/hero_creator_creative_brief.dart`
- `lib/screens/wizard_steps/hero_creator_step.dart` (ExpansibleController — already reflected in HEAD from parallel session, confirmed via grep)
- `lib/screens/wizard_steps/magic_review_step.dart`
- `lib/screens/wizard_steps/hero_creator_story_type_page.dart`

## Manual tasks

- Created: MT-024
- Closed: none

## Next session: start here

> Run MT-024 browser verification — build release web, check all five fixes at 1280×720. Then consider tackling the two remaining audit items: MT-017 (TTS double-fire audit for non-Sprout bands) and the open Explorer/Adventurer layout density polish.
