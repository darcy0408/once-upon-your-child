# SESSION CLOSE — 2026-05-05 00:05 [4af1] — Branch: main — Multi-agent Sprout sweep: TTS storm fix, A7 vocab rule, A8 audit, A2/A3/A4 investigations

## Accomplished

- **TTS warm-up 503 storm — fixed and verified live.** Diagnosed that the MT-013 retry cap only catches `TtsRateLimitException` (429s); 503s slip silently through `TtsApiService.synthesize` returning `null`, which `_prewarm` treats as "successful empty." Result: ~95 `/tts/synthesize` POSTs in <1s on every cold load. Commit `6eaffdd1` adds `_maxPrewarmConsecutiveNulls = 3` circuit breaker outside the inner retry loop. Verified live against Railway: cold load now fires exactly 3 calls and stops, vs. ~95 before. The MT-013 retry/backoff path and `_prewarming` dedup flag are untouched.
- **Sprout vocab rule strengthened.** Investigation found rule #5 in `young_delight_rules` already names "dragon breath" as a canonical example — the model was ignoring it, not lacking it. Commit `6bccd443` rewrites the rule with "HARD CHECK — re-read every page before finalizing", expanded canonical-example list (cape/cousin/archery/lantern/festival/etc.), and "FAIL — rewrite the page" framing.
- **Sprout UX audit produced.** `docs/SPROUT_UX_AUDIT_2026-05-04.md` (commit `b543ae45`, 228 lines) captures 28 findings across 8 dimensions: tap targets, text sizes, visual chrome density, word choices, page sizes/scrolling, affordance clarity, forms/text input, error states. 5 high-impact, 14 medium, 8 low, 1 positive (S-023). Triage tracked as **MT-045**.
- **A2/A3/A4 layout bugs investigated.** Read-only diagnoses with file:line refs: A2 (narrow viewport text invisible) is vertical space starvation from `(40,50,40,40)` padding in `storybook_page.dart` + Wrap-balloon footer at narrow widths; A3 (page-turn lands at bottom) is `SingleChildScrollView` element re-use in `_buildStoryPage` — Flutter re-uses Element across page turns, prior offset persists; A4 (Pick-a-Path label on every page) is hard-coded fallback array in `StorybookProgressIndicator._defaultLabel`. **All three were fixed by parallel sessions** (`1841cd3a` for A2 + A3, `dd67873f` for A4) before I could apply them — lucky timing.
- **A5 Sprout copy.** Commit `26e4c93b` — single-line fallback in `hero_creator_scene_page.dart:501` to show "Big Feelings" instead of "Life Quest" for Sprout. Most call sites were already band-conditional from earlier work; only this fallback was missed.
- **MT-035 debugPrint instrumentation.** Commit `469f0b18` adds 4 `[MT-035]`-prefixed `debugPrint` lines at `lib/story_result_screen.dart:1320-1323` to surface character payload at save time. The instrumentation revealed the broader bug: `_saveStory()` was never being called on Sprout because `widget.characterAge` defaults to 8 (not the user's actual age). Fix landed in parallel session's working tree. Removal tracked as **MT-048**.
- **`.gitignore` extended.** Commit `ee7ec9e7` covers 53 untracked QA-screenshot patterns (`/mt-*.png`, `/step*.png`, `/avatar-*.png`, `/consent-*.png`, etc., all root-anchored to avoid masking `assets/images/...`). All 57 root-level PNGs now properly ignored.
- **`PROJECT_STATUS.md` refreshed.** Commit `90190e79` brings the doc current to 2026-05-04 with Phase 5 (May 2026) — Sprout polish, voice/TTS hardening, BYOK reliability, child-UX audit. Adds OpenRouter fallback + on-device TTS fallback to architecture; replaces resolved BUG-001/002/003 in Known Issues.
- **Worktree branch audit.** All 6 `worktree-agent-*` branches have unmerged commits AND mounted worktrees — none safe to delete. Triage tracked as **MT-049**.
- **TTS storm verified end-to-end.** Browser cold-load on Railway-deployed `grand-light-production-68d9.up.railway.app` against backend logs confirmed exactly 3 503s in ~50ms followed by silence — matches circuit-breaker design. (User reported TTS is intentionally disabled until ElevenLabs credits restored, so 503s are expected; the bug was 95 calls, not 3.)

## Still Pending / Deferred

- **A1 Sprout save fix** — root cause confirmed: auto-save gate used raw `<= 5` against `WizardData.characterAge` default of 8; heart-icon `_toggleFavorite` early-returned on `widget.storyId == null` (wizard stories never have one). Fix is **in working tree** (parallel session's edits to `lib/main_story.dart`, `lib/screens/wizard_steps/hero_creator_step.dart`, `lib/story_result_screen.dart`, `lib/data/scenario_data.dart`, `lib/screens/bedtime_wizard_screen.dart`). Not yet committed/pushed by parallel session at session close.
- **A2/A3 verification** — `1841cd3a` shipped responsive padding (A2) and `ValueKey` per-page scroll (A3). Verification tracked as MT-043 (A2) and MT-044 (A3).
- **A6 Picture-Book label** — addressed by `1841cd3a` via "hide pill on Sprout" rather than the "Read Along when illustrations gated" approach I'd planned. Verification rolls into MT-043.
- **MT-045** Sprout UX audit triage (28 findings) — read the doc, pick top-7 for next session.
- **MT-046** Vocab age gate `<= 7` is too wide — Spark (6-7) gets toddler-strict rule.
- **MT-047** LTR prompt path lacks "explain unfamiliar concepts" rule — strengthened rule #5 doesn't reach LTR mode.
- **MT-048** Remove MT-035 debugPrint after deployed save fix verified.
- **MT-049** Triage 6 stale `worktree-agent-*` branches.

## Blockers

- **Parallel-session conflict avoidance.** Two parallel sessions (b535 closed ebc0, c5c2 etc.) were committing to `main` simultaneously throughout this session. Killed my A1+A6 fix agent mid-work after parallel session covered the same ground (`1841cd3a`). My A2/A3 implementation agents were never spawned because parallel sessions landed those fixes first. Net effect: my session ran investigations + audit + a few non-overlapping commits, while parallel sessions ran code fixes — efficient division by accident.
- **Working tree dirty at close.** 4 files (`lib/data/scenario_data.dart`, `lib/main_story.dart`, `lib/screens/bedtime_wizard_screen.dart`, `lib/screens/wizard_steps/hero_creator_step.dart`) carry parallel session's A1 fix. Not safe for me to commit. **Push skipped at close.**

## Files touched

- `lib/services/app_tts_service.dart` — TTS circuit breaker
- `.gitignore` — QA screenshot patterns
- `docs/PROJECT_STATUS.md` — refresh to 2026-05-04
- `lib/screens/wizard_steps/hero_creator_scene_page.dart` — A5 fallback
- `backend/services/story_service.py` — A7 vocab rule strengthening
- `lib/story_result_screen.dart` — MT-035 debugPrint
- `docs/SPROUT_UX_AUDIT_2026-05-04.md` — A8 audit (new file, 228 lines)

## Manual tasks

- Created: MT-045, MT-046, MT-047, MT-048, MT-049
- Closed:  none

## Next session: start here

> Commit the unstaged A1 fix from the parallel session (4 modified files), push, deploy, then walk a Sprout user end-to-end on production to verify save now fires. That action closes MT-034, MT-035, MT-037, MT-038, MT-043, MT-044 in one walkthrough. Then triage MT-045 (Sprout UX audit) and pick 2-3 items for the next polish pass.
