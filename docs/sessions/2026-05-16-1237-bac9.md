# SESSION CLOSE — 2026-05-16 12:37 [bac9] — Branch: main — Triaged + committed a parallel-session Sprout-reliability sweep

## Accomplished

- **Committed a 7-file Sprout-reliability sweep** left uncommitted by a parallel session (commit `af0e39d9`). The changes were coherent and complete (tests included), not contamination:
  - `story_routes.py` — Sprout (age <=5) now falls back to Flux Schnell when Gemini-via-OpenRouter yields nothing (quota/outage), so a young child still gets a picture on every page; drops the hard 200-error `image_generator is None` path.
  - `app_helpers.py` — dropped `scary`/`monster`/`nightmare` from the Sprout keyword blocklist (they false-positived *reassuring* stories — "it wasn't scary" — into the generic fallback + paid for a regen); those words now defer to the LLM contextual classifier. Added `\b` word-boundary matching so "skill" no longer trips "kill".
  - `api_service_manager.dart` — story-gen client timeout 90s -> 150s, above the backend 120s `SYNC_STORY_TIMEOUT_SECONDS` ceiling.
  - `magic_review_step.dart` — `robin` companion resolves to the band-specific asset instead of a missing legacy `robin_normal.jpg`.
  - `test_image_routing.py` — 2 new tests covering the Sprout Flux fallback.
- **Fixed a stale test** the parallel session missed: `test_app_helpers.py::test_age_5_with_scary_monster_is_flagged` asserted the *old* content-filter contract. Updated it to match the narrowed gate (renamed to `test_age_5_scary_monster_defers_to_llm_classifier`, assertion flipped to `flagged is False`). Folded into `af0e39d9`.
- **Reverted noise** before committing: 6 `lib/providers/*.g.dart` files (confirmed zero semantic diff via `--numstat` — pure LF/CRLF churn from a `build_runner` re-run) and 2 `test/goldens/failures/*.png` artifacts.
- Tests green: `test_image_routing.py` + `test_app_helpers.py` = 29/29.

## Still Pending / Deferred

- **Parallel session's MT-132 fix is in flight** — `lib/main_story.dart`, `lib/story_result_screen.dart`, and uncommitted `docs/MANUAL_TASKS.md` edits (MT-132 marked `[done]`, MT-133 filed) belong to another session. Left untouched for that session to commit and close.
- Untracked, not committed: `mt128-01..04-*.png` (Playwright verify artifacts), `.claude/scheduled_tasks.lock`.

## Blockers

- None.

## Files touched

- backend/routes/story_routes.py
- backend/tests/unit/test_image_routing.py
- backend/tests/unit/test_app_helpers.py
- backend/utils/app_helpers.py
- docs/MANUAL_TASKS.md
- lib/screens/wizard_steps/magic_review_step.dart
- lib/services/api_service_manager.dart

## Manual tasks

- Created: none
- Closed:  none

## Next session: start here

> The parallel session's MT-132 fix (`main_story.dart` + `story_result_screen.dart`) is still uncommitted on disk — let that session close it, or commit it if it's been abandoned. After that, MT-133 needs Darcy's call: the b667 resume/continue entry points are orphaned on the unreachable `StoryScreen` — decide whether `StoryScreen` becomes the real post-onboarding home (option A) or a continue affordance is ported onto the wizard welcome-back grid (option B, recommended).
