# SESSION CLOSE — 2026-05-31 14:39 [e178] — Branch: main — Cloudflare architecture orientation + memory correction

## Accomplished
- Answered Darcy's "where do I see how the app looks / what are we using" architecture question — clarified Flutter (`lib/`) = UI, Python backend = API (no UI), Cloudflare/Railway = infra.
- Ran an Explore sweep of Cloudflare usage and corrected a stale mental model: Cloudflare is now used in 3 places — (1) **Workers AI Flux Schnell is the DEFAULT story-illustration provider** (`backend/cloudflare_image_generator.py`, selected first in `_generate_flux_illustration()` at `backend/routes/story_routes.py:~533`, falls back to Replicate then Gemini; kill-switches `CLOUDFLARE_FLUX_DISABLED` / `FLUX_SCHNELL_DISABLED`); (2) **Cloudflare Pages** hosts the prod frontend (PR #169); (3) **Cloudflare R2** holds daily Postgres backups (MT-122). Avatars are NOT on Cloudflare (Gemini→Replicate→OpenRouter).
- Updated auto-memory (outside repo, not in git): rewrote `netlify_orphaned_frontend.md` (prod frontend = Cloudflare Pages, not Netlify/grand-light), created `cloudflare_usage.md`, updated `MEMORY.md` index.
- Confirmed the `UU` merge markers in the session-start git snapshot were stale — no merge in progress, no conflict markers left in the image-generator files.

## Still Pending / Deferred
- None from this session. The Cloudflare Pages migration cleanup (custom-domain cutover, deleting grand-light, disconnecting Netlify, stale-doc updates) is already tracked as MT-053–MT-056; my memory notes now point at them.

## Blockers
- None. Note: Bash/PowerShell git-state probes returned empty/errored mid-session (parallel-session shell contention suspected); fell back to the Explore agent's findings, which were sufficient.

## Files touched
- None in the repo. (Memory files live at C:\Users\Darcy\.claude\projects\C--dev-story-weaver-app\memory\ — outside git.)

## Manual tasks
- Created: none (Cloudflare cleanup already covered by MT-053, MT-054, MT-055, MT-056)
- Closed:  none

## Next session: start here
> No code work outstanding from this session. If picking up Cloudflare, the open thread is the prod cutover/cleanup in MT-053–MT-056 (custom domain → Pages, then delete grand-light + disconnect Netlify).
