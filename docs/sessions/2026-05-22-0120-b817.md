# SESSION CLOSE — 2026-05-22 01:20 [b817] — Branch: main — Gemini Flash TTS overflow tier + MT-170/179/180 follow-ups

## Accomplished
- `d2e31d0f` — Added Gemini 3.1 Flash TTS as the overflow tier between ElevenLabs and Edge in the narration fallback chain. New `backend/gemini_tts_service.py` (GeminiTTSService + MockGeminiTTSService, 8 curated voices, ElevenLabs-ID→Gemini-voice mapping, inline emotion-tag injection, PCM→MP3 via pydub). `backend/routes/tts_routes.py` gains `_get_gemini_service()` / `_gemini_synthesize()` and the 3-tier chain (ElevenLabs → Gemini → Edge → on-device). `backend/services/cost_tracker.py` gains `gemini_tts_cost()` at $0.054/1k. Path is inert today (free ElevenLabs credits not yet binding) — fires only when ElevenLabs is unconfigured / over-budget / errors.
- `c6aeaf48` — Created MT-178/179/180 to track the deliberately-deferred follow-ups.
- `390de0e5` — MT-180: 52 new tests (`test_gemini_tts_service.py` 40 + `test_tts_routes_fallback.py` 12). Full unit suite 399 pass / 7.7s. Also scoped 10 unrelated `.gitignore` `/test_*.py` patterns to repo root — unscoped versions were silently excluding legit pytest files in `backend/tests/`.
- `28e51bf4` — MT-179: `PREMIUM_BYOK_MATRIX.md` TTS rows updated for the 3-tier chain; new "Post Year-1 ElevenLabs credits" cost subsection (Premium worst-case margin 28%→46%, Family 25%→47%); "structural lock-in" framing softened.
- `43144909` — MT-170: README.md brand sweep ("Once Upon YOUR Child" leads), 7-band age table replaced with canonical 6-band set, "Big Feelings"→"Life Quests", Stripe moved to Completed.
- `388ac56b` — Closed MT-170/179/180 in `MANUAL_TASKS.md` with commit refs.

## Still Pending / Deferred
- **MT-178** — Smoke-test the Gemini overflow path against a real backend (local or Railway). Wiring is proven in mock-mode by the 52 tests, but no real Gemini TTS API call has been made yet. Walkthrough was given to the user this session (7-step local-backend recipe: `$env:ELEVENLABS_API_KEY=""` → `python app.py` → `/auth/anonymous` → POST `/tts/synthesize` → expect `provider:'gemini'` → decode + play MP3). Not yet executed.
- Gemini multi-speaker (narrator + character voice) dialogue path — not wired; ElevenLabs `generate_speech_with_dialogue` stays the only dialogue-differentiated path. Future work, no MT filed.
- Gemini chunking for very long stories — not implemented (single-shot only); add only if a long-story failure surfaces.

## Blockers
- None.

## Files touched
- backend/gemini_tts_service.py (new)
- backend/routes/tts_routes.py
- backend/services/cost_tracker.py
- backend/tests/unit/test_gemini_tts_service.py (new)
- backend/tests/unit/test_tts_routes_fallback.py (new)
- .gitignore
- docs/PREMIUM_BYOK_MATRIX.md
- README.md
- docs/MANUAL_TASKS.md

## Manual tasks
- Created: MT-178, MT-179, MT-180
- Closed:  MT-170, MT-179, MT-180

## Next session: start here
> Run MT-178 — the local-backend smoke test of the Gemini TTS overflow path (recipe is in the conversation history and the MT-178 entry). It's the only thing standing between this feature and "verified." ~10 min; once `provider:'gemini'` shows in the response and the MP3 sounds right, close MT-178.
