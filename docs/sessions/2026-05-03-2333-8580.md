# SESSION CLOSE — 2026-05-03 23:33 [8580] — Branch: main — MT-035 root-cause investigation (anon-user verification structurally blocked)

## Accomplished
- Resumed prior multi-MT verification (MT-019 already PASS in earlier session). Investigated MT-035 against the live Railway deployment (`grand-light` is on commit `4058525e` — HEAD, fix `a7f641b2` is in).
- Confirmed three independent dead-ends for anon-user MT-035 verification:
  1. `lib/screens/character_library_screen.dart:80` loads characters from `/get-characters` API only — no localStorage fallback. Patching `flutter.isar_characters` does not surface a fake character in "My Characters."
  2. `lib/story_result_screen.dart:2201` renders the "My Chronicles" button only when `widget.characterId != null`. The Sprout wizard initialises `WizardData()..characterName = _savedName` (`lib/main_story.dart:206`) and never sets `characterId`, so the button is hidden.
  3. `lib/screens/wizard_story_screen.dart:626` has the same `characterId != null` guard around its Chronicles icon button.
- Verified the SharedPreferences-on-web encoding gotcha: `flutter.isar_stories` is double-encoded in `localStorage` (the SP plugin JSON-wraps the already-JSON string before storing). Manual JS patches need `JSON.stringify(JSON.stringify(arr))`. Single-encoding `flutter.isar_characters` did not surface the character either, but that's moot since the screen reads from the API.
- Patched all 4 stored stories in `localStorage` to add `charactersJson` (manual JS) — verified the data shape would satisfy the `ChroniclesListScreen._loadChronicles()` filter at `lib/screens/chronicles_list_screen.dart:73-76`. Could not exercise the filter through the UI (no path to that screen as anon).

## Still Pending / Deferred
- **MT-035 save-bug re-test still open.** Last session reported `charactersJson: null` on freshly saved stories despite the deployed fix. I attempted a clean re-test (cleared `flutter.isar_stories`, walked the wizard) but the wizard's GO! button at `magic_review_step.dart` (`Ready to go?` screen) wouldn't fire from `page.mouse.click()` in CanvasKit — re-tested at multiple coordinates and via Tab+Enter; no advance. Needs a fresh session with a different click strategy (e.g., flutter semantics tree query, or `dispatchEvent` directly on the glass pane), OR a populated-account test, OR temporary debug logging in `_saveStory()` at `lib/story_result_screen.dart:1294-1335` to print `widget.characterName`, `widget.characterAge`, and `saved.characters.length` at save time.
- **MT-035 view-side verification structurally blocked for anon users.** Per d110's classification (kept), this needs a logged-in account with a saved character. The Sprout flow as anon has no UI route to `ChroniclesListScreen`.
- **MTs not started this session:** MT-018 (Make One Up), MT-020 (BYOK validation + readability), MT-016 (Sprout launch TTS), MT-030 (offline scaffold fallback). All still open.

## Blockers
- Sprout wizard "GO!" button on the Ready-to-go review step did not respond to Playwright `page.mouse.click()` or keyboard Enter in this session — blocked the clean save-bug re-test.
- Anon Sprout users have no UI path to `ChroniclesListScreen`. Deeper than a test-harness gap; the screen is gated behind a populated character record.

## Files touched
- None (no commits this session — investigation only).

## Manual tasks
- Created: none (MT-035 already covers the same ground; adding a save-bug subtask would duplicate)
- Closed:  none

## Next session: start here
> MT-035 needs a populated-account session to verify the view side, OR temporary `debugPrint` instrumentation in `_saveStory()` at `lib/story_result_screen.dart:1294` (log `widget.characterName`, `widget.characterAge`, and `saved.characters.length` right before `StoryLocal.fromSavedStory(newStory)`) + a quick Railway redeploy to re-confirm whether the save bug is real or whether last session's null reading was stale browser cache. The non-blocked MTs to pick up are MT-018 (Make One Up panel), MT-020 (BYOK readability), and MT-030 (offline scaffold) — none require a populated account. Also: 50+ untracked screenshot PNGs sit in repo root (`mt035-*.png`, `mt-after-*.png`, `step*.png`, etc.) — most pre-date this session. `.gitignore` patterns from MT-001 don't cover these naming variants; either extend `.gitignore` or `git clean -f *.png` after spot-checking the list.
