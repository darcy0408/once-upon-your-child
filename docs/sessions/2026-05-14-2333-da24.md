# SESSION CLOSE — 2026-05-14 23:33 [da24] — Branch: main — Closed MT-125 + filed/closed MT-126 (themes-recall actually wired) + filed MT-127

## Accomplished

- **MT-125 closed** (commit `884018cf`). The two pre-existing widget-test failures from MT-104 close — `test/widgets/subscription_ui_test.dart:61` and `test/widgets/subscription_sync_widget_test.dart:71` — were asserting against the legacy `"Adventurer"` displayName. Source enum (`lib/subscription_models.dart`) and the surrounding ecosystem (pricing page, paywall, subscription_screen, settings) all use `"Premium"`. Tests aligned; 3/3 green.

- **MT-126 filed and closed in-session** (commit `a0aab005`, pushed). The themes-recall feature shipped 2026-05-14 (a1bf) was **dead code on the Flutter side**. Confirmed by a delegated Playwright + Railway-logs smoke-test agent: walked Explorer band twice for the same character (`1b320068-3937-406c-88a2-684edc9a629d`), Loki query for `prior_adventures` returned **zero hits** across both stories. Root cause (verified by code read by the agent): `WizardDataMapper.mapToStoryRequest()` never emitted a `character_id` key; `ApiServiceManager.generateStory()` had no `characterId` parameter; backend `payload.get("character_id")` returned None on every request, short-circuiting `_build_prior_adventures_block()`. Persisted Story rows had NULL character_id even on repeats, so no priors ever accumulated.
  - **Fix:** added `'character_id': data.characterId` to mapper (whitespace-stripped, key omitted when null/empty); plumbed `String? characterId` through `generateStory` → `_generateStoryWithBackendRetry` → `_generateStoryWithBackend` body; wired both call sites (`magic_review_step.dart:440`, `bedtime_wizard_screen.dart:558`). 3 new mapper tests in the `MT-126` group; 7/7 wizard mapper suite green.
  - **Out of scope (correctly):** `quick_story_screen.dart` + `quick_story_provider.dart` (no saved character context; free-text name only); `interactive_story_service.dart` (already passes character_id via separate `/generate-interactive` endpoint).
  - Pushed to origin/main; Railway auto-deploy of grand-light follows. Verification re-run deferred to next session.

- **MT-127 filed** (still open). Pre-existing `psycopg2.errors.StringDataRightTruncation` on every `/achievement/sync`: `last_story_date_iso` column is VARCHAR(10) but client posts a full 24-30 char ISO datetime. Side-finding from the MT-126 smoke test agent's Loki logs. Wrapped in try/except so user-facing generation is fine, but the field is never persisted — feature regression for "when did this user last create?". Recommend ALTER TABLE to VARCHAR(32) + model bump. ~20 min.

- **Worktree hygiene.** Pruned two stale agent worktrees (`agent-a7d6cd87…` / branch `mt-104-subscription-dedup`, `agent-aa5b98ccf4c…` / branch `worktree-agent-aa5b98ccf4c30d285`). Both lagged main; their work was already merged (MT-104 as `75a57d2a`, MT-121 as `c9f14b10`). Force-removed via `git worktree remove -f -f` (locks were held by this session's own PID 31212 — legacy state from earlier in the day). Branches deleted.

## Still Pending / Deferred

- **MT-123 audit count.** Surfaced the SQL twice to Darcy (`SELECT count(*) FROM story WHERE created_at BETWEEN '2026-05-14 22:25:00+00' AND '2026-05-15 03:35:00+00'`) but Darcy did not return the count during this session. Run it in Railway dashboard → Postgres → Database tab — should be near zero given near-zero current traffic.
- **MT-126 production re-verify.** Frontend auto-deploy of `a0aab005` was triggered by the push at ~23:30 MT; the next session should re-run the same Playwright Explorer-band twice-for-same-character walk and grep Loki for `prior_adventures injected for character_id=<uuid> (block_len=N>0)`. If still zero hits, look at what `WizardData.characterId` actually contains at submission time — `hero_creator_step.dart:618/899-905` sets it from backend `/characters` POST response, which can return either `character_id` or `id` depending on the path. There's also a `characterId ??=` fallback at line 929 worth checking.
- **MT-124** (first DAILY snapshot landing) and **MT-122** (off-Railway pg_dump) — both correctly held over; MT-124 earliest tomorrow evening, MT-122 deferred until first paying user.
- **MT-120** (per-power visual override verify) — needs BYOK/premium test account; flagged for a session when that's handy.

## Blockers

- None. Two minor notes for the next agent:
  1. `mcp__railway__railway-agent` still has no direct Postgres SQL execution — schema migrations and audit queries must go through the dashboard Database tab or `railway connect`.
  2. Force-removing a worktree locked by `claude.exe` requires `git worktree remove -f -f` (two `-f` flags) — single `-f` will refuse if the lockfile is present, even when the holding PID is the current session.

## Files touched

This session committed:

`884018cf`:
- `test/widgets/subscription_sync_widget_test.dart`
- `test/widgets/subscription_ui_test.dart`

`a0aab005`:
- `lib/screens/bedtime_wizard_screen.dart`
- `lib/screens/wizard_steps/magic_review_step.dart`
- `lib/screens/wizard_steps/wizard_data_mapper.dart`
- `lib/services/api_service_manager.dart`
- `test/screens/wizard_data_mapper_test.dart`

Session-close commit (pending):
- `docs/sessions/2026-05-14-2333-da24.md` (new — this file)
- `docs/MANUAL_TASKS.md`
- `TEAM_COORDINATION.md`

## Manual tasks

- Created: MT-126 (filed + closed same session — themes-recall character_id wiring), MT-127 (achievement_stats VARCHAR(10) truncation, still open)
- Closed:  MT-125 (displayName widget tests), MT-126 (character_id wiring, same-session create+close)

## Next session: start here

> Re-run the Playwright themes-recall smoke test against the post-`a0aab005` deploy. Use the same fingerprint: Explorer band, create or reuse one character, generate two stories back-to-back, grep `mcp__railway__get-logs` on `story-weaver-app` for `prior_adventures injected for character_id=`. If `block_len=N>0` on story 2, MT-126 verification is done and themes-recall is fully live. Also collect MT-123's row count if Darcy hasn't already (one-line SQL in Railway Database tab; see MT-123 entry for the exact query).
