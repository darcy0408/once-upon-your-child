# SESSION CLOSE — 2026-07-07 11:18 [c071] — Branch: main — Screen-free Bedtime Mode overhaul (PR #391 merged)

## Accomplished
- **PR #391 merged** (squash `ae60bfa6`): full overhaul of the screen-free/voice-only Bedtime Mode. Owner had never seen the feature run — root cause was a client-side BYOK gate that dead-ended every keyless user on a "set up a Gemini key" card (which also pushed parents toward Gemini, against the MT-137 direction). The gate is gone; bedtime generates keyless via the backend like every other mode.
- **Audio-only from the very beginning**: new home-screen button under the primary CTA — "Bedtime Story 🌙 — no screen, just listening" (young bands) / "Voice Story — audio only" (mature). New shared `lib/widgets/bedtime_launch_sheet.dart` backs both this entry and the wizard's existing one (wizard's 97-line dialog now delegates to it).
- **Continuing stories wired in**: returning heroes (bands with `usesHeroSaga`) get a spoken "Previously in your saga…" recap and can continue Issue N+1 by voice. Client sends `theme: superhero` + `prior_saga` + `bedtime_mode`; new backend `build_bedtime_overlay()` (story_service.py) composes calming pacing/length onto the saga prompt while preserving `saga_state`; the finished Issue is recorded back via `heroSagaControllerProvider` so the visual superhero flow stays in sync. Design gotchas saved to auto-memory (`bedtime-mode-design-gotchas`): overlay-composition depends on the superhero branch winning the `elif bedtime_mode` chain, and an explicit duration OVERRIDES band word caps (why Sprout sends none).
- **All-band polish**: Sprout 3-question short flow (name/companion/place, band word caps); age question skipped when age already known; one gentle "one more time?" retry before improvising; tap-to-choose chips on every choice question (mic-less fallback — before, no mic meant the app silently picked everything); interactive segments send `tone: cozy-adventure`; fixed a latent `choicesRaw[1]` RangeError on single-choice responses.
- Tests: new `backend/tests/unit/test_bedtime_overlay.py` (5 tests); 123 backend unit tests green incl. prompt-versioning hashes; `flutter test test/unit/services` 68/68; analyze clean. Two lint follow-up commits on the PR branch (isort order, unused pytest import).

## Still Pending / Deferred
- **202 async poller** (filed as MT-335): long/companion-heavy bedtime requests can still hang on the broken poll path — pre-existing, found earlier today by a parallel session, no MT existed until now.
- **BYOK interactive → client-side Gemini** (filed as MT-334): `api_service_manager.dart` `_generateInteractiveStoryWithGemini` runs gemini-2.0-flash from the client when a personal key is set — MT-137-class exposure the server coercion can't reach. Not introduced by this session; surfaced during the audit.
- Real-device voice verification of the whole flow (MT-333) — web STT/TTS differs from phone.

## Blockers
- None.

## Files touched
- lib/screens/bedtime_wizard_screen.dart (major rework)
- lib/widgets/bedtime_launch_sheet.dart (new)
- lib/main_story.dart
- lib/screens/wizard_story_screen.dart
- lib/services/api_service_manager.dart
- backend/services/story_service.py
- backend/tasks/story_tasks.py
- backend/tests/unit/test_bedtime_overlay.py (new)

## Manual tasks
- Created: MT-333, MT-334, MT-335
- Closed: none

## Coordination notes
- Session a493's uncommitted `docs/MANUAL_TASKS.md` edits (new MT-332 + MT-326 progress update) were on disk at close time and ride along in this session's docs commit — expected under the shared-tree protocol; a493's own close will simply see no diff for those lines. a493's modified `docs/UX_AUDIT_SIXHATS_2026-07-05/FIX_PLAN.md` and untracked `screenshots/` were left untouched.

## Next session: start here
> MT-335 (the 202 async poller) is the highest-leverage code fix left — it's a launch blocker that affects every story mode. After the next Railway deploy, MT-333's device walk-through of bedtime mode is the quickest owner win.
