# SESSION CLOSE — 2026-05-16 13:11 [369d] — Branch: main — MT-128 verify, reader-exit fix, per-hero Continue affordance

Long multi-day session (2026-05-15 → 05-16). Verified the b667 resume/continue + illustration-persistence features, found and fixed a reader-exit strand bug, and built a per-hero Continue affordance into the wizard welcome-back grid. Also added a Sprout illustration fallback and filed the Cloudflare image-gen migration.

## Accomplished

- **MT-128 verified** — Playwright Sprout age-3 walkthrough. Confirmed: page bookmark (`story_progress_<id>`) write/read, saved-story reopen-resumes-at-page (left at Page 2, reopened at Page 2 of 3), the "Continue" card renders + reopens. b667 model fields `coverImageBase64`/`pageIllustrationsJson` present in the saved record. Illustration-persistence non-null round-trip + cover "Start Reading" page NOT verifiable while Gemini image quota is exhausted.
- **Sprout illustration Flux fallback** (`af0e39d9`, committed by parallel session) — `backend/routes/story_routes.py`: when Gemini-via-OpenRouter returns empty for Sprout, fall through to Replicate Flux Schnell so young children always get a picture. Reuses existing `REPLICATE_API_TOKEN`. 2 new tests in `test_image_routing.py`; 20/20 green.
- **MT-132 — reader-exit strand fix** (`c16beb3e`) — `_exitReader` did `popUntil(isFirst)`, which (since `_AppEntryPoint` renders `WizardStoryScreen` as root) stranded the child on the wizard "Make Magic" review step with a live GO! button. Now `pushAndRemoveUntil`s a fresh `WizardStoryScreen(initialStep: 0)`.
- **MT-133 — per-hero Continue affordance** (`0de9f590`, committed by parallel session) — `hero_creator_step.dart`: welcome-back grid loads saved stories, shows a "📖 Continue" badge on heroes with a recent story (≤30 days), and intercepts the hero tap with a Continue / Start-a-new-story bottom sheet. `_recentStoryForCharacter` matches by id-then-name (fixes b667 name-only-match bug).
- **`_exitReader` backend-fetch follow-up** (`c503c3e4`, this close) — `_exitReader` was reading characters from local Isar, but a hero created moments earlier hasn't synced there yet, so the grid fell back to the create-a-hero form. Now fetches from `/get-characters` (Isar fallback). Browser-verified: exit → welcome-back grid with "📖 Continue" badge → sheet → Continue reopens the story.
- **MT-131 filed** — Cloudflare Workers AI image-gen migration (genuinely-$0 at current scale). A parallel session has already started it (`f87eff3d`).

## Still Pending / Deferred

- **MT-131** — Cloudflare migration in flight on a parallel session (`backend/gemini_image_generator.py` + `backend/replicate_image_generator.py` modified, uncommitted by that session).
- **MT-129** — generated illustration doesn't match the created character; untouched.
- Illustration-persistence non-null round-trip + the cover "Start Reading" page — blocked on exhausted Gemini image quota; MT-131 (Cloudflare) would unblock.
- MT-133 verification used a same-port reload to reach the welcome-back grid; the integrated exit→grid path was verified across constituent steps, not one unbroken Playwright run (dev-server port churn kept wiping localStorage).

## Blockers

- Repeated dev-server friction: stale build at session start, `flutter run` port churn wiping localStorage between verifications, and one unresponsive/blank dev server (CanvasKit aborted) that needed a full user restart. None blocked the code; they slowed browser verification.

## Files touched

- backend/routes/story_routes.py
- backend/tests/unit/test_image_routing.py
- lib/story_result_screen.dart
- lib/main_story.dart
- lib/screens/wizard_steps/hero_creator_step.dart
- docs/MANUAL_TASKS.md

## Manual tasks

- Created: MT-131, MT-132, MT-133
- Closed:  MT-128, MT-132, MT-133

## Next session: start here

> MT-131 (Cloudflare image-gen) is in flight on a parallel session — check `backend/gemini_image_generator.py`/`replicate_image_generator.py` before touching image routing. Otherwise pick up MT-129 (illustration doesn't resemble the created character) — trace character appearance from the saved `Character`/`GeneratedAvatar` into the `/generate-illustrations` payload.
