# SESSION CLOSE — 2026-07-02 20:58 [de9f] — Branch: main — Diagnosed Max token-limit crunch; added token-discipline guidance to /start-session

## Accomplished
- **Diagnosed the "vastly different week-over-week" Max quota wall** using `/usage` data (4 screenshots). Grounded, not theory:
  - **Ruled OUT GitHub Actions** — verified `.github/workflows/` + `scripts/` contain **zero** Claude/Anthropic usage (no `ANTHROPIC_API_KEY`, no `anthropics/claude-code-action`, no `claude -p`). The June-15 agent-credit split only bites non-interactive Claude usage; Darcy has none. GHA does not appear in the usage breakdown at all.
  - **Ruled OUT Fable** — the "Current week (Fable)" bucket is at **0% used**.
  - **Actual burn drivers (from `/usage` "What's contributing", last 24h):** >150k-context sessions **57%**, sessions active 8+ hours **50%** (longest ever = **6d 20h**), subagent-heavy sessions **31%**, `workflow-subagent` **28%**. Model mix all-time: **Opus 62.2% (4.8) + 34.0% (4.7) = ~96%**, Sonnet 2.5%, Haiku 1.3%.
- **Corrected a pricing error mid-conversation** (verified via the claude-api skill): Fable 5 is **$10/$50 per 1M** — 2× Opus 4.8's $5/$25 and the most expensive Claude model; earlier I'd called Opus+1M "most expensive," which was wrong.
- **Added a "Token discipline" section (§5) to `/start-session`** — commit `8d3508b5`:
  - Subagents: pick the most-specific `subagent_type`; **default Sonnet** for mechanical work (`model:"sonnet"` in Agent tool, `{model:'sonnet',effort:'low'}` in Workflow stages); reserve Opus for hard reasoning/synthesis.
  - Whole-session Sonnet nudge when planned work is routine.
  - **Fable: never auto-launched, but suggest-when-warranted** (Opus visibly thrashing / unusually hard work) with a first-use `/usage` bucket-verify reminder.
  - Best-effort `/close-session` / `/clear` / `/compact` reminders at natural boundaries.

## Still Pending / Deferred
- **Stranded avatar-decode fix** — `backend/gemini_image_generator.py` (+55) and `backend/tests/unit/test_image_routing.py` (+36) are still uncommitted in the working tree. NOT this session's work — another session's in-flight fix (data-URI base64 decode + MIME sniffing; fixes the `Failed to decode custom avatar image: Incorrect padding` seen in `backend_errors.log`, likely MT-086/097). Left untouched. Should be adopted into a PR and landed.
- **Deterministic reminder hook** — offered but not built. A `settings.json` Stop hook could make the close/compact reminders fire reliably (vs. the skill's best-effort). Trade-off: hooks can't cleanly read "context > 150k," so the trigger would be crude. Build only if the skill-based reminders prove insufficient.
- **MT-319 (Fable bucket test)** — created this session; owner-only.

## Blockers
- None.

## Files touched
- `.claude/skills/start-session/SKILL.md` (commit `8d3508b5`)
- `docs/MANUAL_TASKS.md` (MT-319 added — docs commit)
- `docs/sessions/2026-07-02-2058-de9f.md` (this file)
- `TEAM_COORDINATION.md` (index row)

## Manual tasks
- Created: MT-319
- Closed:  none

## Next session: start here
> Same priorities as [d108]/[1909]: the stranded **avatar-decode fix** is a genuine bug fix that's been floating on `main` across 3+ sessions — adopt it into a PR and land it (verify with `pytest backend/tests/unit/test_image_routing.py`). Owner long-pole unchanged: MT-317 (Google Play identity verification + org-vs-personal decision). If curious about the token crunch, MT-319 is a 5-min Fable bucket test.
