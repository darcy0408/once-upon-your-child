# SESSION CLOSE — 2026-05-16 12:38 [ec15] — Branch: main — Explorer-band Robin companion shows placeholder (404 asset fix)

## Accomplished
- Fixed the Explorer-band "Robin" companion rendering a paw-print placeholder on the
  "Make Magic" review step. Root cause: `_companionImage` in
  `lib/screens/wizard_steps/magic_review_step.dart` listed `'robin'` in its
  `legacyIds` set, so it short-circuited to the non-existent
  `assets/images/companions/robin_normal.jpg` (404). But `'robin'` is the id of a
  real band companion (Sprout/Explorer/Adventurer), not a legacy global one.
  Removed `'robin'` from `legacyIds` so it falls through to the band-specific
  branch, which correctly resolves `assets/images/companions/<band>/robin.png`
  (verified `explorer/robin.png` exists on disk).
- NOTE ON ATTRIBUTION: this one-file change was committed by a parallel session,
  not by ec15. Due to the shared-git-index race between concurrent claude.exe
  instances, the edit was swept into **`af0e39d9`** ("fix(sprout): Flux fallback
  for illustrations + content-filter narrowing") — its commit body explicitly
  includes the line "magic_review_step: resolve 'robin' companion to the
  band-specific...". That commit is already pushed to origin/main, so the fix is
  shipped. ec15 itself produced no commits of its own beyond this docs close.

## Still Pending / Deferred
- TTS 503s observed in the dev console — `POST http://127.0.0.1:5000/tts/synthesize`
  → 503 SERVICE UNAVAILABLE (repeated). This is the local backend's TTS endpoint
  failing, not a Robin-related issue. Already tracked: MT-016 explicitly notes
  this localhost 503 symptom, and MT-091 is the broader fix (add Google Cloud TTS
  WaveNet as a middle fallback so 503 only happens when ALL providers fail). No
  new task created — covered by existing backlog.

## Blockers
- None for the Robin fix.

## Files touched
- lib/screens/wizard_steps/magic_review_step.dart  (committed via af0e39d9 by a parallel session)

## Parallel-session work in flight (NOT touched by ec15 — left for the owning session to commit)
- lib/main_story.dart — `_buildContinueStoryCard` / `_continueCoverFallback` /
  `_mostRecentStory` removed (~89 lines). MT-132/MT-133 work.
- lib/story_result_screen.dart — `_exitReader` rewritten async (loads characters,
  `pushAndRemoveUntil`s a fresh wizard at step 0). MT-132 work.
- docs/MANUAL_TASKS.md — MT-132 marked [done], new MT-133 appended.
  Left uncommitted so the MT-132 session commits its own documentation.
- Untracked: `.claude/scheduled_tasks.lock`, `mt128-0{1..4}-*.png` (MT-128 verify
  screenshots from another session) — not committed, per policy.

## Manual tasks
- Created: none
- Closed:  none

## Next session: start here
> Robin placeholder is fixed and live on main (af0e39d9). If picking up TTS, start
> with MT-091 (Google Cloud TTS WaveNet fallback) — the dev-console 503s on
> `/tts/synthesize` are the symptom that task addresses.
