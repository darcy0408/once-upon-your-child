# SESSION CLOSE — 2026-07-07 21:12 [0207] — Branch: main — Screen-free bedtime made real: wakelock+dim, broken interactive path fixed, bedtime offline scaffolds (PR #409 merged, Fable)

## Accomplished
- **Briefing + focus pick**: Darcy asked "what can we do to improve the screen free mode." Mapped the whole feature (Explore agent on Sonnet), then shipped the recommended first chunk in worktree `sw-screenfree`.
- **PR #409 built, CI-greened, squash-merged** (`66dcfcb0`) — four fixes to the screen-free Bedtime Mode (PR #391 follow-ups), all client-side:
  1. **Wakelock + dim-to-dark** — the mode held no wakelock (device slept mid-story, killing TTS) and stayed fully lit. Added `wakelock_plus` (new dep) held through Q&A + narration; UI fades to near-black once narration starts (tap wakes 12s; questions auto-undim for the no-mic chip path). **Behavior change:** story end / sleep-timer expiry no longer auto-pops to the bright home screen — stays dark ("Tap anywhere to exit"), releases the wakelock, tap exits (`_settleForTheNight`).
  2. **Bedtime pick-a-path was broken end-to-end at the API contract** (found while chasing the listeners bug): the old `ApiServiceManager` interactive adapters read `currentSegment['text']` (real key is `segment.content` → nothing was ever spoken) and sent `character/theme/choice/story_so_far` to `/continue-interactive-story`, which requires `story_id`+`choice_id` → **every continue 400'd**. Same client/server contract-drift class as MT-227; invisible to backend tests. Rewired `_runInteractiveStoryLoop` onto `InteractiveStoryService` (the visual Pick-a-Path client): real choice ids, free-text via the `custom` lane (which also runs backend crisis screening — a disclosure now wakes the screen, speaks the gentle message, shows `CrisisResourcesPanel`), spoken duration mapped to `length` short/medium/long, `include_images=false` on both endpoints (new pass-through param on the service). Deleted the six dead adapter methods — this also removes the client-side `gemini-2.0-flash` interactive path (shrinks MT-334 exposure).
  3. **Listeners fixed** — siblings/friends named at the listeners step were computed but never passed on the interactive path; they now ride the `companions` payload (already supported server-side via `_build_companion_context`), alongside the magical companion.
  4. **Bedtime-aware offline fallback** — network failure at lights-out used to serve a normal-energy canned adventure. Added `isBedtime` to `StoryScaffold` + two authored wind-down scaffolds (`bedtime_young_starlit_meadow` Sprout/Explorer, `bedtime_older_quiet_harbor` Adventurer→Adult); `pickScaffoldFor(bedtime:)` prefers them for `bedtime_mode` requests and excludes them from daytime picks.
- Verification: `flutter analyze` clean; `test/unit` + `test/widgets` 533/533 (9 new scaffold tests). CI green (the 4 Netlify ✗s are the known orphaned-site noise, MT-205 sub-item 4).
- **Merged at Darcy's request** (he asked where the merge button was; merged via `gh pr merge 409 --squash`), remote+local branch deleted, `sw-screenfree` worktree removed.
- **Line-endings incident (repair included in the PR)**: a `sed -i` range-delete rewrote `lib/services/api_service_manager.dart` from its checked-in CRLF to LF → 5,548-line phantom diff on the repo's highest-traffic service file. Repaired in a follow-up commit (restore origin blob, re-apply real edits, convert to CRLF); net file diff is the true +2/−240. Amend+force-push was correctly blocked by the auto-mode classifier; normal follow-up commit used instead. Saved to auto-memory (`mixed-line-endings-sed-phantom-diff`).

## Still Pending / Deferred
- **MT-361 (new)** — bedtime polish backlog (fade-out ending, replay-last-story, Sprout narration rate, TTS cap-notice surfacing, review-screen entry orphan, maxTurns↔timer). See MANUAL_TASKS.
- **MT-333 (existing, now broader)** — the real-device bedtime walkthrough should also cover: wakelock holds / screen dims / stays dark at end (only observable on-device), pick-a-path by voice end-to-end (it was broken before #409, so it has NEVER been device-verified working), and the voice-path crisis flow.
- **PR #406 (wind-down breath, parallel session) needs a rebase** — it edits `bedtime_wizard_screen.dart`, which #409 just reworked. Its breath insert sits at the reading-start seam; expect a small conflict.
- MT-343 (prod-verify `include_images` stops illustration spend on audio-only) still open — #409 extends the same flag through `InteractiveStoryService`, so one prod check covers both.

## Blockers
- None. Shared main checkout was mid-conflict from session 0bdc's close at my close time — untouched; these docs went to main via disposable worktree `sw-docs0207`.

## Files touched (PR #409)
- pubspec.yaml / pubspec.lock (+ `wakelock_plus`), macos/Flutter/GeneratedPluginRegistrant.swift
- lib/screens/bedtime_wizard_screen.dart (wakelock, dim state machine, `_settleForTheNight`, interactive loop rewrite, crisis handler)
- lib/services/interactive_story_service.dart (`includeImages` on start + continue)
- lib/services/api_service_manager.dart (bedtime flag → scaffold fallback; six dead interactive adapters deleted)
- lib/services/story_scaffold_fallback.dart, lib/data/story_scaffolds.dart (bedtime-aware picker + 2 scaffolds)
- test/unit/data/story_scaffolds_bedtime_test.dart (new, 9 tests)

## Manual tasks
- Created: MT-361
- Closed: none (MT-333/MT-343 stay open; both now also verify #409 behavior)

## Next session: start here
> Rebase + merge **PR #406** (wind-down breath) — small conflict with #409 in `bedtime_wizard_screen.dart`. Then the **MT-333 device walkthrough** verifies #391+#406+#409 in one pass. MT-361 polish items are independent Sonnet-sized chunks.
