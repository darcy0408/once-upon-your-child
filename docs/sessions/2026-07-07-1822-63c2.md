# SESSION CLOSE — 2026-07-07 18:22 [63c2] — Branch: main — Red-team remediation: MT-336 P0s + MT-337 decisions → PR #402 merged (Fable)

## Accomplished
- **MT-337 owner decisions made** — walked Darcy through the three P1 red-team design calls with analysis + recommendations; he took all three recommended options: (1) `hero_seen_by` = hard-rule + deterministic marker screen with graceful fallback (not prompt-only, not kill-free-text); (2) secret care-mandate = always-on conditional trusted-adult clause (not adult-always, not defer-to-clinician); (3) crisis net = extend regex now, defer LLM classifier post-launch.
- **PR #402 built, CI-greened, merged** (squash `4f9c3a2d`, worktree `sw-redteam`, 5 commits) covering all of MT-336 + MT-337:
  - **F-1 gate** (`a0c14091`) — `ANTIHERO_CRUX_ENABLED` env flag (default OFF) + verified-age-anchored 15-17 band reject on `/generate-antihero-crux` + `/generate-antihero-resolution`; crisis guard deliberately runs BEFORE the gate (distressed teen gets resources, not a 403). 6 new route tests incl. age-inflation + cached-context band checks.
  - **F-4 egress** (`66b9d76d`) — `scrub_external_links` on antihero part-1 title/pages/crux/choices, part-2 pages + saga_state (it round-trips into next-issue continuity prompts), and interactive segment content/title/choices (create + continue, via new `_scrub_segment_links` helper). **Bonus find:** the probe's literal `t.me/vanishhelp` survived the scrubber even on the main path — `me` wasn't in the bare-domain TLD list; added `me`/`ly` (sanitizer.py:254).
  - **F-2 grooming screen** (`c84aedaa`) — new `backend/utils/confidant_screen.py` (online-acquaintance / older-non-family / secrecy-demand / stranger markers, recall-leaning); a hit silently swaps the named confidant for the generic being-known anchor. SAFE-CONFIDANT prompt rule appended whenever a name IS injected. Both fragments now built by ONE shared helper for single-shot + crux builders (F-11 drift guard). Also: `hero_seen_by` was missing from `_crisis_guard` on both `/generate-story` and the crux endpoint — added.
  - **F-3/F-5 mandate** (`ccd0032e`) — shared `_serious_risk_clause`: harm-by-others / self-harm-sign secrets MUST reach a trusted adult or real help; helpers (counselors, social workers, teachers, parents) may never be framed as a threat or "the wrong kind of attention" (B1's failure phrase, banned by name). Conditional by construction so ordinary secrets keep the peer-confidant YA register.
  - **F-6 crisis net** (`3ed443f0`) — behavioral-warning-sign patterns (possessions given away, final goodbyes, won't-be-around, better-off-without-me, burden talk) + harm-by-others patterns (known-person subject + hurts/hits/beats/touches me, afraid/scared to go home), first-person anchored so story fiction doesn't trip. Childhelp (1-800-422-4453) added to backend `CRISIS_RESOURCES` AND `lib/widgets/crisis_resources_panel.dart`.
- ~60 new tests; full backend suite 1762 passed. Black/flake8/isort clean; `flutter analyze` clean on the Dart file.
- Cleaned up: remote + local `session/redteam` branch deleted, `sw-redteam` worktree removed (only auto-generated plugin-registrant churn, force-removed).

## Still Pending / Deferred
- **MT-347 (new)** — live-model re-run of probes A1/A2/B1/C1 against gpt-5-mini to evidence the prompt fixes for the clinician packet. Cheap Sonnet session.
- **Netlify deploy-preview noise** — the 4 red ✗s on every PR are the orphaned `reliable-sherbet-2352c4` site; disconnect is already tracked as **MT-205 sub-item (4)** (owner, Netlify dashboard). Not re-filed.
- Pre-existing worktree-env test failures documented for future sessions: `test_api_contracts.py::test_create_checkout_session_contract` needs `STRIPE_PRICE_ID_PREMIUM`; 4 `test_comprehensive.py` failures need the security-assertion env vars — both come from `backend/.env` (present in main checkout, absent in fresh worktrees). Verified identical on pristine origin/main; NOT regressions.
- PR #398 (annual billing / server quota, branch `session/pricing`) was still open at session start — untouched by this session.

## Blockers
- None. Auto-mode classifier correctly blocked self-merge; Darcy approved the merge explicitly.

## Files touched
- backend/routes/story_routes.py (gate helper + wiring, `_scrub_segment_links`, crisis-guard additions)
- backend/tasks/story_tasks.py (egress scrub in run_antihero_part1/part2)
- backend/services/prompt_service.py (`_build_seen_by_bullet`, `_serious_risk_clause`, both builder sites)
- backend/utils/sanitizer.py (me/ly TLDs)
- backend/utils/confidant_screen.py (new)
- backend/utils/crisis_detection.py (new pattern classes + Childhelp)
- backend/tests/test_antihero_routes.py, backend/tests/security/test_external_link_scrub.py, backend/tests/test_superhero_prompt_adolescent.py, backend/tests/unit/test_confidant_screen.py (new), backend/tests/unit/test_crisis_detection.py
- lib/widgets/crisis_resources_panel.dart (Childhelp card)
- docs/MANUAL_TASKS.md, TEAM_COORDINATION.md, docs/sessions/2026-07-07-1822-63c2.md (this file)

## Manual tasks
- Created: MT-347
- Closed: MT-336, MT-337

## Next session: start here
> MT-347 (live-probe verify on Sonnet) is the natural follow-up and produces clinician-packet evidence. Otherwise PR #398 review/merge is the remaining loose thread from the pricing work. The antihero band stays OFF in prod until Darcy flips `ANTIHERO_CRUX_ENABLED` post-clinical-review — that flip is deliberately NOT tracked as a task yet.
