# SESSION CLOSE — 2026-05-18 16:37 [3dfa] — Branch: main — MT-149 cost-reduction deploy smoke test (passed)

## Accomplished
- Ran the MT-149 functional smoke test of the cost-reduction deploy live against production. All three cost wins verified working — exercised the real code paths via the prod backend API (an anonymous free-tier account from `POST /auth/anonymous`), not the CanvasKit UI. Closed MT-149 in commit `a7793d17` (`docs(tasks): close MT-149 ...`).
  - **Flux Schnell for Sprout illustrations** — `POST /generate-illustrations` age 4 → HTTP 200, `provider: flux_schnell`, image entry `provider: cloudflare-flux-schnell` (the $0 Cloudflare Workers AI primary). Decoded + eyeballed the image: clean, on-style, age-appropriate. `quota_used: 1/60` confirms the default Sprout free cap.
  - **illustration_cache hit** — identical re-read → HTTP 200, `cached: true`, byte-identical image returned (no provider call), `quota_used: null` (zero quota consumed — a re-read is genuinely free).
  - **free-tier flash-lite text** — `POST /generate-story` free tier → HTTP 200, 12-page age-4 story, good quality. `subscription_tier` defaults to `'free'` → `_resolve_text_model('free')`; `GEMINI_MODEL_FREE` confirmed unset on `story-weaver-app` so the code default `gemini-2.5-flash-lite` is in effect.
  - Kill-switches `FLUX_SCHNELL_DISABLED` / `CLOUDFLARE_FLUX_DISABLED` confirmed unset → Flux routing active.
- Saved two reusable notes to auto-memory (outside the repo): `prod_api_smoke_test_recipe.md` (use `/auth/anonymous` to smoke-test authenticated prod behavior without driving CanvasKit) and `railway_deploy_log_gotcha.md` (Railway's deploy log stream carries only gunicorn stderr, not the app's INFO stdout).

## Still Pending / Deferred
- MT-149 residual: watch Railway / Gemini / OpenRouter spend over the next few days. Folded into the MT-149 close note as non-blocking — no separate MT created.
- MT-149's line is `[done]` but still physically under `## Open tasks` in `docs/MANUAL_TASKS.md` — it was not relocated to `## Closed tasks` because a parallel session (7ce6) had uncommitted edits to that file at close time and I would have swept up their work. Relocate in a future session.

## Blockers
- None.

## Files touched
- docs/MANUAL_TASKS.md (commit `a7793d17` — MT-149 status `open` → `done` + close note)

## Manual tasks
- Created: none
- Closed:  MT-149

## Next session: start here
> Cost-reduction deploy is verified live and MT-149 is closed. Highest-value remaining QA is MT-150 (M-8 monetization re-scope + one-free-avatar) and the MT-139 per-band Playwright sweep. Note: `pubspec.lock` and `prod-boot-state.png` were dirty/untracked from a parallel session at close — not this session's, left alone.
