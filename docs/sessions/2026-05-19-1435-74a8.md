# SESSION CLOSE — 2026-05-19 14:35 [74a8] — Branch: main — MT-139 per-age-band production verification sweep

## Accomplished
- **MT-139 sweep** — `d2e1d1d0` — ran a Playwright + Flutter-semantics walkthrough of `grand-light-production-68d9.up.railway.app`. Sprout & Explorer fully walked (wizard → story gen → result screen); Adventurer coping system + age-picker walked. Wrote `docs/qa/MT-139-findings.md` + 20 screenshots.
- **14 verify-MTs closed PASS** (marked `[done]` in `MANUAL_TASKS.md`): MT-032, 043, 052, 053, 054, 055, 058, 059, 065, 067, 074, 080, 081, 100.
- **MT-113 RE-OPENED** — found the rate-limit-shadowing bug is still live on production: free ages-6+ stories get **zero illustrations** and no upsell. The per-page prefetcher fires a burst of `/generate-illustrations` POSTs; the free-tier `1/min` limit returns HTTP 429 (not the `ILLUSTRATION_QUOTA_EXCEEDED` shape). Network trace confirmed: Explorer story = 2×[429], 0×[200]. Re-scoped MT-086/097/098 accordingly.
- **MT-157 filed** — Parent Controls "Allow photo-based avatar creation" toggle wrongly says photos are "processed on-device and never uploaded" — contradicts the COPPA consent screen (photo IS sent to OpenRouter/Replicate/Cloudflare). COPPA-sensitive copy fix.
- Also found: MT-097 gibberish text still baked into some Sprout illustrations on prod (pages 1–2 of the test story).

## Still Pending / Deferred
- **Adventurer / Creator / Adolescent / Adult story walks** — not automatable. The older-band wizard's setting→story-style "Next" button is not reachable via Playwright's semantics tree (owner manually confirmed it works for real users — a tooling limitation, not a bug). Needs a manual or instrumented-build run.
- Unverified verify-MTs: MT-016/018/019/021/030/044/056/068/073/088/090/096/120/050. MT-139 stays open as the umbrella for these.
- MT-088/096 (photo-avatar) need a real child face photo (not supplied). MT-016/019 (TTS audio) blocked by MT-091 (ElevenLabs outage).

## Blockers
- Flutter CanvasKit web build: Playwright can drive it only after enabling the semantics tree (`flt-semantics-placeholder` click); the tree is partial — some buttons' tap actions aren't exposed. See memory note `playwright_canvaskit_technique`.

## Files touched
- docs/qa/MT-139-findings.md
- docs/qa/mt139-*.png (20 screenshots)
- docs/MANUAL_TASKS.md

## Manual tasks
- Created: MT-157
- Closed:  MT-032, MT-043, MT-052, MT-053, MT-054, MT-055, MT-058, MT-059, MT-065, MT-067, MT-074, MT-080, MT-081, MT-100 — all marked `[done]` **in place** in the Open section (not bulk-moved to Closed; a future cleanup pass should move them, as session 0fe7 did for 59 entries). Also re-opened MT-113 (retagged in the Closed section).

## Next session: start here
> Top priority is **MT-113** — free ages-6+ users get no story illustrations on production. A parallel session is already editing `backend/utils/app_helpers.py` + `docs/rate-limiting-audit.md` (the exact rate-limit code) and may already be fixing it — check there first. After that, finish MT-139 with a manual or instrumented-build run of the 4 older bands and the remaining unverified MTs.
