# SESSION CLOSE — 2026-07-07 14:02 [bd5b] — Branch: main — PR-queue clear: #390/#394 merged + post-hoc reviews of #392/#393/#395, MT ID-collision fix

> Note: this session's earlier in-flight edits were attributed to "5ffd" (minted from the session UUID before close); 5ffd and bd5b are the same session.

## Accomplished
- **Cleared the 5-PR morning queue.** Fanned 4 Sonnet reviewers out (one per mechanical PR) while reviewing #390 (safety red-team) directly on Fable. A parallel session merged #392/#393/#395 mid-review; the reviews completed as post-hoc verification — all three verdicts **sound, no reverts**.
- **PR #390 merged** (`1241c403`): fixed its Black lint failure (`0341c77e` on the branch), resolved the 3-file merge conflict caused by the parallel merges, and caught + fixed an **MT ID collision** — the red-team session had filed its tasks as MT-331/332, already claimed by parallel sessions. Renumbered to **MT-336/MT-337** consistently across MANUAL_TASKS, TEAM_COORDINATION, and the 7907 session record (merge commit `e6f99087`).
- **PR #394 merged** (audio-only `include_images` flag) after Sonnet review verdict merge-ready; the conflict GitHub reported had already been resolved by the parallel session's rebase.
- **MT-335 closed** — both prod bugs from the quality sweep are fixed on main: 202 async split-brain (#393, Redis task-owner store + durable `apply_async`) and superhero_meta band mislabel (#392). Reviews confirmed the regression tests would fail on pre-fix main.
- **Filed MT-338** — real finding from the #393 post-hoc review: the fix routes ALL story generation through the `--pool=solo` Celery worker (concurrency 1), serializing concurrent users and making Redis a hard SPOF for 100% of generation. Pre-launch fix required.
- **Filed MT-339** — #395 leftovers (client/server quota drift, stale "Upgrade to Family" button, display-only yearly price). Session 7231 later annotated it: items 1/3/4 are already fixed in its stranded PR #398.
- **Cleanup:** deleted merged branches `session/safety-redteam` + `feat/audio-only-no-images` (remote + local), removed/pruned their worktrees.
- **Memory updated:** `async_story_202_path_broken` (now FIXED + MT-338 pointer + local-dev CELERY_TASK_ALWAYS_EAGER gotcha), `antihero_gate_client_only` (PR #390 merged but gate still NOT landed; MT-336/337 renumbering note).

## Still Pending / Deferred
- **MT-336** — the two greenlit P0 red-team fixes (server-side `ANTIHERO_CRUX_ENABLED` gate at `story_routes.py:1075,1273` + extend `scrub_external_links` to antihero/interactive paths). Small, mechanical, highest-conscience open code item. Exact anchors in `docs/RED_TEAM_STORY_GEN_2026-07-07.md`.
- **PR #398** (annual billing + server quotas, on `session/pricing`) — MT-340 assigns merge + Stripe setup to Darcy; CI was still computing at close.
- **PR #399** (PDF export) — active parallel session's; hands off.
- Interesting near-misses from the post-hoc reviews, below MT threshold: #392's regen retry re-rolls with no corrective prompt prefix (unlike the Sprout `STRICT CONSTRAINT` pattern, likely low hit-rate); `/task-status` rate limit keys by raw IP unlike every other route (noted inside MT-338).

## Blockers
- None. Auto-mode classifier required explicit owner approval for merges/pushes/branch-deletes — all granted via AskUserQuestion mid-session.

## Files touched
- docs/MANUAL_TASKS.md (MT-335 → done + moved to Closed; MT-338/339 filed — landed via session 7231's sweep commit `b456db1f`, attributed 5ffd)
- TEAM_COORDINATION.md (index row)
- docs/sessions/2026-07-07-1402-bd5b.md (this file)
- On session/safety-redteam pre-merge: backend/tests/quality/antihero_redteam_ext.py (Black format), docs/sessions/2026-07-07-1036-7907.md (MT renumber), merge-conflict resolutions
- Memory (outside repo): async_story_202_path_broken.md, antihero_gate_client_only.md, MEMORY.md

## Manual tasks
- Created: MT-338, MT-339 (as 5ffd; already on main via `b456db1f`)
- Closed: MT-335 (as 5ffd/bd5b)

## Next session: start here
> Build **MT-336** on a fresh branch off latest origin/main — server-side antihero gate + egress-scrub extension, with regression tests (Sonnet-appropriate; anchors in `docs/RED_TEAM_STORY_GEN_2026-07-07.md`). Check whether PR #398 went green/merged first (MT-340); if merged, MT-339 shrinks to just the stale Family button + client test pinning.
