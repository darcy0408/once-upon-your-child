# SESSION CLOSE — 2026-07-05 07:34 [d9a5] — Branch: main — Robotic voice on the consent-CTA Pick-Hero screen fixed (PR #384); "wrong boy/girl images" ruled a non-bug

## Accomplished
- **PR #384 opened** (`da583d93`, branch `session/warm-hero-greeting`, pushed) — fixes the robotic voice Darcy heard after the parental-consent → Start Adventure flow on the "Create Your Hero" screen. Root cause: the older-band (Explorer/Adventurer/mature) page-1 greeting in `_speakPagePrompt` interpolated the child's name (`"Hi Jayley! Get ready for a brand-new adventure. Let's create your hero!"`). A name-unique string can never be pre-cached, so on the consent-CTA path — which lands straight on page 1 seconds after a cold launch — the live synth lost the race and dropped to the robotic on-device `flutter_tts` fallback. Fix: made the greeting name-agnostic and added both variants to `kWarmUpPhrases`, so it's a warm cache hit on the first tap (name still shown on-screen as "HI <NAME>!"). Same bug class + fix as PR #383 (welcome screen). `flutter analyze` on both files: clean.
- **Investigated + resolved the "old/wrong boy/girl buttons" report — it is NOT a bug.** Read the committed `assets/images/ui/gender/gender_explorer_boy.webp` / `gender_explorer_girl.webp` and confirmed they are *exactly* the images in Darcy's screenshot. Full repo sweep (tracked + untracked + git status): there is no second/"cooler" Explorer gender set anywhere, no alternate filenames, no old-vs-new pathway. An Explore agent traced the consent CTA → it lands on the single current `HeroCreatorStep` (same widget as every other entry). Conclusion: the "cooler new images" were planned but never generated/committed — the swap never landed. Owner confirmed: "maybe we did not change them."
- **Reconciled against the [[gender_picker_binary_decision]] memory** before touching anything — the Boy/Girl-only picker is a deliberate owner decision (2026-06-17, MT-265 reverted via #291), so I explicitly did NOT let an agent remove the buttons; surfaced the conflict and confirmed intent with Darcy first.

## Still Pending / Deferred
- **PR #384 needs merge + device-verify** (tracked as MT-325). Verify the warm greeting on a cold onceuponyourchild.app load via the consent → Start Adventure path.
- **Cooler gender-picker art (optional, owner-taste)** — if wanted, generate per-band boy/girl images and drop into `assets/images/ui/gender/` under the same filenames (no code change; paths at `hero_creator_step.dart:3113-3132`). Tracked in MT-325.
- **Asset inconsistency found (not fixed)** — `adventurer`+`creator` gender images are `.jpg` while other bands are `.webp`; `custom_avatar_screen.dart:1158-1167` references a non-existent `gender_adventurer_*.webp` (broken ref in the photo-avatar sub-flow). Fold into any future gender-art pass (MT-325).
- **Sprout (≤5) page-1 greeting** has the same latent name-interpolation TTS pattern (`_speakPagePrompt`, sprout branch, ~`hero_creator_step.dart:3924`) — left out of #384 to keep it small; different content shape. Follow-up if the robotic voice recurs on the Sprout avatar flow.

## Blockers
- None. (Image side is not blocked — it's a no-op; there was nothing to change.)

## Files touched
_(committed on branch `session/warm-hero-greeting` / PR #384, not on main)_
- lib/screens/wizard_steps/hero_creator_step.dart
- lib/services/app_tts_service.dart

## Manual tasks
- Created: MT-325
- Closed:  none

## Next session: start here
> Merge PR #384 and device-verify the warm greeting (MT-325). Otherwise the open owner threads are unchanged from last session: MT-321 (COPPA G-2/G-3 config flips), MT-322 (marketing/landing-page decision), or MT-320 clinician-reply check for the adolescent-band sign-off.
