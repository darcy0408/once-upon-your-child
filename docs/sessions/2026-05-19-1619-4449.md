# SESSION CLOSE — 2026-05-19 16:19 [4449] — Branch: main — Six Hats security audit 03 + full Critical/High/Medium/Low remediation

## Accomplished
- **Ran Six Hats security audit (03).** Full report at `audit-reports/03-security-20260519.md` plus six per-hat sub-files. Findings: **0 Critical, 1 High, 5 Medium, 7 Low/Info.** The two prior remediation batches (2026-05-16/17) were verified against current source: 20 of 21 spot-checked findings confirmed still fixed; one partial regression of prior L-7 re-filed as S-04. Prompt archived to `audit-reports/prompts/03-security-six-hats.md` for reproducibility.
- **S-01 (High) + S-04 (Medium) shipped — commit `817a1d46`.** Rate-limit `key_func` no longer trusts the client-supplied `X-User-ID` header — `get_user_identifier` / `get_user_tier` (`backend/utils/app_helpers.py`) and `get_current_user_id` (`backend/middleware/auth.py`) now key only on auth-middleware context or a verified JWT, falling back to remote IP. Closes a brute-force/limit-exhaustion bypass. `/usage/summary`, `/usage/daily`, `/usage/mock-mode` stopped echoing raw `str(e)`. Updated `docs/rate-limiting-audit.md` to reflect the removed `X-User-ID` strategy.
- **S-02/03/05/06/07/09/11 shipped — commit `c74776ae`.** Delegated S-03/S-05/S-06 to three parallel agents (disjoint files). Net: 14 files changed, 1,472 insertions / 98 deletions; full backend suite **943 passed**.
  - S-02 — deleted the dead `security/` tree (1,746 LOC); orphan test `test_iam_manager_isolation` removed from `test_authentication.py`. Directory delete itself landed via a parallel-session commit before I could commit it (git-index race).
  - S-03 — BYOK keys moved to AES-256-GCM authenticated encryption. New `gcm:`-prefixed versioned blob; `decrypt_api_key` stays backward-compatible with legacy AES-256-CBC; GCM auth-tag failure raises (tamper detection); `decrypt_user_api_key()` lazy-re-wraps legacy blobs on access — no migration downtime. The agent confirmed there is currently no runtime decrypt path; helper is in place for whatever next reads a stored key.
  - S-05 — avatar rate limiter moved from a per-process dict to Redis (`INCR`+`EXPIRE`), accurate across both gunicorn workers, self-evicting. In-process fallback now prunes stale hour-buckets (memory-leak fix).
  - S-06 — new `backend/utils/iap_notification_verify.py`: Apple JWS x5c-chain verification (Apple Root CA-G3 bundled at `backend/utils/AppleRootCA-G3.cer`, overridable via `APPLE_ROOT_CA_PATH`) and Google Pub/Sub OIDC verification (`google.oauth2.id_token.verify_oauth2_token`, audience from `GOOGLE_PUBSUB_AUDIENCE`). Wired as a hard gate on `/iap/{apple,google}/notifications` — unverified → 403, missing config → 503 (fail closed). Phase-2 entitlement logic still TODO; the gate is in place so it cannot land unverified.
  - S-07 — JWT TTLs moved onto the `Config` class so `app.config.from_object()` applies them before `JWTManager()` constructs.
  - S-09 — `/debug-gemini` and `/debug-openrouter` now return 403 when `is_production()`.
  - S-11 — `/auth/login` gained a Redis-backed per-account failed-login counter (`login:fail:{username}`, 15-min TTL). Password is checked first, so a correct password always succeeds — no victim-DoS lockout — but failed attempts past the threshold return 429.
- **MT-165/166/167 filed — commit `1423eaf7`.**
- **S-08 (Low) shipped — commit `f0edb28c`.** Tightened nginx `connect-src` from a `https: wss:` wildcard to the two concrete origins the production app actually calls — the backend API + Sentry ingest host. Verified via the Railway API that `grand-light` sets no `BACKEND_URL`/`CUSTOM_BACKEND_URL` override, so the build falls back to the hardcoded default backend in `lib/config/flavor_config.dart`. MT-165 closed inline.
- **All four commits pushed to `origin/main`** (parallel session beat me to the push; `git push` reported "Everything up-to-date" with `HEAD == origin/main` at `f0edb28c`).

## Still Pending / Deferred
- **MT-166** — COPPA launch gate: set `COPPA_REQUIRE_VERIFIED_CONSENT=true` in the backend Railway env once the Resend consent-email round-trip is confirmed working. Hard launch blocker for public release.
- **MT-167** — (1) S-06 operator step: set `GOOGLE_PUBSUB_AUDIENCE` in the backend env if/when STORE-1 IAP migration (MT-143) proceeds — without it the Google notification endpoint 503s. The Apple side needs no config (root CA bundled). (2) S-10/S-12/S-13 informational findings.
- **Post-deploy CSP verification** — when `grand-light` next redeploys, open the production app with the browser console open and confirm no `Content-Security-Policy` violations from the tightened `connect-src`. If Sentry telemetry or an API call gets blocked it will show there, and the fix is a one-line revert in `nginx.conf:50`.

## Blockers
- None.

## Files touched
- audit-reports/03-security-20260519.md (new)
- audit-reports/03-security-20260519-{white,red,black,yellow,green,blue}.md (new — six files)
- audit-reports/prompts/03-security-six-hats.md (new)
- backend/app.py
- backend/config/__init__.py
- backend/encryption_utils.py
- backend/middleware/auth.py
- backend/routes/api_key_routes.py
- backend/routes/avatar_routes.py
- backend/routes/iap_routes.py
- backend/routes/utility_routes.py
- backend/utils/app_helpers.py — note: subsequently re-modified by parallel-session commit `7d884d2b` (MT-113 expensive-tier limit bump); not this session's change.
- backend/utils/AppleRootCA-G3.cer (new)
- backend/utils/iap_notification_verify.py (new)
- backend/tests/api/test_avatar_routes.py
- backend/tests/security/test_authentication.py
- backend/tests/test_iap_notification_verify.py (new)
- backend/tests/unit/test_encryption_utils.py (new)
- docs/MANUAL_TASKS.md
- docs/rate-limiting-audit.md
- nginx.conf
- Not touched / left for owners: parallel-session churn (`macos/Flutter/GeneratedPluginRegistrant.swift`, untracked `mt139-*.png` / `prod-boot-state.png` scratch screenshots).

## Manual tasks
- Created: MT-165, MT-166, MT-167
- Closed:  MT-165

## Next session: start here
> Audit 03 is fully remediated and pushed; nothing in this session is half-done. The only follow-ups are an operator step (MT-166, COPPA flag — launch decision) and a post-deploy browser-console check that the tightened CSP (S-08) doesn't block any production traffic. If next session is hands-on, the highest-value targets remaining in the open backlog are MT-158 (parent content warnings + per-quest sensitivity metadata) and MT-159 (crisis resources in `questSomeoneNeedsHelp`).
