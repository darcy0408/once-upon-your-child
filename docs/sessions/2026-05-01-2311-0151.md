# SESSION CLOSE — 2026-05-01 23:11 [0151] — Branch: main — Welcome flow voice/UX fixes + wizard back-nav + Sprout review TTS

## Accomplished

- **`3f2d7493` feat(welcome)** — fixed the voice-input bug where the speech recogniser was picking up the TTS prompt (web speakers → mic) and concatenating it with the user's answer ("what is your name Darcy"). `_listen()` now stops in-flight TTS before opening the mic, and `_extractName()` strips "what is your name" / "tell me your name" prefixes as a defensive backstop. Also: replaced the long teaser/welcome prompt with "What's your name?", started the young-age grid at 3 (clean 3×3), and removed the post-age "Once Upon a Time / Tap me!" title splash + 7-second auto-advance timer + all 5 splash builders + dead state (`_titleTimer`, `_advanceFromTitle`, `_isAdventurer`). Net: 309 → 309 - 271 = 38 line file in `welcome_screen.dart` (well, big shrink).
- **`8d8d2669` fix(wizard)** — back from MagicReviewStep was dropping users on the gender picker because the PageView tears down HeroCreatorStep when off-screen and `initState` reset `_heroPage` to 0/1. Now `_previousStep` sets `_requestedSubStep = 3` + bumps the nonce when leaving step 1, and `HeroCreatorStep.initState` honours `widget.requestedSubStep` so the user lands on page 6 ("Make Magic") regardless of state preservation. Extracted the sub-step → page mapping into a static helper.
- **`20d55d56` fix(review)** — Sprout launch screen `speak()` was inside an `addPostFrameCallback` inside `build()` with no guard, so every rebuild fired another speak. Two of those racing on web caused the "robotic voice" the user reported (one ElevenLabs request fails mid-pipeline → fallthrough to `_fallback.speak` at `app_tts_service.dart:284`). Now tracks `_lastSpokenLaunchPrompt`; rebuilds with the same text are no-ops, edits re-speak. Spoken text composed from the recap card data: "I am Darcy. Going to Stomp the Dinosaurs. With Mochi."

## Still Pending / Deferred

- **Robotic voice may persist** even after the dup-fire fix if the user's local backend isn't configured with `ELEVENLABS_API_KEY`, or the auth path is failing. Check `POST /tts/synthesize` — 503 indicates backend config, other errors indicate the `TtsApiService.synthesize` path. See `app_tts_service.dart:251`. Captured as MT-016.
- **Other-band launch screens** (Explorer/Adventurer/Creator/Adolescent/Adult) likely have the same `addPostFrameCallback`-inside-`build` TTS pattern. Only Sprout was fixed. Captured as MT-017.
- **Pre-existing analyzer warnings** in `welcome_screen.dart`: `_glyphForAge` and `_glyphForOlderBand` are unreferenced (orphans from a previous refactor, unrelated to this session). Left alone — may be intentional.
- **Orphaned widget**: `lib/widgets/adventurer_welcome_sequence.dart` no longer has any importer after the splash removal. User asked to keep it on disk for potential future reuse (e.g. first-ever band-up celebration). Not deleted.

## Blockers

- None.

## Files touched

- `lib/screens/welcome_screen.dart`
- `lib/screens/wizard_story_screen.dart`
- `lib/screens/wizard_steps/hero_creator_step.dart`
- `lib/screens/wizard_steps/magic_review_step.dart`

## Manual tasks

- Created: MT-016, MT-017
- Closed:  none

## Next session: start here

> Pick up MT-016 first — hot-reload the running Chrome instance, walk a Sprout-age user (e.g. age 4) through the wizard end-to-end, and confirm the launch screen now speaks the recap once in the ElevenLabs voice. If still robotic, check backend `POST /tts/synthesize` response. After that, MT-017 to apply the same dup-fire guard to the other bands' launch builders in `magic_review_step.dart`.
