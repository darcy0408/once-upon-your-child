# SESSION CLOSE — 2026-05-12 16:16 [cb6a] — Branch: main — Triage-backlog unblock + Sentry wiring (5 MTs resolved, 1 new)

## Accomplished
- **MT-095 closed** — Archived the two 2026-05-11 triage reports (`ea5a0541`) + closed in MANUAL_TASKS (`8a16845b`). Root finding: 6e04 session was wrong; `.gitignore:125` already had `!docs/agent-briefs/reports/` un-ignoring the path. No policy change needed. Future triage briefs land in that directory cleanly.
- **MT-092 deferred / wontfix** (`11261e09`) — Sentry Seer Autofix add-on not worth paying for at current scale (1 issue / 14 days, 0 users impacted, manual triage suffices). Saved a memory ([sentry-seer-overkill](../../../Users/Darcy/.claude/projects/C--dev-story-weaver-app/memory/sentry_seer_overkill.md)) so future triage briefs don't keep flagging it.
- **MT-094 closed** — Sentry inbound filters (env / error-message / release) are gated to Business plan and above on this org; the Developer plan only exposes toggle filters + IP filter. Took the SDK route instead: added `beforeSend` hook in `lib/main.dart` that returns `null` for any event whose exception stack frames reference `dwds/src/injected/client.js` (`bce275c8`). Existing 33 events on STORY-WEAVER-5 manually resolved in the Sentry UI. Saved a memory ([sentry-developer-plan-filters](../../../Users/Darcy/.claude/projects/C--dev-story-weaver-app/memory/sentry_developer_plan_filters.md)) about the plan-tier limitation.
- **MT-093 closed** (`52714a02`) — Wired Python backend into Sentry. Audit found backend code was already complete (`backend/app.py:198` has full `sentry_sdk.init` with FlaskIntegration + `before_send` PII scrubber; `requirements.in:30` pins `sentry-sdk[flask]==2.58.0`). Gap was purely the env var. Created `story-weaver-backend` Sentry project (Flask platform), set `SENTRY_DSN` on `story-weaver-app` Railway service. Verified end-to-end: deploy `7ef73d8e`'s own boot-time `OperationalError: table audit_log already exists` surfaced as STORY-WEAVER-BACKEND-1 in the new project within ~2 min — no manual test request needed. Saved a memory ([sentry-verification-via-boot-errors](../../../Users/Darcy/.claude/projects/C--dev-story-weaver-app/memory/sentry_verification_via_boot_errors.md)) about using boot-time errors as a free verify trigger for future Sentry wiring work.
- **MT-109 filed + closed** (`29b8d5f5`) — Side finding from MT-093 verification: every fresh deploy was logging `OperationalError: table audit_log already exists`, crashing one gunicorn worker, restarting, then succeeding. With Sentry now wired, that error would page Sentry on every deploy and burn the 5K/month quota with self-noise. Root cause: gunicorn forks 2 sync workers; both call `create_app() → db.create_all()` concurrently; SQLAlchemy's existence-check + CREATE TABLE isn't atomic across processes. Fix: wrap `db.create_all()` in try/except, swallow `'already exists'`, log a single info line, re-raise on anything else. Matches existing surrounding error-handling style at `backend/app.py:519`. **Verified on deploy `c7ff581d` at 16:25:08 UTC** — workers boot once, no restart, no audit_log error in logs.

## Still Pending / Deferred
- **MT-112 (new)** — Anonymous-user `UNIQUE constraint failed: user.email` race at `backend/app.py:524-539`. Same gunicorn pattern as MT-109 but already caught by `except Exception as e: logger.error(...)`. Doesn't crash, but now pages Sentry every deploy. 1-line fix to down-level the IntegrityError log to info, OR upsert via `INSERT ... ON CONFLICT DO NOTHING`.
- **Working tree noise** — 30+ untracked `superhero-smoke-*.png` and `v2-*.png` files from a parallel session's Playwright runs; 10 unchanged `.g.dart` files showing as modified (Isar/Riverpod codegen, present across multiple sessions). Not touched — belongs to other sessions or codegen.
- **STORY-WEAVER-BACKEND-1** — Was visible in Sentry pre-fix (1 event from the audit_log race). Post-fix it should stop receiving new events; if it appears again in the next deploy, MT-109 didn't catch all paths.

## Blockers
- None for any of the work above.

## Files touched
- `docs/agent-briefs/reports/inflight_triage_2026-05-11.md` (new, archived)
- `docs/agent-briefs/reports/sentry_triage_2026-05-11.md` (new, archived)
- `docs/MANUAL_TASKS.md` (5 closures + MT-109 + MT-112)
- `lib/main.dart` (beforeSend hook)
- `backend/app.py` (db.create_all race guard)
- `C:\Users\Darcy\.claude\projects\C--dev-story-weaver-app\memory\sentry_seer_overkill.md` (new)
- `C:\Users\Darcy\.claude\projects\C--dev-story-weaver-app\memory\sentry_developer_plan_filters.md` (new)
- `C:\Users\Darcy\.claude\projects\C--dev-story-weaver-app\memory\sentry_verification_via_boot_errors.md` (new)
- `C:\Users\Darcy\.claude\projects\C--dev-story-weaver-app\memory\MEMORY.md` (index updated)

## Commits this session
- `ea5a0541` docs: archive 2026-05-11 triage reports (closes MT-095)
- `8a16845b` docs(mt): close MT-095 — .gitignore exception was already in place
- `11261e09` docs(mt): defer MT-092 — Seer not worth paying for at current scale
- `bce275c8` chore(sentry): drop dwds dev-tooling events via beforeSend (closes MT-094)
- `e75b91ab` docs(mt): close MT-094 — dwds suppression shipped via beforeSend
- `52714a02` docs(mt): close MT-093 — backend Sentry wired up
- `29b8d5f5` fix(backend): swallow gunicorn worker race on db.create_all() [MT-109]

## Manual tasks
- Created: **MT-112** (anonymous-user race — parallel session took MT-110/MT-111 mid-session for Explorer findings)
- Closed:  **MT-095, MT-094, MT-093, MT-109** ([done]). **MT-092** ([wontfix]).

## Next session: start here
> Consider knocking out MT-112 (1-line down-level of the anonymous-user IntegrityError log) before the next deploy regenerates STORY-WEAVER-BACKEND-N noise in Sentry. After that, the natural next pickup is MT-111 (Explorer superhero mid-sentence truncation) which is a real user-facing bug from parallel session `6298`, or MT-090/MT-089/MT-087 if you want Playwright verification work instead.
