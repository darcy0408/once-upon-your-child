# SESSION CLOSE — 2026-05-11 08:16 [fa4d] — Branch: main — Premium = the family tier (6 slots, adult relatives, rotating hero)

## Accomplished

- **`94ff8bd3 feat(premium):`** — Implemented the "whole family" Premium spec per Plan 2.
  - `lib/subscription_models.dart`: Premium `maxCharacters` 3 → 6; rewrote `premiumTier.features` for "whole family" framing.
  - `lib/character_management_screen_v2.dart`: replaced hardcoded `/5` count with a `FutureBuilder<int>` reading `SubscriptionService.getMaxCharacters()`.
  - `lib/widgets/archetype_card.dart`: added `CharacterArchetypes.adultArchetypes` (Mom/Dad/Grandma/Grandpa/Aunt/Uncle) + new `AdultRelativeArchetype` data class. Adults have no personality sliders or specialAbility — supportive presence only. Emoji icons for now (art TBD).
  - `lib/models/wizard_data.dart`: new field `adultRelatives: List<Map<String, String>>` plumbed through `clone()` / `toJson` / `fromJson`.
  - `lib/screens/wizard_steps/hero_creator_step.dart`:
    - "Add a Grown-up" button + `_AdultRelativePickerSheet` modal (relation chips + optional name) → `_AdultRelativeChip` inline display.
    - Rotating hero: `_lastHeroId` persisted to `SharedPreferences` (key `last_hero_id`) on every character selection. `_buildPage0` reorders the list so the suggested-next sibling appears first with a 🌟 "Your turn!" gold badge; last hero moves to bottom but stays selectable. "Actually, let X go again" override link. Only shows with ≥2 saved characters + prior `last_hero_id`.
  - `lib/screens/wizard_steps/wizard_data_mapper.dart`: adult relatives are merged into `additionalCharacters` with the relation prepended ("Mom Sarah", "Grandpa Joe") — this lets the existing backend prompt builder treat them as adult-flavoured names without requiring a backend prompt change. Also sends a structured `adultRelatives: [{name, relation}]` payload key for future backend prompt-side support (MT-083).
  - `lib/subscription_screen.dart`: Premium card now headlines "Premium — for the whole family" with the new bullet list.
- **`4648d3ed fix(story-service):`** — Two unrelated stale-test fixes found while validating Premium.
  - `backend/services/story_service.py:604`: en-dash (U+2013) → hyphen (U+002D) in word-range string. Other lines in the file used plain hyphens; line 604 was the outlier. Zero LLM impact. Fixed 15 parameterized tests.
  - `backend/tests/unit/test_story_service.py`: rewrote `test_transform_parent_context_abstracts_raw_language` → `test_transform_parent_context_wraps_raw_input_in_delimiters`. The function switched from semantic abstraction to delimiter-wrapping in `61b87a32 security(imagine-it): injection hardening`; the test was reflecting the old contract. New assertions verify the `[user_input field="..."]...[/user_input]` wrapper format, the kid-safe meta-instruction line, and that `parent_hidden_context` is correctly NOT propagated (per `96770649`). Backend tests: 188 passed / 0 failed (up from 172 / 16 red on entry).
- **`4c1938e3 docs(premium):`** — Decision-log entry + 4 new MTs.
  - `docs/PREMIUM_BYOK_MATRIX.md`: `### [decided] 2026-05-10 — Premium = the family tier (no separate Family at v1) [fa4d]` with spec table, full implementation log mapping each bullet to its file, and explicit "deliberately not touched" section.
  - `docs/MANUAL_TASKS.md`: filed MT-080 through MT-083.
- **Push:** `53dc7a58..4c1938e3 main → main` pushed earlier in the session at the user's request.

## Still Pending / Deferred

- **Browser verification of all three Premium features.** Filed as MT-080 (6-cap + copy), MT-081 ("Add a Grown-up" picker + adult-in-story rendering), MT-082 (rotating-hero step). Spec says ~1–2 days total — UI is wired but unverified in a real browser.
- **Backend prompt-side support for `adultRelatives` payload key (MT-083).** Frontend already sends the structured `adultRelatives: [{name, relation}]` alongside the relation-prepended `additionalCharacters` entries. Current backend renders them as ordinary GUESTS by name; ideal contract is a dedicated `ADULT FAMILY (supportive adult presence — not peer characters, never villains)` section in `backend/services/story_service.py::AdvancedStoryEngine.generate_enhanced_prompt`. Deferred because `story_service.py` was on the locked Phase 1 monetization list when this session opened (lock expired mid-session when `6bef121b` landed, but rewriting the prompt section is its own scope).
- **Art assets for the 6 adult-relative archetypes.** Currently emoji icons (🧑‍🦰 🧑‍🦱 👵 👴 🧑‍🦳 🧑). `AdultRelativeArchetype.imagePath` field is there; Darcy will commission art later.
- **`story_weaver_core/lib/subscription_models.dart` shadow copy not updated** (still `maxCharacters: 5` on Premium). Deliberately deferred — the matrix flagged it as a separate consolidation item ("2 competing SubscriptionService classes"). If the consolidation lands first, it'll need a follow-up bump to 6.

## Blockers

- None. All planned scope shipped.

## Files touched

- `backend/services/story_service.py`
- `backend/tests/unit/test_story_service.py`
- `docs/MANUAL_TASKS.md`
- `docs/PREMIUM_BYOK_MATRIX.md`
- `lib/character_management_screen_v2.dart`
- `lib/models/wizard_data.dart`
- `lib/screens/wizard_steps/hero_creator_step.dart`
- `lib/screens/wizard_steps/wizard_data_mapper.dart`
- `lib/subscription_models.dart`
- `lib/subscription_screen.dart`
- `lib/widgets/archetype_card.dart`

## Manual tasks

- Created: MT-080, MT-081, MT-082, MT-083
- Closed:  none

## Notes for the next session

- **Parallel-session activity:** While this session was running, a separate session committed `6bef121b feat(monetize): Phase 1 cost attribution + TTS char budget + Premium upsell` (the Phase 1 files originally listed as "do not touch" in Plan 2). Those files dropped out of `git status` mid-session — caught it before assuming the test failures were mine. A second parallel session committed `2039080b feat(image-gen): hybrid routing — Flux Schnell primary for ages 6+, Gemini for Sprout (MT-084)` and `0e884f46 docs(image-gen): file MT-084 — flip Flux Schnell primary for ages 6+` after my push; those are on local main ahead of origin and will go up when that session closes.
- **Per-memory git-index race awareness:** Used explicit `git commit -- <paths>` form on every commit. No contamination observed.
- **Backend ↔ frontend payload note:** The wizard payload for adult relatives is currently sent two ways for resilience — once as relation-prepended strings inside `additionalCharacters` (works with today's backend) and once as a structured `adultRelatives: [{name, relation}]` key (ready for tomorrow's backend). When MT-083 lands, the prompt builder should prefer the structured key and `additionalCharacters` should be cleaned up to only carry plain peer names.

## Next session: start here

> Browser-verify the three Premium UI surfaces (MT-080, MT-081, MT-082) in order — these are the highest-value follow-ups and unblock shipping the Premium tier to real users. Walk a Premium user with 2 saved kid characters through: (1) hit the new 6-cap on the My Kids screen, (2) tap "Add a Grown-up" in the wizard and verify the chip appears + the story renders the adult as supportive, (3) start a second story and confirm the rotating-hero banner shows the sibling-not-last-hero at the top with the 🌟 badge.
