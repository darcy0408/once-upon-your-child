# SESSION CLOSE — 2026-06-14 12:08 [f312] — Branch: main (shipped via PR #268 + #272) — Story-gen LIVE off Gemini on OpenAI GPT-5 mini + prompt warmth upgrade

## Accomplished
- **Story-text generation is LIVE in prod off Gemini** — now on **OpenAI GPT-5 mini for every tier**. Railway `story-weaver-app`: set `OPENAI_API_KEY`, `STORY_GEN_PROVIDER=openai`, `OPENAI_FREE_MODEL=OPENAI_PAID_MODEL=gpt-5-mini`. Verified live: `provider_sequence=["openai(success)"]`, real story, companion honored, no Gemini call. (Closes **MT-252**.)
- **PR #268 merged** (`4aa69b39`) — direct-OpenAI `openai` provider + `tiered` (free→OpenAI / paid→Claude) mode, mirroring the claude provider; 52 unit tests; `openai==2.41.1`+`tqdm` hand-pinned into requirements.txt with verified PyPI hashes (lockfile-landmine-safe). Built last session, merged this one.
- **PR #272 merged** (`a5ce8270`) — `EMOTIONAL HEART` section added to `prompt_service.build_story_prompt` (theme-as-spine + show-don't-tell + earned closing reframe; explicitly forbids "she learned that…" morals). A/B-validated on mini: before-prompt ended with a 2-point listy moral; after ended "…without saying it in a lesson or a long speech, told Biscuit, 'We kept going.' …It sounded like courage." Zero preachy-phrase hits. Live in prod.
- **DECISION (closes MT-253):** chose GPT-5 mini for ALL tiers (equity — Darcy's explicit value that poor/free kids get stories as good as paying kids). Drove it with an n=5 blind taste test + a ceiling run (mini / GPT-5.4 / GPT-5.5 / Claude Opus 4.8 on the same prompt). Finding: mini follows prompts fully and writes comparably; the prompt is a bigger lever than the model. Opus warmest but ~11× cost; GPT-5.5 underwhelmed (reasoning overhead → thinnest story).
- **Diagnosed Gemini billing** (paid Tier-1; prod key = "StoryWeaver" proj `…589`; ~$2–5/mo, no credits) and **fixed a prod key typo** — the OpenAI key was first added on Railway as `OPEN_API_KEY` (missing "AI") → `openai(fail:no_key)` → static fallback; renamed to `OPENAI_API_KEY`, stray since deleted by Darcy.
- Memory written: `story_model_decision.md`, `gemini_billing_project_layout.md` (updated to live state), `user_profile.md` (registered-company note).

## Still Pending / Deferred
- **MT-248 remaining:** only the ElevenLabs / Edge-TTS replacement (child-permissible vendor, e.g. Azure AI Speech) + MT-250 (`/tts/transcribe` COPPA). The story-text blocker is now CLEARED.
- **Latency (not a bug):** age 9–12 stories run ~31–39s on mini (`ai_call_ms` ~30s; sync timeout is 75s so they complete, but it's a noticeable wait). Younger bands are short/fast. If ever worth improving: stream the story or route long stories through the async task path. Story-gen currently runs SYNC in the web process (`task_id="sync_task"`), not a separate Celery worker.
- **MT-255 (new):** OpenAI account hygiene — confirm usage cap is set + convert org from Individual → Business verification (owner-only).
- `tiered`/`claude` provider modes are built and dormant; would need `ANTHROPIC_API_KEY` on Railway if a paid Claude tier is ever wanted.

## Blockers
- None for this session's goals. NOTE: the shared main checkout (`C:\dev\story-weaver-app`) is mid-merge on a PARALLEL session's work — `prompt_service.py` had an unresolved conflict (`UU`, "Adolescent Crux Choice", markers at L1391–1731) and the checkout's branch kept flipping (`adolescent-crux-choice` → `saga-loop-adventurer`). My #272 was cut from clean `origin/main` and edits a different region (build_story_prompt ~L184 vs antihero builders ~L1391), so they won't line-conflict; that session must still reconcile its own conflict before it merges.

## Files touched
- backend/services/openai_story_generator.py (new — #268)
- backend/tasks/story_tasks.py (#268)
- backend/app.py (#268)
- backend/config/__init__.py (#268)
- backend/requirements.in (#268)
- backend/requirements.txt (#268)
- backend/.env.example (#268)
- backend/tests/unit/test_openai_story_generator.py (new — #268)
- backend/services/prompt_service.py (#272 — EMOTIONAL HEART)
- docs/MANUAL_TASKS.md, TEAM_COORDINATION.md, docs/sessions/2026-06-14-1208-f312.md (this close)

## Manual tasks
- Created: MT-255
- Closed:  MT-252, MT-253   (MT-248 updated, still open for TTS)

## Next session: start here
> Story-gen is shipped and healthy on OpenAI GPT-5 mini. Highest-value next moves: (1) MT-248's remaining TTS half (Edge/ElevenLabs → child-permissible vendor) + MT-250 `/tts/transcribe` COPPA — these are the last launch-gate text/voice items; (2) optionally extend the `EMOTIONAL HEART` win or address age-9–12 ~35s latency (stream/async). Quick owner item: MT-255 (OpenAI usage cap + Business verification).
