# SESSION CLOSE — 2026-05-11 23:22 [cbe9] — Branch: main — Wizard create-new fixes (age picker, name gate, companion-orb dedupe)

## Accomplished
- `8bcfcd29` — fix(wizard): age picker on create-new + name gate + avatar name guard. Three connected bugs from "Or create someone new" after generating a Sprout character:
  - `_switchToNewCharacter` now opens an age picker dialog (6 bands) and fires `onAgeChanged` to swap the global theme. Was previously locked to the prior character's age (Sprout → Sprout forever).
  - `_handleGenderSelection` gates the page-1 auto-advance on `characterName.isNotEmpty` — empty → SnackBar + refocus name field. Without this, users tapped past page 1, never set a name, and the final Make-Magic button silently stayed disabled (`wizardData.isComplete` requires a name).
  - `CustomAvatarScreen._generateAvatar` now early-returns with a friendly SnackBar when `_name.isEmpty`, instead of letting the backend reject with 400 "All fields required".
- `032d5354` — chore: bundle pending pre-session work that was already in the working tree (cryptography 46.0.5→46.0.7 + urllib3>=2.7.0 CVE bumps in `backend/requirements.in`, imagine_it_screen copy tweaks, story_result_screen pruning, new `page_turn.mp3` SFX asset, `docs/agent-briefs/` directory, and `smoke_*.png` Playwright screenshots at repo root).
- `265c9a9f` — fix(wizard): dedupe companion showcase by id. Picking Robin showed 2-5 orbs because `id: 'robin'` is reused across Explorer/Adventurer/Creator/Adolescent/Adult in `lib/widgets/hero_creator/companion_widgets.dart`, and `_buildCompanionShowcase` filters `allCompanionEntries()` (concatenation of all bands) by selected id. Now keeps one entry per id, preferring the active band's image.
- Pushed `5ee30ab2` (parent-controls Subscription button) on Darcy's behalf — was queued unpushed at session end. Not authored here.
- `npm audit fix` was attempted but another parallel session got the commit first (`8ca74b0c`).

## Still Pending / Deferred
- 25 GitHub Dependabot vulnerabilities remain on default branch (6 high, 17 moderate, 2 low) — npm side cleared, Python/Dart deps still need an audit pass.
- The `smoke_*.png` screenshots committed at repo root in `032d5354` are messy organisation — would be cleaner in `docs/agent-briefs/` but `docs/agent-briefs/reports/` is gitignored (see MT-095 for the bigger decision).
- Backend 503s observed in Darcy's browser console during testing — unrelated to wizard wiring, likely Railway backend transient unhealthy state. Not investigated.
- Parallel-session edits to `backend/gemini_image_generator.py`, `backend/services/story_service.py`, `lib/widgets/avatar_generating_view.dart` appeared mid-session and resolved themselves (committed by another agent). Left untouched.

## Blockers
- None.

## Files touched
- `lib/screens/wizard_steps/hero_creator_step.dart`
- `lib/custom_avatar_screen.dart`
- `backend/requirements.in`
- `lib/screens/wizard_steps/imagine_it_screen.dart`
- `lib/story_result_screen.dart`
- `pubspec.lock`
- `assets/sounds/page_turn.mp3` (new)
- `docs/agent-briefs/BRIEF_coppa_debug_bypass.md` (new)
- `docs/agent-briefs/BRIEF_inflight_triage.md` (new)
- `docs/agent-briefs/BRIEF_integration_tests.md` (new)
- `docs/agent-briefs/BRIEF_sentry_triage.md` (new)
- `docs/agent-briefs/PLAYWRIGHT_SMOKE_TEST.md` (new)
- `smoke_01_initial_load.png` through `smoke_09_after_end.png` (new, 9 files)

## Manual tasks
- Created: MT-100
- Closed:  none

## Next session: start here
> All three wizard fixes deployed and confirmed working by Darcy in this session ("it worked!"). The Robin dedupe is live but Darcy hasn't visually re-confirmed on Railway yet — MT-100 covers that browser-verify pass plus regression checks on non-cross-band companions. Worth pairing with a Dependabot audit on the remaining 25 vulnerabilities (Python/Dart side, npm is clean).
