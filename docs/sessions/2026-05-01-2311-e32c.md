# SESSION CLOSE — 2026-05-01 23:11 [e32c] — Branch: main — Sprout UX redesign + TTS dual-voice fix

## Accomplished
- **Replaced Sprout "Make One Up" 4-tile picker with free-form text+mic panel** (`21e6c3e8`)  
  Sprout age band now sees the same text+voice input that older bands get. Larger mic button (32px), Fredoka font header ("Where do you want to go? Tell us!"), Sprout-friendly hint examples. Deleted `_buildSproutWorldTiles()` method entirely. Updated TTS pre-warm narration to match.
- **Redesigned Sprout story-type screen** (`21e6c3e8`)  
  Replaced 2-up 136px `ImageModeOrb` cards with full-width stacked `_buildSproutModeCard` cards (approx 80px tall, emoji 44px, Fredoka bold 22px label). Added missing **Rhyme Time** as third option — phonological awareness is peak-relevant for 3–5 yr olds. Title font changed from ornate `CinzelDecorative` to `Fredoka` for Sprout. Subtitle changed to "Pick the one you like! 👇". Animated emoji scale on select, `AnimatedSwitcher` checkmark.
- **Fixed TTS dual-voice overlap (primary bug)** (`21e6c3e8` + absorbed into `20d55d56`)  
  `AppTtsService.speak()` now calls `await _fallback.stop()` before playing via `AudioPlayer`, so a prior flutter_tts fallback narration can't overlap a freshly-arriving ElevenLabs audio. Also: `MagicReviewStep.dispose()` now calls `AppTtsService.instance.stop()`; `_launchStoryCreation` now calls `stop()` before any countdown speak and has an explicit `band.band != AgeBand.sprout` guard to skip the 3-2-1 countdown for Sprouts.

## Notes on parallel session activity
Three commits landed during this session from concurrent agents:
- `3f2d7493` feat(welcome): fix voice bleed, simplify prompt, drop title splash
- `8d8d2669` fix(wizard): back from review lands on Make Magic, not gender picker  
- `20d55d56` fix(review): speak Sprout recap once in ElevenLabs voice

The `20d55d56` commit captured my working-tree edits to `magic_review_step.dart` (dispose stop, countdown stop+guard, Sprout ageBand check) before I ran `git add`. Verified all three fixes are present in the committed file.

## Still Pending / Deferred
- Rhyme Time for Sprout has no backend test coverage — see MT-019
- "Make One Up" mic panel not yet device-tested with real speech input — see MT-018
- MT-016 ("Browser-verify Sprout launch TTS") also covers the `_fallback.stop()` fix from this session — should test both the rebuild guard (from `20d55d56`) and the cross-sink stop (from `21e6c3e8`) together

## Blockers
- None

## Files touched (my commit 21e6c3e8)
- `lib/screens/wizard_steps/hero_creator_scene_page.dart`
- `lib/screens/wizard_steps/hero_creator_story_type_page.dart`
- `lib/services/app_tts_service.dart`
- (`lib/screens/wizard_steps/magic_review_step.dart` — my edits absorbed into parallel commit `20d55d56`)

## Manual tasks
- Created: MT-018, MT-019
- Closed:  none

## Next session: start here
> Run as a Sprout (age 3–4) end-to-end: name → archetype → companion → world (try "Make One Up" + mic) → story type (try Rhyme Time) → GO! — verify no voice overlap, recap speaks once, story generates. If ElevenLabs fails for the custom recap text, check `POST /tts/synthesize` auth headers (MT-016 context).
