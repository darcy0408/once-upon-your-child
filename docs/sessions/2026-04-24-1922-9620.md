# SESSION CLOSE — 2026-04-24 19:22 [9620] — Branch: main — MT-005 BUG-001 re-verify (18+ band wizard advance)

## Accomplished
- **Re-verified MT-005 BUG-001** (already closed by b00e/QA-2026-04-24, user wanted a second confirm): fresh Playwright session against Railway prod URL, 18+ age band, name entry → age picker → 18+ → wizard opens → scrolled through Character step (gender images loaded, archetype 2×2 grid present) → tapped Create Story → wizard advanced all the way to Step 4 "Ready to begin?" review with zero "Please choose a look for your character first" banner. **PASS confirmed.**
- **Documented Flutter/Playwright interaction technique** in `memory/reference_playwright_target.md`: local DDC dev server (ports 3000/8080) is unusable in Playwright due to missing COOP/COEP headers (SharedArrayBuffer blocked); only Railway prod URL works. Correct interaction: `page.mouse.click(x, y)` + `page.keyboard.type(text, {delay})` — NOT synthetic DOM events (Flutter CanvasKit ignores them).
- **No code commits this session.** `.gitignore` screenshot glob patterns were already committed in `ea3846f` (session b00e leftover).

## Still Pending / Deferred
- MT-007: BYOK wizard runtime verify (paste real key, confirm white text, Finish saves, no re-launch on "Full illustrations") — not touched this session
- MT-012: Age-gate COPPA consolidation audit (`welcome_screen.dart` vs `age_gate_screen.dart`) — not touched
- MT-013: BUG-002 retry cap hard limit in `app_tts_service.dart:~147`
- MT-010: Stale-JWT 403 vs 401 for real (non-anon) users

## Blockers
- Local dev server (`flutter run`) can't be Playwright-tested: missing `Cross-Origin-Opener-Policy: same-origin` + `Cross-Origin-Embedder-Policy: require-corp` headers → `SharedArrayBuffer` undefined → Flutter DDC runtime never bootstraps. Workaround: always use Railway prod URL.

## Files touched
- `memory/reference_playwright_target.md` (new, outside repo — memory system only)
- No in-repo file changes this session

## Manual tasks
- Created: none
- Closed:  none (MT-005 BUG-001 was already closed by b00e; this was a confirmation pass only)

## Next session: start here
> MT-007 BYOK runtime verify is the most actionable open item — code fix is confirmed correct, just needs a real `AIza…` key test on a BYOK-subscribed account. MT-012 COPPA consolidation is the highest-risk open item and should go to Opus.
