# SESSION CLOSE — 2026-06-07 15:00 [eb2e] — Branch: adventurer-craft-fixes — Companion-flow overhaul: kill "My Pet/Human", unify Add-a-Person, grown-up photos, premium signalling (PR #246 merged)

## Accomplished
User flagged that adding a person (e.g. a cousin) in the My Companions step read as **"My Pet / Human"**, that grown-ups couldn't get a photo, and that "Human" felt clinical. Shipped a two-chunk overhaul plus two bugs found during live testing — all on isolated worktree `sw-addperson` (`session/addperson`), squash-**merged to main as PR #246** (CI fully green: iOS build, backend, frontend, api-contract, Netlify preview). Worktree + branch cleaned up afterward.

- **Chunk 1 (`afb9ca4f`) — stop labelling people as "My Pet/Human".** New shared helpers in `companion_widgets.dart` (`kHumanSpecies`, `defaultCompanionName` → people "My Friend", `companionSubtitle` → relationship/"Friend", never "Human"); routed every default-name/subtitle site (grid, showcase orb, editor card, photo-pick fallback) through them. Buttons → **Add a Person** / **Add a Pet**; species dropdown shows **"Person"** (internal value unchanged).
- **Chunk 2 (`961b166f`) — unify the flow.** "Add a Person" opens a "Who's joining?" picker (Family & friends: cousin/brother/sister/friend/classmate + Grown-ups: reusing adult archetypes). Relationship stamped on the companion → shown as subtitle ("Cousin", "Grandma"). Removed the separate "Add a Grown-up" button + adult-relative chip UI (+ deleted the now-unused `_AdultRelativeChip`/`_AdultRelativePickerSheet` classes). `pet_card` carries `relation`+`isAdult` through the pending mechanism and preserves them across edits. **Mapper:** grown-up Human-pets route OUT of `companion_pets` into the adult-family path (`is_adult_relative` + `adultRelatives` payload, now with `avatar_data`) so they stay "supportive presence, never a villain" and are never double-cast; legacy `data.adultRelatives` merged in.
- **Bug fixes from live testing (in Chunk 2 + follow-ups):**
  - Orb remove **"✕" badge** was sliced by the circular clip → moved to a non-clipping outer Stack.
  - **"magic flow not working"** diagnosed (read-only subagent) as an **HTTP 403 premium gate** — photo→portrait is a paid feature; the free/anon dev tester hits it. Not a bug/quota. Client now special-cases `403`/`upgrade_required` and reads `body['error']`.
- **Premium signalling (`f1275a05` + `6d910acb`).** Upfront tappable hint under the add buttons (non-premium only): "✨ Magical photo portraits are a Premium feature — your companion still joins with their real photo." → info dialog → **"See Premium"** routes through `showPaywallGated` (COPPA math gate for kids) to `PremiumUpgradeScreen`.

## Product decision (recorded)
Recommended — and user accepted — **keeping companion photo-portraits premium-only** (it's the Family-tier differentiator; hero's one-free already delivers the aha; per-image cost ~$0.0375 multiplies fast with up to 3 companions/story). Reserve a possible "one free companion transform" only if conversion data later shows abandonment at this step.

## Still Pending / Deferred
- **Device/browser verification** of the whole flow — logic+analyze+CI verified only. Tracked as **MT-230**.
- To see the photo→portrait magic actually run in dev you must be **premium** (real 403 gate otherwise). Optional: flip local user `subscription_tier` in Postgres — not done.
- `pubspec.lock` modified in this checkout's working tree — parallel-session churn, NOT this session; left untouched.

## Blockers
- None. PR merged clean.

## Files touched
(All via worktree `sw-addperson`, now on main via PR #246)
- lib/widgets/hero_creator/companion_widgets.dart
- lib/widgets/hero_creator/pet_card.dart
- lib/screens/wizard_steps/hero_creator_step.dart
- lib/screens/wizard_steps/wizard_data_mapper.dart

## Manual tasks
- Created: MT-230
- Closed:  none

## Next session: start here
> Companion-flow overhaul is on main (PR #246). Quick win: walk **MT-230** live (Add a Person → relationship → photo; check labels, the "✕" badge, and the premium hint→upgrade route). The big remaining hygiene item is still **MT-229** (delete ~16 confirmed fossil branches) once you authorize the deletes.
