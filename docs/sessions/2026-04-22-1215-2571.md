# SESSION CLOSE — 2026-04-22 12:15 [2571] — Branch: main — BUG-002 TTS backoff root-cause fix + Playwright MCP isolation

## Accomplished

- **BUG-002 root cause found via static code audit** — `TtsRateLimitException` was thrown at `tts_api_service.dart:117` inside the outer `try` block, where the bare `catch (e)` clause immediately swallowed it. The exception was logged as "TTS API unavailable" and `null` returned — it never reached `_prewarm()`'s `on TtsRateLimitException` backoff handler. Net effect: on every 429, the phrase was silently skipped with no delay and no retry. The backoff curve (2 → 4 → 8 → 30 s) was dead code as shipped in `b6b5c15`.
- **BUG-002 fix committed** (`16d4dac`) — added `on TtsRateLimitException { rethrow; }` before the generic `catch (e)` in `TtsApiService.synthesize()`. Exception now propagates to `_prewarm()` and the backoff loop fires correctly. `dart analyze` clean.
- **Playwright MCP `--isolated` flag** added to `.mcp.json` (`bc4071b`) — prevents "Browser is already in use" lockfile clashes when multiple Claude Code instances run simultaneously. Effective on next process start.
- **Git archaeology** (`9d47fa9`, `5de3ba7`) — recovered orphaned commits from a mid-session rebase by a concurrent instance; committed staged leftovers from another session; wrote session close entry to TEAM_COORDINATION.md.

## Still Pending / Deferred

- **BUG-002 runtime verification** (MT-011) — code path is now correct, but actual runtime behaviour under ElevenLabs 429s is unverified. Needs a fresh Claude Code instance. Full procedure in `docs/briefings/TASK3_BUG002_TTS_FRESH_SESSION.md`.
- **BUG-001 Create Story end-to-end** (MT-005, opened by c29c) — avatar gate fix confirmed; full wizard flow through `/generate-story` 200 not verified in browser.

## Blockers

- **Playwright MCP lockfile** — 14+ competing non-isolated `@playwright/mcp` node processes sharing one Chrome profile (`mcp-chrome-for-testing-85fd96c`). The node process holding the lockfile is the current session's own MCP server — cannot be killed from inside the session without dropping the tool connection. Fix is already in `.mcp.json`; takes effect on next restart.

## Files touched

- `lib/services/tts_api_service.dart` — `on TtsRateLimitException { rethrow; }` added before generic catch (`16d4dac`)
- `.mcp.json` — `--isolated` flag appended to Playwright MCP args (`bc4071b`)
- `TEAM_COORDINATION.md` — BUG-002 static audit findings + deferral plan (`bc4071b`, `5de3ba7`)

## Manual tasks

- Created: MT-011 (BUG-002 runtime verification)
- Referenced: MT-005 (BUG-001 Create Story — already open), MT-001 (screenshot cleanup — already open)
- Closed: none

## Next session: start here

> The TTS backoff fix is correct in code — highest priority is runtime verification (MT-011). Start a **fresh** Claude Code instance (so `--isolated` Playwright activates), then follow `docs/briefings/TASK3_BUG002_TTS_FRESH_SESSION.md` and write the verdict back to `TEAM_COORDINATION.md`.
