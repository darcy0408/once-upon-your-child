# SESSION CLOSE — 2026-05-11 23:21 [f8c1] — Branch: main — Phase 1 monetization: Stripe wiring, Flux Schnell routing, free-tier illustration cap, matrix ratifications

## Accomplished

Long session pulling together monetization Phase 1 across multiple concurrent work streams. Highlights in chronological order:

- `add7e31b` — fix(sprout): rebalance non-LTR pages + LTR re-split fallback + filter "The End". Fixes 3yo walkthrough regression where Sprout LTR returned 2 pages and non-LTR returned 150/150/24 word lopsided pages. Added `_post_process_ltr_pages()` deterministic split + balanced `PageSplitter._split_by_sentences()`.
- `6bef121b` — feat(monetize): Phase 1 cost attribution + TTS char budget + Premium upsell. Shipped `cost_tracker.py`, TTS monthly char quota in `ai_quota.py`, ElevenLabs global budget guard, `TtsCapExceededException`, and MT-072 dual-action upgrade card on result screen.
- `ae954484` — docs(premium): cap-then-BYOK decisions + Option C orthogonal-axes proposal. My contribution to `docs/PREMIUM_BYOK_MATRIX.md`.
- `dc7ecf47` — test(image-gen): complete A/B comparison. 45 images across 3 providers (15 Gemini + 9 SDXL + 8 Flux Schnell). Hybrid recommendation: keep Gemini for Sprout (style match), flip Flux Schnell for ages 6+.
- `2039080b` — feat(image-gen): hybrid routing — Flux Schnell primary for ages 6+ (MT-084). New `generate_story_illustration_flux_schnell()` + dispatch in `story_routes.py` + `FLUX_SCHNELL_DISABLED` kill-switch + 7 unit tests.
- `8c516e02` — feat(image-gen): free-tier illustrations with monthly cap (MT-086). 10/100/200 images/mo for Free/Premium/Family; ages-6+ non-BYOK now eligible for per-page illustrations via Flux Schnell. Sprout/BYOK paths unmetered.
- `a7177258` + `83fee146` — docs(premium): ratify matrix items #10 (display name split), #12 (Family-as-narration-ceiling), #13 (BYOK Imagen graceful-fail), #17 (Stripe Smart Retries enabled by Darcy), plus #1/#2 (Premium-only at v1, defer Family to Phase 2 w/ family-photo feature), #5 (TTS char-baseline verification — bump recommended), #14 (Sprout illustration carve-out resolved by MT-086).
- `38e06784` — feat(tts): bump caps to 15k Premium / 35k Family + file MT-091 (Cloud TTS fallback). Per-band char-count math showed 1,800-char/story assumption was Sprout-class only; Adventurer+ single stories can exceed 10k cap.
- `8985d41e` — fix(stripe): write `is_paid_premium` SharedPref on every subscription emit (matrix Q2). The launch-blocker [7366] flagged — frontend gates reading `is_paid_premium` but nothing wrote it. Fix mirrors tier+status in `_emit()` so cache hydrate + network sync both populate it.
- `5f05f36c` — feat(settings): add real Stripe Checkout button in Dev Tools card. Unblocked MT-078 smoke testing — there was no path to actual Stripe Checkout anywhere in the app's user flows.
- `5ee30ab2` — feat(parent-controls): add Subscription section with Manage Subscription button. Discoverability parallel to settings_screen — Darcy could reach Parent Controls but not Settings from his dev session, so added a separate entry point.

Also did the OpenRouter dashboard verification with Darcy (saved finding to memory): production Imagen calls bill at $0.0375/image, NOT the $0.003 token-rate audit theory.

Also wrote two copy-pasteable Plan prompts during the session (image-gen A/B test for fresh session, Premium "family-friendly" spec for fresh session). Both were picked up by parallel agents and landed during the day (commits `94ff8bd3` family-tier, others).

Also Darcy enabled Smart Retries + custom recovery emails in Stripe Dashboard (manually). Stripe customer portal URL `https://grand-light-production-68d9.up.railway.app` saved to memory `stripe_customer_portal_url.md` so future sessions know. Brand naming saved to `brand_name.md` ("Once Upon YOUR Child Enchanted" is the current Premium product name; bigger brand picture is still iterating).

## Still Pending / Deferred

- **MT-078 smoke test paused mid-flow.** Darcy completed Stripe Test-mode setup (Product `Once Upon YOUR Child Enchanted` $9.99 created, webhook endpoint added at `https://story-weaver-app-production.up.railway.app/api/stripe/webhook` listening to 4 events) and Railway env vars are saved (`STRIPE_PRICE_ID_PREMIUM`, `STRIPE_WEBHOOK_SECRET` set; `STRIPE_API_KEY` exists but **`STRIPE_SECRET_KEY` does NOT** — see Manual Tasks). Did NOT actually run a test-card checkout. Pickup point: push commits, hard-refresh production grand-light app, navigate Parent Controls → Subscription → Manage Subscription → Subscribe → test card 4242 4242 4242 4242. While Darcy clicks through, I'm to tail Railway logs via MCP to verify webhook fires.
- **Live mode setup** — entire MT-078 process needs to be repeated in Stripe Live mode after Test passes.
- **Branch is 1 commit ahead of origin/main** (my `5ee30ab2`) — push pending. Will happen in Step 7b of this close.
- **TTS cap bump** (commit `38e06784`) shipped 10k→15k Premium and 25k→35k Family. NOT browser-verified — TTS flows still need real-story char counts to confirm the new caps feel right.
- **Multiple matrix items still open**: #3 (Premium cap drop 100→80), #4 (Imagen planning rate $0.05), #7 (`is_paid_premium` fix — I shipped it but `[7366]` had also claimed it, possible dual-commit collision — verify on next pickup), #9 (`subscription_screen.dart` rename to `upgrade_screen.dart`), #11 (free-tier custom-avatar exposure framing), #15 (trial-end soft landing UX), #16 (Free→Paid funnel — largely satisfied by MT-086 + MT-087 but the "first-taste free illustration" Stage 2 from `[3240]`'s funnel sketch is not formally implemented).

## Blockers

- Test-card smoke test wasn't executed. Reasons: time of day + Darcy's localhost/Railway state was a mess (multiple dev servers on different ports, mobile emulation on, kept landing in wizard-as-entry-point and couldn't reach Settings tab on home shell). Resolved by adding a Subscription section to Parent Controls (commit `5ee30ab2`) so there's a path Darcy can definitely reach next session — but he stopped before testing it.
- `STRIPE_SECRET_KEY` env var is missing on Railway story-weaver-app service. Backend reads from `STRIPE_SECRET_KEY` (`backend/config/__init__.py:101`) but Railway has the value under `STRIPE_API_KEY` (older name). Recommended Darcy add a NEW variable `STRIPE_SECRET_KEY` with the same value as `STRIPE_API_KEY` — he completed the other env var work but I didn't see him confirm this one. If he didn't add it, the smoke test will fail at the `stripe.api_key = None` boundary. Filed as part of Manual Tasks below.

## Files touched

Commits I authored this session (the rest were either parallel-session commits during the same window, or my own previously-recorded session commits already shipped):

- `backend/services/story_service.py`
- `backend/services/story_duration_service.py`
- `backend/services/cost_tracker.py` (new)
- `backend/utils/ai_quota.py`
- `backend/routes/story_routes.py`
- `backend/routes/tts_routes.py`
- `backend/gemini_image_generator.py`
- `backend/replicate_image_generator.py`
- `backend/tasks/story_tasks.py`
- `backend/tests/unit/test_story_service.py`
- `backend/tests/unit/test_image_routing.py` (new)
- `backend/tests/image_quality/run_provider_comparison.py` (new harness; created collaboratively with parallel session's prior work)
- `backend/tests/image_quality/results/manifest.json` (new)
- `backend/tests/image_quality/results/prompts.json` (new)
- `backend/tests/image_quality/.gitignore` (new)
- `lib/services/tts_api_service.dart`
- `lib/services/app_tts_service.dart`
- `lib/services/subscription_sync_service.dart`
- `lib/story_result_screen.dart`
- `lib/settings_screen.dart`
- `lib/screens/parent_controls_screen.dart`
- `docs/PREMIUM_BYOK_MATRIX.md`
- `docs/MANUAL_TASKS.md`
- `docs/IMAGE_GEN_AB_TEST_RESULTS.md` (new — partially edited by Darcy too)

Memory files written this session:
- `stripe_customer_portal_url.md` — grand-light URL Stripe wants for customer-portal redirects
- `brand_name.md` — naming conventions across surfaces

## Manual tasks

- Created: **MT-086** (Browser-verify free-tier illustrations with monthly cap), **MT-091** (Replace robotic flutter_tts fallback with Google Cloud TTS WaveNet).
- Closed:  **MT-072** (Free-tier illustration upgrade CTA — shipped as Premium-primary + BYOK-secondary dual-action card), **MT-084** (Flip image-gen primary to Flux Schnell for ages 6+).
- New manual task to file: **fix `STRIPE_SECRET_KEY` env var on Railway** before MT-078 smoke test (filed below as MT-092 if not already covered by the existing MT-078).

## Next session: start here

> Run `git push origin main` to ship today's 16+ commits to Railway. Wait for grand-light to redeploy (~2 min). Then resume MT-078 smoke test from Parent Controls → Subscription → Manage Subscription → Subscribe (Premium) → test card 4242. **Before clicking Subscribe, verify Railway env vars include `STRIPE_SECRET_KEY` (not just `STRIPE_API_KEY`)** — backend reads from `STRIPE_SECRET_KEY` per `backend/config/__init__.py:101`. While Darcy clicks the test card, tail Railway logs via MCP to confirm webhook fires + tier updates + `is_paid_premium` SharedPref writes.
