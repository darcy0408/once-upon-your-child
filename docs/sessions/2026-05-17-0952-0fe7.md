# SESSION CLOSE — 2026-05-17 09:52 [0fe7] — Branch: main — Backlog code-task batch + MANUAL_TASKS.md cleanup

## Accomplished
- Triaged the full 137-task manual backlog into actionable buckets (code I can do / Playwright-verifiable / Darcy-only) and shipped the safe code subset.
- **MT-136** — `280aee98` — corrected the BYOK wizard photo-avatar benefit copy; it claimed the cartoon is "never shared with anyone" but creating it sends the child's photo to the AI provider. Aligned with the C-1 consent-screen language.
- **MT-064** — `dd1e66bd` — added `/flutter_*.png` and `.claude/scheduled_tasks.lock` to `.gitignore`; deleted the stray 0-byte `flutter_01.png`. (`/imagine_it_*.png` was already ignored; those screenshots already gone.)
- **MT-076** — `dd1e66bd` — fixed `process_gender_images.py` `SRC` path → `assets/BoyGirl images/BoyGirl placeholder images/` where all 12 JOBS source files live. Script intentionally NOT re-run (would overwrite current gender assets).
- **MT-063** — `dd1e66bd` — fixed the one failing widget test in `test/widgets/wizard_steps/hero_creator_step_test.dart` (stale finders + unhandled age-band dialog + untyped `attributes:{}` fixtures). 6/6 pass.
- **MT-069(b)** — `5c9b27d5` — `magic_review_step.dart` now detects 429/quota/rate-limit story-gen errors and shows a kid-friendly line + parent note instead of the generic "Something went wiggly".
- **MANUAL_TASKS.md cleanup** — `ff35df45` — moved 59 `[done]`/`[wontfix]` entries that were parked in `## Open tasks` down to `## Closed tasks`. Verified: 140 MT IDs intact, identical ID set, 0 done-tags left in Open.
- **MT-139 filed** — `644291b3` — captured the ~28-MT browser-verify backlog as one tracked task.
- This close (0fe7) also marked MT-136/064/076/063 `[done]` and moved them to Closed; updated MT-069 in place (b done, a open).

## Still Pending / Deferred
- **MT-069(a)** — BYOK wizard returns to "Set up Free Premium" after a key is saved. The wizard itself saves correctly (`byok_setup_wizard.dart` `_EnterKeyStep` writes prefs + SecureStorage + pops). Bug is caller-side state refresh across ~6 screens; overlaps MT-073/074. Deferred.
- **MT-071** — its named file `lib/screens/wizard_story_screen.dart` is the *wizard flow* (PageController for wizard steps), NOT the story reader. The real reader is `lib/story_result_screen.dart` (~3000 lines, `PageFlipBuilder`). Re-scope the file reference before attempting tap-to-turn.
- **MT-098 / MT-046 / MT-047 / MT-083 / MT-129 / MT-070** — all touch `backend/tasks/story_tasks.py`, `backend/services/story_service.py`, `backend/routes/story_routes.py`, or `backend/replicate_image_generator.py`, which a parallel session has uncommitted. Not safe to edit mid-flight.
- **MT-139** — the per-band Playwright verification sweep itself (newly filed).

## Blockers
- A parallel session has ~30 files uncommitted (backend story pipeline + `lib/services/*` + Dockerfiles + new `backend/models/stripe_event.py`, `add_stripe_webhook_event.py`, `audit/`). This blocked 6 code MTs from being safely editable and means production is mid-flight — MT-139 should wait until that work lands.

## Files touched
- lib/screens/byok_setup_wizard.dart
- .gitignore
- scripts/process_gender_images.py
- test/widgets/wizard_steps/hero_creator_step_test.dart
- lib/screens/wizard_steps/magic_review_step.dart
- docs/MANUAL_TASKS.md

## Manual tasks
- Created: MT-139
- Closed:  MT-136, MT-064, MT-076, MT-063  (MT-069 part b done; task stays open for part a)

## Next session: start here
> Run **MT-139** (per-band Playwright verification sweep) once the parallel session's ~30 in-flight files are committed and deployed. Alternatively, pick up the 6 parallel-blocked backend code MTs (MT-098/046/047/083/129/070) once `story_tasks.py` / `story_service.py` / `story_routes.py` are committed.
