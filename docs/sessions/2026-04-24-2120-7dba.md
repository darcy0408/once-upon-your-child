# SESSION CLOSE — 2026-04-24 21:20 [7dba] — Branch: main — MT-013 BUG-002 retry cap + wasted-delay fix

## Accomplished

- **MT-013 closed** (`06ff9c3`) — `lib/services/app_tts_service.dart` `_prewarm()`:
  - Extracted magic number `4` into `static const int _maxPrewarmRetries = 4`
  - Added early `break` when `attempts >= _maxPrewarmRetries` — eliminates the wasted 16s backoff delay that fired after the final failed attempt with no subsequent retry
  - `dart analyze` clean; no behaviour change on the success path

## Still Pending / Deferred

- **MT-014** — BYOK runtime verify: Darcy must test with real `AIza…` key on a BYOK-subscribed account (wizard not accessible to anon/free users)
- **MT-012** — COPPA consent audit for 13–17 path (`welcome_screen.dart` vs `age_gate_screen.dart`); Opus-level risk, don't touch in a Sonnet session
- **MT-010** — stale JWT returns 403 instead of 401 from Stripe for real users
- **MT-006** — Recent Sessions table insertion order cosmetic fix

## Blockers

- None

## Files touched

- `lib/services/app_tts_service.dart`
- `docs/MANUAL_TASKS.md` (MT-013 closed)

## Manual tasks

- Created: none
- Closed: MT-013

## Next session: start here

> BUG-002 is fully resolved. Next highest-priority code item: MT-012 COPPA audit (assign to Opus — `welcome_screen.dart:998` diverges from `age_gate_screen.dart` for 13–17 path). MT-014 BYOK runtime needs Darcy's own BYOK account.
