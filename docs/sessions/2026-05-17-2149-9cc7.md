# SESSION CLOSE — 2026-05-17 21:49 [9cc7] — Branch: main — Railway deploy of Phase 4 + Celery -B revert

Continuation of session [ebfe] (Legal & Compliance audit). This stretch covered deploying
the Phase 4 work to Railway and an unsuccessful attempt to enable the Celery beat scheduler.

## Accomplished
- Verified the `main` push auto-deployed to Railway — `story-weaver-app` (backend),
  `lovely-perfection` (worker), `grand-light` (frontend) all reached SUCCESS.
- Confirmed the Phase 4 boot-migrations ran on production Postgres: `consent_record.policy_version`
  and `user.last_active_at` columns present (the `DuplicateColumn ... already exists` log is the
  benign idempotent re-run). No tracebacks in the backend deploy logs.
- Attempted to enable the Celery beat scheduler (`-B`) so the new data-retention job fires.
  Discovered `-B` (embedded beat) does not work with `--pool=solo`. The Railway agent then
  removed `--pool=solo` on its own — which risked breaking the worker's gRPC-based Gemini story
  generation (the prefork pool's `fork()` is gRPC-unsafe). Caught it and had the Railway agent
  revert the worker to its original known-good start command (`celery ... worker --loglevel=info
  --pool=solo`); deploy `9e4dafd5` SUCCESS, worker verified healthy.
- Reverted the `-B` edits in `railway.toml` + `Dockerfile.worker` — commit `8a4b1f26`. The
  railway.toml comment now explains beat must run as a separate service, not embedded.
- Filed MT-148 (set up a dedicated `celery beat` service). Its content landed in commit
  `45b831c4` (session 149a's close) via a shared-git-index race — verified correct and present
  at HEAD; tagged "created by ebfe" in the entry text.

## Still Pending / Deferred
- Celery beat is NOT running anywhere → the daily data-retention purge (CMP-5,
  `backend/tasks/retention_tasks.py`) never fires. Tracked as MT-148. Not urgent — the purge only
  targets accounts inactive 730 days.
- `git push` of `main` is outstanding (the permission classifier gates push-to-default-branch).
  At this session's start `8a4b1f26` + `45b831c4` were unpushed; this close adds the docs commit.
- The `feat/cost-reduction` branch still carries the pre-revert `-B` commit (`d364faee`) — if it
  re-merges to `main`, watch for `-B` reappearing in railway.toml / Dockerfile.worker.
- All owner-side items from the ebfe close remain open (MT-141 … MT-148).

## Blockers
- None.

## Files touched
- railway.toml
- Dockerfile.worker
- docs/MANUAL_TASKS.md (MT-148 — committed via 45b831c4 due to the index race)

## Manual tasks
- Created: MT-148 (filed this session; content committed in 45b831c4)
- Closed:  MT-140 (main deployed + Phase 4 migrations verified live)

## Next session: start here
> `main` is deployed and the Phase 4 backend code is live on Railway. The retention job will not
> fire until a dedicated `celery beat` service is created (MT-148). Everything else outstanding is
> owner work — decisions, real legal data, store-console forms (MT-141 … MT-148).
