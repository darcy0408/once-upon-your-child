# SESSION CLOSE — 2026-05-15 20:20 [0e2f] — Branch: main — MT-126 re-verify (agent verdict overturned) + MT-127 fix + MT-122 R2 backup shipped + MT-123/124 closed

## Accomplished

- **MT-127 fixed** (commit `a51dcf83`). `achievement_stats.last_story_date_iso` is `VARCHAR(10)` (the model comment documents `YYYY-MM-DD` — date-only was deliberate), but the Flutter client built the value from `DateTime.toIso8601String()` — a 23-char timestamp — so every `/achievement/sync` threw `psycopg2 StringDataRightTruncation` and the field never persisted. Fixed at the source rather than widening the column: `achievement_service.dart:351` now does `.toIso8601String().split('T').first`; `achievement_service.py` adds a defensive `[:10]` truncation for older app clients still posting full timestamps. Backend achievement tests 10/10 green. Prod logs confirmed the live bug red-handed (`last_story_date_iso': '2026-05-15T00:00:00.000'`).

- **MT-126 re-verified — themes-recall actually works; the prior FAIL verdict was invalid.** A background Playwright agent walked Explorer band twice for one character and reported FAIL ("zero `prior_adventures` log hits"). Root cause of the false negative: `backend/app.py:19` sets the root logger to `WARNING`, but both the `prior_adventures injected` (`story_tasks.py:795`) and `story_persisted` (`story_tasks.py:1277`) lines are `logger.info` — **production suppresses them entirely**, so their absence proves nothing. Independent evidence that the feature works: (1) Flutter wiring confirmed by code read — mapper → call site → `generateStory` body all plumb `character_id`; (2) HTTP logs show `PATCH /characters/96e9c820-…` immediately before `POST /generate-story`, so `wizardData.characterId` was populated; (3) a prod-DB count via `railway run` shows **`STORIES_WITH_CHARACTER_ID = 2`** — `character_id` reaches the backend and persists. Added a WARNING-level log so future recall is observable (commit `c253ef94`).

- **MT-122 shipped — external Postgres backup** (commits `3dabacab`, `5d412f41`, `18fbb028`). New `.github/workflows/postgres-backup.yml`: daily `pg_dump` (via `postgres:alpine` Docker image) → gzip → upload to Cloudflare R2 bucket `story-weaver-pg-backups`. Smoke-tested green via `workflow_dispatch`; dump verified present at `postgres/story-weaver-pg-2026-05-16.sql.gz` (filenames are UTC-dated). Daily 09:00 UTC cron enabled. Five repo secrets configured by Darcy. First image pin (`postgres:17-alpine`) failed — Railway Postgres is **v18.3** and pg_dump refuses a newer server; switched to `postgres:alpine` (latest, forward-compatible).

- **MT-123 closed** — ran the gap-window `SELECT count(*)` against prod Postgres: **0 rows**. No story data lost during the 2026-05-14 schema-migration gap.

- **MT-124 closed as wontfix** — Railway native volume backups require the **Pro plan**; this account is Hobby, so the Postgres Backups tab shows "No Backups" and no snapshot will ever land. The MT-116 `accept-deploy` could not have produced backups on Hobby. Superseded by MT-122.

## Still Pending / Deferred

- **MT-126 optional final nail.** A query script at `%TEMP%\mt123_count.py` (extended version) lists each `character_id`-bearing story + a distinct-character count. Running it once more (`railway run -s Postgres -- python …`) would 100% confirm both persisted stories share one character (proving recall fired on story #2). Evidence already strongly indicates yes; MT-126 was left closed (it was filed+closed by da24).
- **MT-120** — per-power visual override verify; still needs a paid-tier/BYOK account.

## Blockers

- The Claude Code auto-mode classifier blocks direct production Postgres queries even when the user approved via `AskUserQuestion` (it can't see that answer). Worked around by having Darcy run the `railway run` line himself via the `!` prefix. Note for future prod-DB reads: either add a Bash permission rule for `railway run`, or expect to hand the command to Darcy.
- The machine's `RAILWAY_TOKEN` env var is stale/invalid and shadows the working `railway login` account session — every `railway` invocation must be prefixed with `unset RAILWAY_TOKEN`.

## Files touched

- `lib/services/achievement_service.dart`
- `backend/services/achievement_service.py`
- `backend/tasks/story_tasks.py`
- `.github/workflows/postgres-backup.yml` (new)

## Manual tasks

- Created: none
- Closed:  MT-122 (done), MT-123 (done), MT-124 (wontfix), MT-127 (done)

## Next session: start here

> All five tickets from this session are resolved. If you want airtight MT-126 closure, run the extended `%TEMP%\mt123_count.py` to confirm the 2 stories share one `character_id`. Otherwise the highest-value open items are MT-128 (browser-verify resume/continue — parallel session's), MT-129 (illustration/character mismatch), and MT-120 (paid-tier per-power verify).
