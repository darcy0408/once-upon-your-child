# SESSION CLOSE — 2026-04-22 12:15 [c4ea] — Branch: main — BUG-003 Stripe anon guard + BUG-002 TTS backoff verification

## Accomplished
- **BUG-003 fix** (`6d71454`) — Added anon guard to `StripeService.getSubscriptionStatus()` (lowest-level HTTP boundary); `anon_` users now early-return `{status: inactive, tier: free}` without making any network call. Covers all 3 previously unguarded call sites: `subscription_service.dart:29`, `subscription_management_screen.dart:85`, `subscription_provider.dart:58`.
- **BUG-003 unit test** (`f60aafb`) — Added `stripe_service_test.dart`: verifies no HTTP call is made for `anon_` users (`verifyNever` on mock client).
- **BUG-003 deployed** (`7eff5de`) — Pushed to origin/main; Railway auto-deploy triggered; Railway deployment `b33e04b9` confirmed live.
- **PROJECT_STATUS updated** — Split BUG-002/003 into separate entries; marked BUG-003 ✅ fixed+deployed 2026-04-21.
- **BUG-002 TTS re-verification** — Fresh Playwright session monitoring 50 network requests confirmed: no 429 storm, 2 bounded bursts of 4–5 retries (9 total). Fix is working but no hard retry cap yet — follow-up needed.
- **TEAM_COORDINATION updated** — Added BUG-003 call-site audit table + BUG-002 re-verification results with verdict ⚠️ PARTIAL PASS.

## Still Pending / Deferred
- **BUG-002 retry cap** — `lib/services/app_tts_service.dart` ~line 147 `_prewarm()` loop has no hard cap. Second burst hit 5 retries vs ≤4 target. Fix: add `const _maxPrewarmRetries = 4;` guard. ~5-min change.
- **Stale-JWT 403** — non-anon user `user_c4b28920-cdb0-495d-ba08-db4197a09369` getting 403 from Stripe endpoint (should be 401). See MT-010.
- **BUG-001 browser confirm** — still needs incognito manual test (MT-005).

## Blockers
- Railway auth required interactive browser login (`railway login` non-interactive mode fails). Resolved by running `railway login` in a separate PowerShell terminal. No code blocker.

## Files touched
- `lib/services/stripe_service.dart`
- `test/unit/services/stripe_service_test.dart`
- `docs/PROJECT_STATUS.md`
- `TEAM_COORDINATION.md`
- `~/.claude/settings.json` (removed stale RAILWAY_TOKEN — not in repo)

## Manual tasks
- Created: MT-009, MT-010
- Closed: none

## Next session: start here
> BUG-002 retry cap is a 5-minute fix in `lib/services/app_tts_service.dart:~147` — add `_maxPrewarmRetries = 4` to the `_prewarm()` loop. After that, MT-005 (BUG-001 browser confirm in incognito) closes the last open QA bug.
